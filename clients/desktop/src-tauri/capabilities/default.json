{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the spike shell (core only; no plugins yet).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
