{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core:default, plus start_dragging for the web bundle's data-tauri-drag-region top bars (#2671). App commands need no entry here.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
