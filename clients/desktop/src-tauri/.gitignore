# Rust build output — never commit.
/target/
# Tauri codegen (schemas, etc.) — regenerated on build.
/gen/schemas/
# App-command permissions tauri-build writes from build.rs's list (#2772).
/permissions/autogenerated/
