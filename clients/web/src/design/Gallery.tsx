/**
 * DS-2 `#/design` gallery — stub for the red-proof commit (#1956).
 * The green commit replaces this with the component×state×scheme matrix.
 */
export const GALLERY_TEXT_FIXTURE = "short";

export function Gallery() {
  return <div data-testid="design-gallery" />;
}
