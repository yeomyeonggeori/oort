// =============================================================================
// 상태 칩의 기하 (design-taste-web §8: text-first, one token colour, no pulse).
//
// The TONE is per surface and stays with that surface's vocabulary map; the
// GEOMETRY is one line and belongs to nobody in particular, which is exactly why
// it kept being retyped. It was written out four times across the timeline and
// the workstream surfaces, so a chip that was 2px shorter than its neighbour was
// one dropped `py-px` away, in a file whose reviewer had no reason to look at
// the other three.
//
// Only the shape lives here. A chip that also imported its colour from a shared
// file would be one enum away from the timeline's 실패 and the workstream's
// 취소됨 sharing an ink they never agreed on.
// =============================================================================

export const CHIP_CLASS =
  "shrink-0 rounded-sm px-2 py-px text-timestamp font-medium";
