# momo — Gemfile
# fastlane 버전 고정(2025-05 Apple API 변경 대응 — 항상 최신 안정으로 갱신).
# CI: `bundle install` → `bundle exec fastlane <platform> <lane>`

source "https://rubygems.org"

gem "fastlane", "~> 2.228"   # ⚠️ Codex: 셋업 시점 최신 안정 태그로 갱신 후 Gemfile.lock 커밋

# fastlane 플러그인(선택): create-dmg 래퍼 등 필요 시 추가
# plugins_path = File.join(File.dirname(__FILE__), "fastlane", "Pluginfile")
# eval_gemfile(plugins_path) if File.exist?(plugins_path)
