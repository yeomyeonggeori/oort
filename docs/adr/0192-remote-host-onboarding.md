# ADR-0192: 원격 호스트 온보딩 — 한 줄 설치와 QR이 기본, Claude 공식 원격은 옵션, 설정 묶음은 0188 D6의 좁은 예외

- Status: **Accepted** (2026-09-26 성재 결재. 근거는 아래 인용)
- Date: 2026-09-26
- Deciders: 성재
- 결재 인용: 작업 공간 2.0 제안서 §4 Q1~Q11에 성재가 「전부 권장대로 가자」고 답했다. 이 ADR은 Q6, Q8, Q9, Q10, Q11을 적는다.
- 기안: Opus 5.5 worker(#2754)
- 근거 자료: 제안서 `claudedocs/agent-workspace-2.0/brief.md` §2.4(공식 원격 기능), §2.5(Railway), §3.7(원격 터미널 세팅 네 경로), §3.9(커넥터). gitignore 대상이라 로컬에만 있다.
- 증보: ADR-0125(호스트 등록 경로), ADR-0188 D6(설정 묶음 예외. 0188 D6 본문에 이 ADR을 가리키는 한 줄을 달았다)
- 관계: ADR-0171(봉인·write-only URL 모양), ADR-0180(QR 기기 연결), ADR-0113·0147(커넥터 커스터디), ADR-0157(샌드박스 네트워크), ADR-0191(계정 프로필)

## Context
- 셀프호스트 팀 인스턴스(Railway) 뒤에서 사용자가 「내 맥이 아닌 곳」에서 하네스를 돌리는 길은 넷이다. (i) oort 호스트 에이전트 설치, (ii) 호스트가 있는 사람에게 요청, (iii) Claude 공식 원격 위임, (iv) Railway 팀 샌드박스.
- 모두 0188 불변식 위에 서야 한다. 결정자 = 소유자, 원격 셸 금지, 모드 고정, 서버는 바이트를 거치지 않는다.
- 0188 D6은 원격 세션이 프로젝트 hooks·MCP·allow 규칙을 읽지 않게 막는다. 그런데 원격 호스트로 작업 세팅(skills, MCP, CLAUDE.md, dotfiles)을 옮기고 싶다는 요구가 있다.
- Claude Remote Control·Routines는 oort 밖에서 권한을 처리한다. 그 작업의 승인은 oort 원장에 남지 않는다.

## Options
| 경로 | 사용자 단계 | 보안 | 비용 | 판단 |
|---|---|---|---|---|
| **(i) 한 줄 설치 + QR** | 3~4 | outbound만, 서명 heartbeat, 소유자 결정 | 0(사용자 기계) | **기본으로 채택** |
| (ii) 연동 요청 | 1 + 받는 쪽 | 경로 자체는 무해 | 0 | **안내로 채택**(D6) |
| **(iii) Claude 공식 위임** | 1~2 | Anthropic 중계. 원장에 승인이 남지 않는다 | 구독만 | **옵션으로 채택**(D4) |
| (iv) Railway 팀 샌드박스 | 2~3 | VM 격리, 공용 호스트의 신원 규칙이 필요 | 상시 약 $150/월 | **목표 A 뒤**(D7) |

## Decision

### D1. 기본 경로는 한 줄 설치와 QR 페어링이다
1. 데스크탑 설정 「작업 호스트 → + 호스트 추가」.
2. 화면에 한 줄 명령(`curl -fsSL https://<팀서버>/install-workd.sh | sh -s -- --pair <코드>`)과 같은 코드의 QR이 뜬다.
3. 원격 기계(맥 미니, 리눅스 VPS)에서 실행하면 workd가 팀 서버로 outbound 등록한다.
4. 데스크탑에 「team-box 연결됨 · claude ✓ · codex ✗(로그인 필요)」가 뜬다. 하네스 로그인은 원격 기계에서 본인이 한다(ADR-0191).
- QR은 원격 기계가 찍는 것이 아니다. 데스크탑 화면에서 폰으로 「이 호스트를 폰에서도 보기」를 넘기는 용도다(ADR-0180 문법).
- 설치 스크립트와 workd 리눅스 바이너리는 서명한다.

### D2. 페어링 코드 계약
- 페어링 코드는 **짧은 수명(분 단위)의 1회용**이고 발급한 **소유자 계정에 묶인다.** 소비되면 member-scope 호스트가 그 소유자 것으로 등록된다.
- 재사용, 만료, 다른 계정의 코드는 거부한다. 구현은 셋을 red proof로 잠근다.
- host 등록 사실은 소유자의 모든 기기에 알린다(0188 D2).
- 발급·소비 라우트는 새 공개 API다. 이 ADR이 그 근거다. 토큰은 해시로만 저장한다.

### D3. 설정 묶음은 0188 D6의 좁은 예외다 (Q10)
원격 호스트에 작업 세팅을 옮기는 「설정 묶음」을 연다. 조건은 아래 전부다.
- **소유자가 데스크탑에서 명시적으로 고른 항목만** 들어간다.
- 각 항목은 **해시로 고정**한다. 호스트는 해시가 다르면 적용하지 않는다.
- **hooks와 allow 규칙은 제외**한다. 권한 다리를 우회하는 길이기 때문이다.
- 묶음으로 들어간 **MCP 서버의 도구 호출도 ACP 권한 다리를 거친다.**
- **시크릿은 묶음에 넣지 않는다.** MCP의 OAuth는 그 호스트 안에서 연결 카드로 한다(0125 D9).
- 묶음은 선언형 파일(`oort-host-profile.json`)이다. 대상은 skills, 플러그인 마켓플레이스 항목, MCP 서버, CLAUDE.md·AGENTS.md, dotfiles 저장소다.
- 이 조건 밖의 프로젝트 hooks·MCP·allow 규칙은 0188 D6대로 계속 읽지 않는다.

### D4. Claude 공식 원격 위임은 옵션이고, 원장 비대칭을 받아들이고 표시한다 (Q6)
- **로컬 칸:** L 칸 머리의 「폰에서도 이어 보기」 버튼이 `claude --rc`로 Remote Control을 켜고, 세션 URL을 내 DM이나 채널 카드로 공유한다. oort는 Claude 토큰도 바이트도 만지지 않는다.
- **클라우드:** 에이전트 멤버 「Claude(클라우드)」를 Routines 연결로 등록한다. 운영자가 루틴 URL과 토큰을 붙여 넣고(ADR-0171과 같은 봉인·write-only), 멘션하면 `fire`를 부르고 받은 세션 URL을 카드로 단다.
- **비대칭:** 이 경로로 한 작업의 권한·승인은 **oort 원장에 남지 않는다.** 이를 받아들이고, 카드에 「이 작업은 Claude에서 승인합니다」를 표시한다. 감사가 필요한 작업은 A 레인(workd)을 쓰라고 안내한다.
- 상태 웹훅이 없으므로 카드는 「시작됨 · Claude에서 보기」까지만 안다. Codex는 링크 공유까지만 한다.
- 순서: Remote Control 버튼은 로컬 워크벤치(M1)와 함께, Routines 연결은 M4에.

### D5. 로그인이 든 체크포인트는 공유하지 않는다 (Q11)
- 로그인이 든 샌드박스 체크포인트나 호스트 이미지는 **소유자 한 사람의 것**이다. 팀원에게 포크하거나 공유하지 않는다. 공유하면 구독 풀링이 되어 Anthropic·OpenAI 약관에 걸린다.
- 팀 공용 호스트라도 세션마다 **그 세션 소유자의 로그인만** 싣는다.
- 팀 공용 에이전트가 꼭 필요하면 구독이 아니라 API 키 과금 경로(Managed Agents, provider 체인)로 간다.

### D6. 호스트가 없는 사람에게는 「팀에 요청」 안내를 준다
- 비개발자가 「코드 실행이 필요합니다 · 연결된 호스트가 없습니다」를 만나면 [팀에 요청]을 누른다.
- 결정자 = 소유자 불변식 때문에 **남의 member-scope 호스트는 빌려 쓸 수 없다.** 요청의 답은 요청자 본인의 호스트나 팀 공용 호스트다.

### D7. 이번에 정하지 않는 것 — 목표 A 뒤 과제
- **팀 샌드박스 호스트(scope=workspace, Railway Sandbox)와 팀 공용 에이전트(Q8):** 목표 A는 「내 맥에서 내 계정으로」(0188)까지다. Railway scope=workspace 호스트의 격리 수준, 결정자 규칙, 0157 재실측, 비용은 목표 A 뒤에 새 ADR(A4)로 정한다. 스파이크는 #2788.
- **커넥터 순서(Q9):** 웹훅 레시피(#2783), ADR-0186 행동 카드, 하네스 MCP 보기(#2784)를 먼저 한다. oort 서버가 토큰을 쥐는 MCP 카탈로그는 **커넥터 커스터디 재정합 ADR(A5, 0113·0147·0188 D6)** 뒤로 미룬다. A5도 목표 A 뒤다. 행동 명령 #2785는 A5를 기다린다.

### 확인 항목
- A 레인이 공식 바이너리가 아니라 ACP 어댑터(claude-agent-acp, codex-acp)로 하네스를 띄우는 경로가 약관의 같은 편에 서는지는 확인하지 못했다. 0188이 이미 채택한 경로라 재론하지 않고, M4 착수 전(#2786) 확인해 이 ADR에 증보로 적는다.

## 업계 비교
- **Orca:** SSH 첫 연결에 relay 데몬을 설치하고, 헤드리스 서버는 `orca serve --pairing-address`와 클라이언트별 폐기 가능한 토큰으로 붙는다. 에이전트 CLI 로그인은 서버에서 한다. oort는 Tailscale 대신 팀 서버로 outbound 다이얼한다.
- **Claude Remote Control:** outbound HTTPS만, 폰에서 권한 응답. oort는 이것을 대체하지 않고 링크로 연결한다.
- **Claude 클라우드 세션·Cursor·Railway:** 작업 세팅은 레포 커밋(`CLAUDE.md`, `.mcp.json`), `environment.json`, 체크포인트로 옮긴다. oort의 설정 묶음은 그 교집합이다. 시크릿은 어느 쪽도 묶음에 넣지 않는다.
- **Codex:** `codex remote-control`과 app-server로 ChatGPT 앱이 조종한다. oort는 링크와 상태 표시에서 멈춘다.

## Consequences
- (+) 원격 호스트가 비용 0으로 모든 ACP 하네스를 원장 감사와 함께 돌린다.
- (+) Claude 사용자는 설정 없이 폰에서 이어 볼 수 있다.
- (−) 위임 경로는 oort 원장에 승인이 남지 않는다. 카드로 정직하게 표시한다.
- (−) 설정 묶음은 0188 D6에 예외를 만든다. 조건 하나라도 빠지면 권한 다리 우회가 된다. 구현은 hooks·allow 거부, 해시 불일치 거부, MCP 도구 호출의 권한 다리 경유를 red proof로 잠근다(#2787).
- 파생 이슈: #2786(한 줄 설치·QR), #2787(설정 묶음), #2789(Remote Control 버튼), #2790(Routines 연결), #2791(팀에 요청), #2788(팀 샌드박스 스파이크, 목표 A 뒤), #2785(커넥터 행동, A5 뒤).

## 결재 기록
- **2026-09-26 성재:** 「전부 권장대로 가자」. 이 ADR에 들어간 권장안:
  - Q6 (i) 한 줄 설치 + QR 페어링을 기본으로, (iii) Claude 공식 위임을 옵션으로. 위임 작업이 원장에 남지 않는 비대칭은 받아들이고 카드에 표시한다.
  - Q8 팀 공용 에이전트는 Railway scope=workspace 호스트에서, 새 ADR로 결정자와 과금을 정한 뒤. 목표 A는 「내 맥에서 내 계정으로」.
  - Q9 커넥터는 웹훅 레시피·행동 카드·하네스 MCP 보기를 먼저, MCP 카탈로그는 커스터디 ADR 뒤.
  - Q10 설정 묶음을 0188 D6의 좁은 예외로 연다. 조건은 D3.
  - Q11 로그인이 든 체크포인트를 팀원과 공유하지 않는다.
