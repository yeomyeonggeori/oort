# 계획 이탈 로그 (Deviation Log)

> 목적: 구현이 기획(티켓 수용기준·ADR·아키텍처 정본)과 달랐던 지점을 놓치지 않고 기획 레이어로 환류한다. (`docs/planning/README.md` §5)
> 기록 주체: 오케스트레이터(momo-main), 머지 시점. 판정 주체: 기획 레이어(+성재 승인).
> 상태: `pending`(판정 대기) → `accepted`(정본 반영) / `rejected`(복구 티켓) / `noted`(기록만).

| 날짜 | 출처 (티켓/PR) | 이탈 내용 | 오케스트레이터 분석·리서치 | 상태 | 판정·후속 |
|---|---|---|---|---|---|
| 2026-07-10 | MOMO-337 / PR #310 | 핸드오프는 `/gateway/jobs/pending`을 기존 경로로 전제했으나 실제 코드에는 없었다. 수용기준의 4-route bearer 이관을 닫기 위해 actor-bound pending recovery endpoint를 신설했다. | Postgres/outbox SoT와 REST 경계를 유지하고 `available_at <= now()` 및 token actor binding을 강제하므로 제품 경계 변경이 아닌 누락된 recovery surface 보완으로 판정했다. 다만 bearer 사용 audit write 증폭을 피하려면 adapter가 realtime-first여야 한다. | `accepted` | MOMO-338 #308에 realtime-first, bounded reconnect/recovery, idle tight polling 금지를 추가. |
| 2026-07-10 | MOMO-338 / #308 | 어댑터 bearer 단일화 리뷰에서 기존 `agent:` namespace가 user-visible status/partial과 Context Packet job을 함께 운반해 같은 채널 관찰자에게 private work가 노출되는 문제를 발견했다. Python 범위를 넘어 `agentwork:` namespace와 서버 proxy/config를 추가했다. | `agent:` 전체를 self-only로 만들면 기존 macOS working UX와 MOMO-212 live gate가 깨진다. 따라서 ADR-0101에 observable progress와 exact-actor private work 분리를 반영하고 dev/local-alpha/prod config를 함께 갱신했다. 다중 gateway 인스턴스의 provider 중복 실행은 durable claim/lease 없이는 닫히지 않아 후속 서버 티켓으로 분리한다. | `accepted` | MOMO-338 merge에 `agentwork:` self-only regression 포함. 후속: MOMO-341 claim/lease + takeover. |
| 2026-07-17 | MOMO-411/412 게이트 (선재 발견) | runtime-db 게이트의 `MomoWindowChromeSnapshotTests`(dark/narrow/overlay light) 4건이 origin/main HEAD에서 이미 FAIL — UX 트랙 window chrome 최근 머지분의 환경 의존 스냅샷 drift. 411/412(server·tooling만 변경)와 무관. | origin/main HEAD 격리 재현으로 선재 확정. 엔진 트랙 게이트가 이 때문에 무한 대기하지 않도록 서버 표면 단독 verifier로 머지 판정. | `accepted` | #448이 레퍼런스 갱신으로 1차 해소, MOMO-448(#450)이 stale light 캐노니컬 재기록 + ApprovalInbox close-crop + 신규 3서피스 스냅샷 11장으로 종결. overlay light는 offscreen NSWindow 폴백 렌더로 전환해 headless 재기록 가능해짐. |
| 2026-07-17 | MOMO-412 / PR #443 (리뷰 후속) | webhook native secret KDF의 master key가 `config.jwtHMAC` 재사용(M2) — JWT secret 회전 시 발급된 모든 native webhook secret이 조용히 무효화되는 운영 결합. per-install rate limit 부재(M1)로 토큰 유출 시 채널 flood 상한이 회전/revoke뿐. | 암호학적으론 도메인 분리(`momo.webhook.native.v1\n`)로 안전하나 운영 결합은 실재. infra/prod 무변경 계약상 v0 수용, 후속 분리 권고. | `pending` | `WEBHOOK_MASTER_KEY` 분리 + per-install rate 예산 후속 티켓 제안(성재/판정 대기). |
| 2026-07-21 | W-2 / PR #561 | 패킷은 "read-only v0, 쓰기=read-state만"으로 한정했으나 worker가 실전송 컴포저(sendMessage + draft-attempt당 clientMsgId idempotency)까지 구현. | 코드 품질이 L4 §3.1 idempotency 계약을 정확히 준수하고, 오케스트레이터가 실서버 실전송 왕복을 검증(PASS). 되돌리는 비용 > W-4 선행 수용 이득으로 판정. | `accepted` | W-4 발급 시 "컴포저 기구현" 반영해 범위 축소(read-state·승인 카드·recovery 왕복 중심). |
| 2026-07-15 | MOMO-390 / PR #403 (선재 발견 보고) | 이 PR의 이탈이 아니라 main 기저(e35be71)의 기존 결함 발견: `verify_staging_smoke.sh`/`verify_internal_hosting_smoke.sh`가 `agent/ch/dm/user` 4개 namespace만 기대해 `centrifugo.prod.json`의 `agentwork` namespace(MOMO-338 도입)와 불일치 — 두 스크립트가 main에서 이미 FAIL. | MOMO-338이 namespace를 추가할 때 두 smoke 스크립트의 기대 목록이 갱신되지 않은 gate drift. 제품 경계 무관, 스크립트 기대값 갱신만 필요한 소형 tooling 수정. | `accepted` | MOMO-399 발급(staging/internal smoke namespace 기대 갱신, ADR 불요) — 엔진/인프라 트랙 위임 범위 내 판정. |

| 2026-07-18 | MOMO-471 / PR #494 (게이트 선재 발견) | `testWorkspaceSearchLightSnapshot`가 격리 통과·full `make test` 실패(폰트캐시/GPU 순서 의존 렌더 비결정, precision 0.98 미세 초과). origin/main에서도 재현 — V-3 무관. | V-3 자체(huddle/Core 34 test) PASS 확인 후 표면 무관 flake로 판정, 재기록 커밋 revert해 PR을 huddle 전용 유지. MOMO-411/412 선례(선재 스냅샷 FAIL 분리)와 동일 처리. | `pending` | MOMO-472(#495) 안정화 티켓 발급 — full-suite 3회 통과 증명 필요. |

| 2026-07-18 | MOMO-474 / PR #499 (게이트 선재) | server 전용 PR인데 runtime-db 게이트의 `testChannelCreationSheetEnglishLargeTextSnapshot`(macOS)가 full-suite flake로 실패. server 변경은 macOS 렌더에 영향 불가 = 선재. | 첨부 verifier+서버 107 test+openapi+실 Google smoke PASS 확인 후 track/engine 머지. MOMO-472(#495) 범위를 이 스냅샷까지 확장. | `pending` | MOMO-472에서 두 스냅샷 결정적 렌더 이관. |

| 2026-08-05 | #1042 / PR #1058 (SRV-B7) | 이탈 3건: ①패킷 파일 허용 목록 밖 `openapi_shape_check.py` 가산(`--sampled-elsewhere` — known-unsampled와 의미 반대라 재사용 거부, 겹침 가드 포함) ②패킷 「함정」 전제 오류(repo에 infra/rust 컴포즈 기존재 — 오케스트레이터의 main 체크아웃 실측 오류) ③「예상 빨강」 재해석(빨강은 Rust가 아닌 Swift 선존재 — #1040이 스펙을 Rust 와이어로 맞춘 예정된 귀결. 2차 패스만으론 미해소, 잠식 기제=「등재=응답 모양 권위 이전」 동시 구현 필요). | ①부채(줄어들 수만)/권위 이전(늘어날 수만) 의미 분리 논거 타당 — 뭉개면 게이트가 거짓말을 배움. red proof 2종+겹침 가드 실측 확인. ②재발 방지 교훈: 경로 실측은 origin/track/engine 대상(JOURNAL 기록). ③해석이 완료조건 ①③의 유일한 정합적 동시 성립 — merge-base 선존재 증명 확인. 전 게이트 2연속 green·cargo 713/0. | `accepted` | 오케스트레이터 mid-flight 승인 3건 전부 추인. 후속: 부분집합에 relay/agent-worker 합류(매니페스트 확장 지점)·pgdata 고정이름 볼륨 함정 런북 반영. |

| 2026-08-15 | #1409 / LIVE-1 (동결 bcc4882d) | ADR-0165 D1의 1순위(Selkies-GStreamer) 대신 `gstreamer-webrtcbin`을 템플릿 사양에 선언. 근거=성능이 아니라 D4: Selkies는 입력 datachannel이 제품이라 view-only가 "채널 부재"가 아니라 "설정"이 됨 — D4의 구조적 요구와 상충. 구조 논거이며 실측 아님. | 논거 타당 판정(D4 "입력 채널 자체 미개설"의 유일한 구조적 성립형). 되돌리기=template.spec.json 한 필드(서버 코드는 producer 이름 무결속). ADR-0165가 아직 Proposed라 초안 D1을 webrtcbin 선언으로 갱신 — 성재 Accept 시 그 형태로 확정. | `accepted` | ADR-0165 D1 갱신(2026-08-15). 실측 확정은 전용 호스트 실기동 시(runtime-unverified 라벨 해소와 동시). |
| 2026-08-15 | #1409 / LIVE-1 (동결 bcc4882d) | 브라우저→샌드박스 도달성(시그널링 WS·ICE)이 미실측·구조적 위험: cubesandbox `create_body`에 포트 노출 표현이 없고 ADR-0157 증보 1 실측은 VM이 닫힌 채(eBPF deny_out) 출고됨을 확인. 워커가 3형상(전용 호스트 리버스 프록시 / oort 운영 TURN=D3 증보 / 샌드박스별 공인 포트)을 기록만 하고 미선택. | 서버 축 계약은 형상 무관 완결 — 단 **LIVE-2가 실화면을 그리려면 이 결정이 선행**. 추측 금지 규율 준수 정당. 전용 호스트에서 30분급 도달성 스파이크(U1 선례)로 형상을 실측 후 택하는 것을 권고. | `accepted` | 성재 결재(2026-08-15): **스파이크 선행** — 전용 호스트 30분급 도달성 실측 후 3형상 중 실측 근거로 확정, ADR-0165 D3 증보로 기록. 스파이크 티켓 발급. LIVE-2는 로컬 모의 시그널링으로 UI 계약까지 병행. |
| 2026-08-15 | #1409 / LIVE-1 (동결 bcc4882d) | display에 controller 등급이 없어 `owner_only` 세션의 소유자가 **자기 화면을 볼 수 없음**(PTY는 소유자=controller 문으로 도달). owner 예외는 신규 권한 모델이라 발명 금지 규율대로 fail-closed 거부 구현+테스트 고정. | `owner_only`의 의미("소유자만 본다" vs "아무도 안 본다")는 제품 결정. fail-closed가 안전 방향이라 v0 수용 타당. | `accepted` | 성재 결재(2026-08-15): **LIVE-3와 묶기** — fail-closed v0 확정, owner 예외는 control 개방(ADR-0004 증보 3 Accepted)과 같은 권한 표면에서 함께 결정. |

| 2026-08-15 | #1412 / LIVE-2 (동결 3d96221a) | 이탈 5건: D1 60초 재검증을 토큰 재발급이 아닌 원장 재검증으로(재발급 루프는 관전자당 분당 realtime 프레임 발행 — observerCountLabel이 거절해 둔 거래·producer가 stream:true 재검증 담당) · D2 카피 web-local(폰 카운터파트 부재·PTY 선례 동형) · D3 폰 파일 6개 수정(required 필드의 typecheck 파급 — 가드 정규식은 강화 방향) · D4 협상 타임아웃 30초(미실측 명시)+ICE candidate 버퍼 추가 · D5 owner_only 자기 문장(터미널 문장 재사용은 반쪽 진실). | D1은 패킷 문구("PTY 동형 재발급 루프")가 부정확했던 것 — PTY에 재발급 루프 없음, 워커 교정이 맞다. D3는 타입 파급 불가피+배제 가드 강화라 "폰 범위 밖" 취지 유지. D4 타임아웃 상수는 #1411 스파이크가 실측 대체. 전 게이트 그린(web 984·core 1425·폰 1160·design 프리플라이트 13/13). | `accepted` | 기획 레이어 수용 5건. 후속: #1411이 D4 타임아웃 실측 대체 · 선재 red(co-open-900 236px)는 #1413 발급. |

| 2026-08-16 | #1424 / LIVE-3 (동결 6cf952dc) | 이탈 3건: ①패킷 §1-2 "capability 만료 시 창 닫힘"을 60초 TTL 문자 해석하면 D3 위반(stream:true 재검증이 만료 절을 완화 — 살아있는 스트림은 만료 행 위에서 돎, 로그인 도중 60초에 에이전트 재개) → 창 자체 lease 90초(재검증 3회)+controller 재검증 갱신으로 대체 ②run 층 파킹 미구현 — RunStatus::Paused 배선 기존재·writer 부재, 게이트(거부)는 구현 ③라우트 밖 세션 종료는 lease ≤90초 backstop(momo-t3 크레이트가 outbox 불가 — 침묵 종료 회피). | ①패킷 문구 부정확이 원인(LIVE-2 D1과 동형 패턴 — 기제 세부를 검증 없이 패킷에 박지 말 것, 교훈). 워커 교정이 D3 보존의 유일 정합. ②D3(비관측)는 게이트로 성립 — 에이전트의 세션 유일 경로(work-controls)가 차단·시도 무흔적. 단 D6 "토큰 0"은 파킹 없이는 불완전 → **LIVE-4 발사 전 선행 조건**으로 #1425 발급. ③90초 유계·정직 명시 — 수용. | `accepted` | ①③ 수용. ②조건부 수용 — **#1425(run 파킹 writer)가 LIVE-4 선행**. D5(egress grant)는 패킷 범위 밖 명시 — egress 파도(P1~P7)에서. |

| 2026-08-16 | #1425 / PR #1427 (랜딩 8ce5ad38) | 이탈 3건: ①live3_2 주어 분리 — 파킹 run의 거절 문장이 세션 게이트와 분화(`agent run is not eligible for work control`) ②in-process 워커 턴은 파킹이 중간에 못 끊음(claim된 턴은 완주 커밋 — 정지는 다음 턴부터) ③queued run 비파킹(이슈 계약 문자 준수 — 소비 없음·복원 칸 부재·LIVE-3 게이트 2종이 세션을 지킴). | ①에이전트-facing 문장이라 LIVE-4 UX 비접촉(우로보로스 인터뷰에서 확인) — 기록만 ②정직 라벨 `runtime-unverified(in-process worker turn)` 명기, 워커 커밋 경로 가드는 승인 hold 상호작용 재설계라 별건 ③근거 타당(D6 주어=소진). | `accepted` | 3건 전부 수용. grok freeze가 별건 H1(교차-세션 run 잠금)을 적발 — 수리·재-freeze 무결 후 랜딩(잠금 순서 계약 session/host→window→run 성문화). |

| 2026-08-16 | #1428 / LIVE-4 (동결 abf53209) | 이탈 2+경계 1: ①카드 터미널 4종(`stopped` 추가 — 중단은 창이 존재한 적 없는 종결이라 end_reason 3종에 욱여넣으면 거짓 진술, 기존 어휘 "중단됨" 재사용) ②에이전트 방출 도구 신설(패킷 좌표에 없던 emitter — 스키마 무변경·`ApprovalReason::HumanIsTheAction`은 면제 분기 밖이라 사람 건너뛰기 불가, 오케스트레이터 코드 확인) ③세션 표면 control 내구 투영 미구현(3투영 드리프트 가드 vs bare RETURNING 충돌 — 리로드 후 세션 상세는 무표시가 정직, 카드는 props로 내구). | ①정확성 우선 판정 — 상태 수 최소화보다 거짓 진술 회피가 상위 규율 ②최소·구조적 안전 확인 ③"어느 읽기가 control 상태의 SoT인가"는 세션 표면이 control 표면이 되는 LIVE-5의 설계 문제 — 지금 풀면 두 번 품. | `accepted` | ①② 수용. ③ **LIVE-5 예약 스코프에 편입**(`research/2026-08-16-live4-interview-and-plan.md` §3에 항목 추가). |

| 2026-08-17 | #1454 / PR #1460 (UXC-A 프로듀서) | 이탈 5건: D1 트리거=「모델이 ` ```oort:report ` 펜스로 선언」 설계 상신(패킷 §9 미결정 항목 — 부수효과: 전 에이전트 턴 시스템 맥락 +~6줄, config 플래그는 후속) · D2 승인 기계·툴 카탈로그 비접촉(대가: 리포트 저작 중 스트리밍이 절단선에서 정지) · D3 `cargo fmt --check` 기준 커밋에서 선재 red 11건(미접촉 6파일 — rustfmt 버전 스큐, #1442 계열) · D4 `hosted_identities_…` PG 테스트 같은 DB 재실행 시 선재 실패(고정 토큰 해시 유니크 충돌 — diff 무관) · D5 웹 카드 렌더 육안 미확인 `runtime-unverified` 정직 라벨(conformance로 갈음 — 패킷 §6 허용). | D1: 서버 대리 지표 분류의 오탐 방향 분석이 타당, 「서버는 검증·전달만」 원칙과 정합 — 수용, config 플래그 후속 티켓. D2 수용(카드가 서며 해소). D3·D4는 선재 결함 분리 규율(MOMO-471 선례) — 별건. D5 수용. **Fable 검수 M-1 적발**: 펜스 맨-끝 미강제 → 닫는 펜스 뒤 산문 삭제+외곽 코드블록 안 예시 펜스의 카드 승격 — 수리 회전 1 완료(`7ac5dd47`, red 3+뮤테이션 증명). **grok freeze(C0/H1/M2/L1) 재판정 전부 실결함**: H(본문·summary 없는 유효 리포트가 원시 펜스 JSON을 채널에 커밋) · M(안 닫힌 외곽 펜스 안 예시의 카드 승격 — 회전 1 잔여 구멍) · M(ZWSP/BOM 태그 검출 우회) · L(elapsed의 DB·프로세스 시계 혼합) — 회전 2 완료(`4516a243`: 폴백 산문→summary→title·body 없는 카드 커밋 nullable·Refused 비대칭 의도 유지 — 재-freeze 동의). 재-freeze가 가족 변형 2건 추가(물결 펜스·틱 앞 제로폭) → 회전 3(`2592244d`: FenceMark 2문법 상태 기계 — CommonMark 완결, 뮤테이션 증명 누적 8) → **track/engine@30186176 랜딩**(PR #1460·이슈 close). L-1(시계)은 같은 SELECT의 now() 투영으로 수리(뮤테이션 증명 불가 — 정직 라벨). | `accepted` | 회전 후 랜딩. 후속 티켓 후보: ①REPORT_PROTOCOL config 플래그 ②D4 선재 PG 테스트 재실행 내성 ③goal_claim.sh가 track/engine이 아닌 origin/main에서 분기(워커 실측 — 다음 엔진 워커 동일 함정). |
| 2026-08-17 | #1442 / PR #1459 (toolchain 정합) | 이탈 5건: D1 MSRV 정직 상향(1.80→1.88)이 `clippy::manual_is_multiple_of` 1건 점화(`korean.rs:116`) — 소스 비접촉 계약대로 정지 · D2 스코프 확장: desktop 워크스페이스 `rust-version` 1.77.2→1.89.0(실측 바닥 — 패킷 미지목 발견) · D3 트랙 경계: 엔진 PR이 UXUI 소유 `clients/desktop` 1값 수정 · D4 게이트 선존 조건으로 `momo-tracks/engine`을 clean 확인 후 `--ff-only`만 수행 · D5 미수정 보고 4건(MSRV CI 게이트 부재·부동 `rust:1-slim-bookworm`·release-desktop 하한 미검사·`fmt --check` 문서 결함=#1377 소관). 핵심 실측: 레포에 rust-toolchain 파일 0개 — 커서의 "고정 1.83"은 커서 환경 기본값, 실결함=**양쪽 워크스페이스의 거짓 `rust-version` 선언**(브래킷 실행으로 바닥 확정: server=1.88.0/desktop=1.89.0). rust-toolchain.toml 비신설 판정 3근거. | D1: momo-main 계약 해제 — #1460이 `korean.rs` 비접촉 실측(충돌 0), lint 실수리가 정직 방향 → 수리 지시(2026-08-17). D2·D3: 전수 조사의 자연 귀결, 최소 diff — 수용(UXUI 트랙엔 이 로그가 통보). D4 수용(새 커밋 0·push 0). D5는 발견 보고 규율 준수 — 별건 큐. ⚠ CI rust 레인이 clippy를 안 돎(pr-ci.yml) — CI 초록≠머지 신호, 로컬 게이트 증거 필수(기존 규율 재확인). | `pending` | 회전 후 랜딩(머지 순서 #1454 뒤). 후속 티켓 후보: ①CI rust 레인 clippy 편입 ②MSRV 브래킷 게이트 ③부동 베이스 이미지 고정 여부(성재 운영 결정 큐). |

| 2026-08-17 | #1441 / PR #1462 (UXC-C 세션 칩) | 이탈 6건: D1 `goal_claim.sh`가 origin/main에서 분기(재지정으로 해소 — #1454 워커와 동일 함정, 스크립트 결함) · D2 검증 칩을 목록 행에는 안 세움(행은 스레드를 안 읽음 — 세션마다 /replies 추가 읽기=read-model 결정이라 티켓 분리 제안) · D3 세션 스레드 밖 리포트는 세션 것이라 주장 안 함(`runtime-unverified(producer→session thread placement)` — #1454 프로듀서가 채널 본문에 저작하면 칩 영구 부재) · D4 패킷의 pnpm→실레포 npm 교정 · D5 STATUS.md 미갱신(병렬 충돌 회피) · D6 캡처 레인 추가(#1440 선례). | D1: 후속 티켓(스크립트 base 수정). D2: read-model 티켓으로 수용 — grok H2(5×200 창 절단으로 초장기 세션 칩 영구 부재)도 같은 티켓에 묶음. D3: 랜딩 후 실기동 실측 항목. D4~D6 수용. **리뷰 폐곡선 적발**: design-review High 1(320px 상세 머리 제목 붕괴 — 이 PR이 세운 shrink-0 2항목 소산) + grok 실결함 2(G-H1 ended 프레임이 events 캐시 미무효화·주석-코드 불일치 / G-M1 코어 상한 절단 표를 접어 「통과 40」 거짓 — omitted 무시) → 3건 묶음 회전 지시(2026-08-17). design-review Medium(통과 세션 초록 칩 2개 공존)은 워커 판단 지지 — `SESSION_STATUS_CLASS.done`의 ok 유지 여부는 코어 역할표 결정이라 성재 큐. 회전 3커밋 완료(320px 수리 실측 127→202px·폭 회귀 숫자 가드·G-H1/G-M1 red proof) → design-review 재확인 PASS(B0/H0)·재-freeze C0/H0/M0/L0 → **track/engine@b9cfbf69 랜딩**(PR #1462·이슈 close). | `accepted` | 랜딩 완료(2026-08-17). 후속 티켓 후보: ①세션 검증 read-model(목록 행 칩+장스레드 최신 리포트 — D2+grok H2) ②goal_claim.sh 트랙 base ③종료 상태 칩 muted 전환 여부(성재/코어 표) ④경과 명칭 3종(「동안 작업」·「실행 시간」·「작업 시간」) 정렬+「1초 미만」 카피. |

## 소급 항목 (2026-07-09 감사에서 발견된 역사적 이탈)

아래는 이 로그가 없던 시기의 이탈로, ADR 큐에 이미 배정됨 — 참고용.

| 출처 | 이탈 | 배정 |
|---|---|---|
| MOMO-256 | 김인턴 → Hermes 정체성 교체 (근거 무기록) | ADR-0106 |
| MOMO-325~333 | gateway 경로의 사실상 기본화 (계약 문서와 모순) | ADR-0102 |
| LOCAL_SOLO 레인 | M2 멀티팀 → 로컬 솔로 피벗 | ADR-0103 |
| 2026-06-26 | Actions 중단 → 로컬 게이트 머지 권위 관례화 | ADR-0107 |
