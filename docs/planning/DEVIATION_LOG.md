# 계획 이탈 로그 (Deviation Log)

> 목적: 구현이 기획(티켓 수용기준·ADR·아키텍처 정본)과 달랐던 지점을 놓치지 않고 기획 레이어로 환류한다. (`docs/planning/README.md` §5)
> 기록 주체: 오케스트레이터(momo-main), 머지 시점. 판정 주체: 기획 레이어(+성재 승인).
> 상태: `pending`(판정 대기) → `accepted`(정본 반영) / `rejected`(복구 티켓) / `noted`(기록만).

| 날짜 | 출처 (티켓/PR) | 이탈 내용 | 오케스트레이터 분석·리서치 | 상태 | 판정·후속 |
|---|---|---|---|---|---|
| 2026-07-10 | MOMO-337 / PR #310 | 핸드오프는 `/gateway/jobs/pending`을 기존 경로로 전제했으나 실제 코드에는 없었다. 수용기준의 4-route bearer 이관을 닫기 위해 actor-bound pending recovery endpoint를 신설했다. | Postgres/outbox SoT와 REST 경계를 유지하고 `available_at <= now()` 및 token actor binding을 강제하므로 제품 경계 변경이 아닌 누락된 recovery surface 보완으로 판정했다. 다만 bearer 사용 audit write 증폭을 피하려면 adapter가 realtime-first여야 한다. | `accepted` | MOMO-338 #308에 realtime-first, bounded reconnect/recovery, idle tight polling 금지를 추가. |
| 2026-07-10 | MOMO-338 / #308 | 어댑터 bearer 단일화 리뷰에서 기존 `agent:` namespace가 user-visible status/partial과 Context Packet job을 함께 운반해 같은 채널 관찰자에게 private work가 노출되는 문제를 발견했다. Python 범위를 넘어 `agentwork:` namespace와 서버 proxy/config를 추가했다. | `agent:` 전체를 self-only로 만들면 기존 macOS working UX와 MOMO-212 live gate가 깨진다. 따라서 ADR-0101에 observable progress와 exact-actor private work 분리를 반영하고 dev/local-alpha/prod config를 함께 갱신했다. 다중 gateway 인스턴스의 provider 중복 실행은 durable claim/lease 없이는 닫히지 않아 후속 서버 티켓으로 분리한다. | `accepted` | MOMO-338 merge에 `agentwork:` self-only regression 포함. 후속: MOMO-341 claim/lease + takeover. |
| 2026-07-17 | MOMO-411/412 게이트 (선재 발견) | runtime-db 게이트의 `MomoWindowChromeSnapshotTests`(dark/narrow/overlay light) 4건이 origin/main HEAD에서 이미 FAIL — UX 트랙 window chrome 최근 머지분의 환경 의존 스냅샷 drift. 411/412(server·tooling만 변경)와 무관. | origin/main HEAD 격리 재현으로 선재 확정. 엔진 트랙 게이트가 이 때문에 무한 대기하지 않도록 서버 표면 단독 verifier로 머지 판정. | `accepted` | #448이 레퍼런스 갱신으로 1차 해소, MOMO-448(#450)이 stale light 캐노니컬 재기록 + ApprovalInbox close-crop + 신규 3서피스 스냅샷 11장으로 종결. overlay light는 offscreen NSWindow 폴백 렌더로 전환해 headless 재기록 가능해짐. |
| 2026-07-17 | MOMO-412 / PR #443 (리뷰 후속) | webhook native secret KDF의 master key가 `config.jwtHMAC` 재사용(M2) — JWT secret 회전 시 발급된 모든 native webhook secret이 조용히 무효화되는 운영 결합. per-install rate limit 부재(M1)로 토큰 유출 시 채널 flood 상한이 회전/revoke뿐. | 암호학적으론 도메인 분리(`momo.webhook.native.v1\n`)로 안전하나 운영 결합은 실재. infra/prod 무변경 계약상 v0 수용, 후속 분리 권고. | `accepted` | 2026-09-04 판정(성재 위임): 코드 확인 결과 **아직 유효** — `OUTBOUND_WEBHOOK_MASTER_KEY` 미설정 시 `JWT_HMAC` 폴백(`main.rs:205`)이 셀프호스트 기본값(`self_host_env.sh`가 생성 안 함), per-install rate limit 부재. **#2066** 발행(키 독립 생성·폴백 거부·rate 예산, ADR-0004 증보 필요). |
| 2026-07-21 | W-2 / PR #561 | 패킷은 "read-only v0, 쓰기=read-state만"으로 한정했으나 worker가 실전송 컴포저(sendMessage + draft-attempt당 clientMsgId idempotency)까지 구현. | 코드 품질이 L4 §3.1 idempotency 계약을 정확히 준수하고, 오케스트레이터가 실서버 실전송 왕복을 검증(PASS). 되돌리는 비용 > W-4 선행 수용 이득으로 판정. | `accepted` | W-4 발급 시 "컴포저 기구현" 반영해 범위 축소(read-state·승인 카드·recovery 왕복 중심). |
| 2026-07-15 | MOMO-390 / PR #403 (선재 발견 보고) | 이 PR의 이탈이 아니라 main 기저(e35be71)의 기존 결함 발견: `verify_staging_smoke.sh`/`verify_internal_hosting_smoke.sh`가 `agent/ch/dm/user` 4개 namespace만 기대해 `centrifugo.prod.json`의 `agentwork` namespace(MOMO-338 도입)와 불일치 — 두 스크립트가 main에서 이미 FAIL. | MOMO-338이 namespace를 추가할 때 두 smoke 스크립트의 기대 목록이 갱신되지 않은 gate drift. 제품 경계 무관, 스크립트 기대값 갱신만 필요한 소형 tooling 수정. | `accepted` | MOMO-399 발급(staging/internal smoke namespace 기대 갱신, ADR 불요) — 엔진/인프라 트랙 위임 범위 내 판정. |

| 2026-07-18 | MOMO-471 / PR #494 (게이트 선재 발견) | `testWorkspaceSearchLightSnapshot`가 격리 통과·full `make test` 실패(폰트캐시/GPU 순서 의존 렌더 비결정, precision 0.98 미세 초과). origin/main에서도 재현 — V-3 무관. | V-3 자체(huddle/Core 34 test) PASS 확인 후 표면 무관 flake로 판정, 재기록 커밋 revert해 PR을 huddle 전용 유지. MOMO-411/412 선례(선재 스냅샷 FAIL 분리)와 동일 처리. | `noted` | 2026-09-04 판정: 대상 스냅샷 시험은 macOS Swift 클라에 있었고 그 트리는 2026-08 W-S1(8b2db408)로 **은퇴** — 시험 자체가 소멸. #495 close. |

| 2026-07-18 | MOMO-474 / PR #499 (게이트 선재) | server 전용 PR인데 runtime-db 게이트의 `testChannelCreationSheetEnglishLargeTextSnapshot`(macOS)가 full-suite flake로 실패. server 변경은 macOS 렌더에 영향 불가 = 선재. | 첨부 verifier+서버 107 test+openapi+실 Google smoke PASS 확인 후 track/engine 머지. MOMO-472(#495) 범위를 이 스냅샷까지 확장. | `noted` | 2026-09-04 판정: 위와 동일 — macOS 표면 은퇴로 소멸. #495 close. |
| 2026-08-22 | T-4 #1654 / PR #1658 | AC 실측을 발행 앱 이미지 풀 스택 2벌이 아니라 canonical compose postgres 2벌+`schema_v0.sql` 시드로 수행. 복원 래퍼 `self_host_pg_restore.sh` 가산. | V-1(#1650)이 발행 이미지 부팅·표면을 이미 증명했고, dump/restore 바이트 경로는 공유 lib 단일화로 rehearsal 게이트와 동일. 실 마이그레이션 스키마에서의 dump→restore는 E2E 수용 런(D7 첫날 백업 경로)에서 자연 폐곡선. 래퍼 가산은 D9 취지 일치. | `noted`(검증 축소) / `accepted`(래퍼 가산) | E2E 런에서 실 스택 pg_dump 1회가 이 갭을 닫는지 확인(패킷 §V-1·§T-4 결속). |
| 2026-08-23 | T-1 #1651 / PR #1662 (게이트 선재) | `local_gate --profile runtime-db`가 111/168 `verify_prod_rls_posture.sh`(MOMO-554)에서 중단 — prod compose가 `PROVIDER_LINK_MASTER_KEY`·`MOMO_WORKHOST_*` env를 필수 요구. | **main에서 동일 커맨드 동일 실패 재현(선재 확정)** — T-1은 해당 서비스 env·prod compose 비접촉. 게이트 111 스텝 PASS + `verify_owner_claim.sh`가 ADR-0166 검증 계약 전문 실측 PASS(재사용 409·TTL 410·해시만·로그 비출현·FORCE RLS definer momo_app 전용). MOMO-474 선례(선재 실패 분리). | `noted` | prod env 세트가 있는 호스트에서 runtime-db 완주 1회(ITO/배포 세션 몫). |

| 2026-08-05 | #1042 / PR #1058 (SRV-B7) | 이탈 3건: ①패킷 파일 허용 목록 밖 `openapi_shape_check.py` 가산(`--sampled-elsewhere` — known-unsampled와 의미 반대라 재사용 거부, 겹침 가드 포함) ②패킷 「함정」 전제 오류(repo에 infra/rust 컴포즈 기존재 — 오케스트레이터의 main 체크아웃 실측 오류) ③「예상 빨강」 재해석(빨강은 Rust가 아닌 Swift 선존재 — #1040이 스펙을 Rust 와이어로 맞춘 예정된 귀결. 2차 패스만으론 미해소, 잠식 기제=「등재=응답 모양 권위 이전」 동시 구현 필요). | ①부채(줄어들 수만)/권위 이전(늘어날 수만) 의미 분리 논거 타당 — 뭉개면 게이트가 거짓말을 배움. red proof 2종+겹침 가드 실측 확인. ②재발 방지 교훈: 경로 실측은 origin/track/engine 대상(JOURNAL 기록). ③해석이 완료조건 ①③의 유일한 정합적 동시 성립 — merge-base 선존재 증명 확인. 전 게이트 2연속 green·cargo 713/0. | `accepted` | 오케스트레이터 mid-flight 승인 3건 전부 추인. 후속: 부분집합에 relay/agent-worker 합류(매니페스트 확장 지점)·pgdata 고정이름 볼륨 함정 런북 반영. |

| 2026-08-15 | #1409 / LIVE-1 (동결 bcc4882d) | ADR-0165 D1의 1순위(Selkies-GStreamer) 대신 `gstreamer-webrtcbin`을 템플릿 사양에 선언. 근거=성능이 아니라 D4: Selkies는 입력 datachannel이 제품이라 view-only가 "채널 부재"가 아니라 "설정"이 됨 — D4의 구조적 요구와 상충. 구조 논거이며 실측 아님. | 논거 타당 판정(D4 "입력 채널 자체 미개설"의 유일한 구조적 성립형). 되돌리기=template.spec.json 한 필드(서버 코드는 producer 이름 무결속). ADR-0165가 아직 Proposed라 초안 D1을 webrtcbin 선언으로 갱신 — 성재 Accept 시 그 형태로 확정. | `accepted` | ADR-0165 D1 갱신(2026-08-15). 실측 확정은 전용 호스트 실기동 시(runtime-unverified 라벨 해소와 동시). |
| 2026-08-15 | #1409 / LIVE-1 (동결 bcc4882d) | 브라우저→샌드박스 도달성(시그널링 WS·ICE)이 미실측·구조적 위험: cubesandbox `create_body`에 포트 노출 표현이 없고 ADR-0157 증보 1 실측은 VM이 닫힌 채(eBPF deny_out) 출고됨을 확인. 워커가 3형상(전용 호스트 리버스 프록시 / oort 운영 TURN=D3 증보 / 샌드박스별 공인 포트)을 기록만 하고 미선택. | 서버 축 계약은 형상 무관 완결 — 단 **LIVE-2가 실화면을 그리려면 이 결정이 선행**. 추측 금지 규율 준수 정당. 전용 호스트에서 30분급 도달성 스파이크(U1 선례)로 형상을 실측 후 택하는 것을 권고. | `accepted` | 성재 결재(2026-08-15): **스파이크 선행** — 전용 호스트 30분급 도달성 실측 후 3형상 중 실측 근거로 확정, ADR-0165 D3 증보로 기록. 스파이크 티켓 발급. LIVE-2는 로컬 모의 시그널링으로 UI 계약까지 병행. |
| 2026-08-15 | #1409 / LIVE-1 (동결 bcc4882d) | display에 controller 등급이 없어 `owner_only` 세션의 소유자가 **자기 화면을 볼 수 없음**(PTY는 소유자=controller 문으로 도달). owner 예외는 신규 권한 모델이라 발명 금지 규율대로 fail-closed 거부 구현+테스트 고정. | `owner_only`의 의미("소유자만 본다" vs "아무도 안 본다")는 제품 결정. fail-closed가 안전 방향이라 v0 수용 타당. | `accepted` | 성재 결재(2026-08-15): **LIVE-3와 묶기** — fail-closed v0 확정, owner 예외는 control 개방(ADR-0004 증보 3 Accepted)과 같은 권한 표면에서 함께 결정. |

| 2026-08-15 | #1412 / LIVE-2 (동결 3d96221a) | 이탈 5건: D1 60초 재검증을 토큰 재발급이 아닌 원장 재검증으로(재발급 루프는 관전자당 분당 realtime 프레임 발행 — observerCountLabel이 거절해 둔 거래·producer가 stream:true 재검증 담당) · D2 카피 web-local(폰 카운터파트 부재·PTY 선례 동형) · D3 폰 파일 6개 수정(required 필드의 typecheck 파급 — 가드 정규식은 강화 방향) · D4 협상 타임아웃 30초(미실측 명시)+ICE candidate 버퍼 추가 · D5 owner_only 자기 문장(터미널 문장 재사용은 반쪽 진실). | D1은 패킷 문구("PTY 동형 재발급 루프")가 부정확했던 것 — PTY에 재발급 루프 없음, 워커 교정이 맞다. D3는 타입 파급 불가피+배제 가드 강화라 "폰 범위 밖" 취지 유지. D4 타임아웃 상수는 #1411 스파이크가 실측 대체. 전 게이트 그린(web 984·core 1425·폰 1160·design 프리플라이트 13/13). | `accepted` | 기획 레이어 수용 5건. 후속: #1411이 D4 타임아웃 실측 대체 · 선재 red(co-open-900 236px)는 #1413 발급. |

| 2026-08-16 | #1424 / LIVE-3 (동결 6cf952dc) | 이탈 3건: ①패킷 §1-2 "capability 만료 시 창 닫힘"을 60초 TTL 문자 해석하면 D3 위반(stream:true 재검증이 만료 절을 완화 — 살아있는 스트림은 만료 행 위에서 돎, 로그인 도중 60초에 에이전트 재개) → 창 자체 lease 90초(재검증 3회)+controller 재검증 갱신으로 대체 ②run 층 파킹 미구현 — RunStatus::Paused 배선 기존재·writer 부재, 게이트(거부)는 구현 ③라우트 밖 세션 종료는 lease ≤90초 backstop(momo-t3 크레이트가 outbox 불가 — 침묵 종료 회피). | ①패킷 문구 부정확이 원인(LIVE-2 D1과 동형 패턴 — 기제 세부를 검증 없이 패킷에 박지 말 것, 교훈). 워커 교정이 D3 보존의 유일 정합. ②D3(비관측)는 게이트로 성립 — 에이전트의 세션 유일 경로(work-controls)가 차단·시도 무흔적. 단 D6 "토큰 0"은 파킹 없이는 불완전 → **LIVE-4 발사 전 선행 조건**으로 #1425 발급. ③90초 유계·정직 명시 — 수용. | `accepted` | ①③ 수용. ②조건부 수용 — **#1425(run 파킹 writer)가 LIVE-4 선행**. D5(egress grant)는 패킷 범위 밖 명시 — egress 파도(P1~P7)에서. |

| 2026-08-16 | #1425 / PR #1427 (랜딩 8ce5ad38) | 이탈 3건: ①live3_2 주어 분리 — 파킹 run의 거절 문장이 세션 게이트와 분화(`agent run is not eligible for work control`) ②in-process 워커 턴은 파킹이 중간에 못 끊음(claim된 턴은 완주 커밋 — 정지는 다음 턴부터) ③queued run 비파킹(이슈 계약 문자 준수 — 소비 없음·복원 칸 부재·LIVE-3 게이트 2종이 세션을 지킴). | ①에이전트-facing 문장이라 LIVE-4 UX 비접촉(우로보로스 인터뷰에서 확인) — 기록만 ②정직 라벨 `runtime-unverified(in-process worker turn)` 명기, 워커 커밋 경로 가드는 승인 hold 상호작용 재설계라 별건 ③근거 타당(D6 주어=소진). | `accepted` | 3건 전부 수용. grok freeze가 별건 H1(교차-세션 run 잠금)을 적발 — 수리·재-freeze 무결 후 랜딩(잠금 순서 계약 session/host→window→run 성문화). |

| 2026-08-16 | #1428 / LIVE-4 (동결 abf53209) | 이탈 2+경계 1: ①카드 터미널 4종(`stopped` 추가 — 중단은 창이 존재한 적 없는 종결이라 end_reason 3종에 욱여넣으면 거짓 진술, 기존 어휘 "중단됨" 재사용) ②에이전트 방출 도구 신설(패킷 좌표에 없던 emitter — 스키마 무변경·`ApprovalReason::HumanIsTheAction`은 면제 분기 밖이라 사람 건너뛰기 불가, 오케스트레이터 코드 확인) ③세션 표면 control 내구 투영 미구현(3투영 드리프트 가드 vs bare RETURNING 충돌 — 리로드 후 세션 상세는 무표시가 정직, 카드는 props로 내구). | ①정확성 우선 판정 — 상태 수 최소화보다 거짓 진술 회피가 상위 규율 ②최소·구조적 안전 확인 ③"어느 읽기가 control 상태의 SoT인가"는 세션 표면이 control 표면이 되는 LIVE-5의 설계 문제 — 지금 풀면 두 번 품. | `accepted` | ①② 수용. ③ **LIVE-5 예약 스코프에 편입**(`research/2026-08-16-live4-interview-and-plan.md` §3에 항목 추가). |

| 2026-08-17 | #1454 / PR #1460 (UXC-A 프로듀서) | 이탈 5건: D1 트리거=「모델이 ` ```oort:report ` 펜스로 선언」 설계 상신(패킷 §9 미결정 항목 — 부수효과: 전 에이전트 턴 시스템 맥락 +~6줄, config 플래그는 후속) · D2 승인 기계·툴 카탈로그 비접촉(대가: 리포트 저작 중 스트리밍이 절단선에서 정지) · D3 `cargo fmt --check` 기준 커밋에서 선재 red 11건(미접촉 6파일 — rustfmt 버전 스큐, #1442 계열) · D4 `hosted_identities_…` PG 테스트 같은 DB 재실행 시 선재 실패(고정 토큰 해시 유니크 충돌 — diff 무관) · D5 웹 카드 렌더 육안 미확인 `runtime-unverified` 정직 라벨(conformance로 갈음 — 패킷 §6 허용). | D1: 서버 대리 지표 분류의 오탐 방향 분석이 타당, 「서버는 검증·전달만」 원칙과 정합 — 수용, config 플래그 후속 티켓. D2 수용(카드가 서며 해소). D3·D4는 선재 결함 분리 규율(MOMO-471 선례) — 별건. D5 수용. **Fable 검수 M-1 적발**: 펜스 맨-끝 미강제 → 닫는 펜스 뒤 산문 삭제+외곽 코드블록 안 예시 펜스의 카드 승격 — 수리 회전 1 완료(`7ac5dd47`, red 3+뮤테이션 증명). **grok freeze(C0/H1/M2/L1) 재판정 전부 실결함**: H(본문·summary 없는 유효 리포트가 원시 펜스 JSON을 채널에 커밋) · M(안 닫힌 외곽 펜스 안 예시의 카드 승격 — 회전 1 잔여 구멍) · M(ZWSP/BOM 태그 검출 우회) · L(elapsed의 DB·프로세스 시계 혼합) — 회전 2 완료(`4516a243`: 폴백 산문→summary→title·body 없는 카드 커밋 nullable·Refused 비대칭 의도 유지 — 재-freeze 동의). 재-freeze가 가족 변형 2건 추가(물결 펜스·틱 앞 제로폭) → 회전 3(`2592244d`: FenceMark 2문법 상태 기계 — CommonMark 완결, 뮤테이션 증명 누적 8) → **track/engine@30186176 랜딩**(PR #1460·이슈 close). L-1(시계)은 같은 SELECT의 now() 투영으로 수리(뮤테이션 증명 불가 — 정직 라벨). | `accepted` | 회전 후 랜딩. 후속 티켓 후보: ①REPORT_PROTOCOL config 플래그 ②D4 선재 PG 테스트 재실행 내성 ③goal_claim.sh가 track/engine이 아닌 origin/main에서 분기(워커 실측 — 다음 엔진 워커 동일 함정). |
| 2026-08-17 | #1442 / PR #1459 (toolchain 정합) | 이탈 5건: D1 MSRV 정직 상향(1.80→1.88)이 `clippy::manual_is_multiple_of` 1건 점화(`korean.rs:116`) — 소스 비접촉 계약대로 정지 · D2 스코프 확장: desktop 워크스페이스 `rust-version` 1.77.2→1.89.0(실측 바닥 — 패킷 미지목 발견) · D3 트랙 경계: 엔진 PR이 UXUI 소유 `clients/desktop` 1값 수정 · D4 게이트 선존 조건으로 `momo-tracks/engine`을 clean 확인 후 `--ff-only`만 수행 · D5 미수정 보고 4건(MSRV CI 게이트 부재·부동 `rust:1-slim-bookworm`·release-desktop 하한 미검사·`fmt --check` 문서 결함=#1377 소관). 핵심 실측: 레포에 rust-toolchain 파일 0개 — 커서의 "고정 1.83"은 커서 환경 기본값, 실결함=**양쪽 워크스페이스의 거짓 `rust-version` 선언**(브래킷 실행으로 바닥 확정: server=1.88.0/desktop=1.89.0). rust-toolchain.toml 비신설 판정 3근거. | D1: momo-main 계약 해제 — #1460이 `korean.rs` 비접촉 실측(충돌 0), lint 실수리가 정직 방향 → 수리 지시(2026-08-17). D2·D3: 전수 조사의 자연 귀결, 최소 diff — 수용(UXUI 트랙엔 이 로그가 통보). D4 수용(새 커밋 0·push 0). D5는 발견 보고 규율 준수 — 별건 큐. ⚠ CI rust 레인이 clippy를 안 돎(pr-ci.yml) — CI 초록≠머지 신호, 로컬 게이트 증거 필수(기존 규율 재확인). **결재 집행(2026-08-17 전권 위임)**: lint 6건 실측 확장→보안 술어 4곳 성재 큐→패치 승인·git apply 바이트 정합(`afda586a`)·clippy 그린·보안 술어 런타임 실증(refresh 회전·http_smoke 6/6)·STATUS 정정·정책 감사 마커 → **track/engine@907e076c 랜딩**(PR #1459·이슈 close). | `accepted` | 랜딩 완료(2026-08-17). 후속 티켓 후보: ①CI rust 레인 clippy 편입 ②MSRV 브래킷 게이트 ③부동 베이스 이미지 고정 여부(성재 운영 결정 큐). |

| 2026-08-17 | #1441 / PR #1462 (UXC-C 세션 칩) | 이탈 6건: D1 `goal_claim.sh`가 origin/main에서 분기(재지정으로 해소 — #1454 워커와 동일 함정, 스크립트 결함) · D2 검증 칩을 목록 행에는 안 세움(행은 스레드를 안 읽음 — 세션마다 /replies 추가 읽기=read-model 결정이라 티켓 분리 제안) · D3 세션 스레드 밖 리포트는 세션 것이라 주장 안 함(`runtime-unverified(producer→session thread placement)` — #1454 프로듀서가 채널 본문에 저작하면 칩 영구 부재) · D4 패킷의 pnpm→실레포 npm 교정 · D5 STATUS.md 미갱신(병렬 충돌 회피) · D6 캡처 레인 추가(#1440 선례). | D1: 후속 티켓(스크립트 base 수정). D2: read-model 티켓으로 수용 — grok H2(5×200 창 절단으로 초장기 세션 칩 영구 부재)도 같은 티켓에 묶음. D3: 랜딩 후 실기동 실측 항목. D4~D6 수용. **리뷰 폐곡선 적발**: design-review High 1(320px 상세 머리 제목 붕괴 — 이 PR이 세운 shrink-0 2항목 소산) + grok 실결함 2(G-H1 ended 프레임이 events 캐시 미무효화·주석-코드 불일치 / G-M1 코어 상한 절단 표를 접어 「통과 40」 거짓 — omitted 무시) → 3건 묶음 회전 지시(2026-08-17). design-review Medium(통과 세션 초록 칩 2개 공존)은 워커 판단 지지 — `SESSION_STATUS_CLASS.done`의 ok 유지 여부는 코어 역할표 결정이라 성재 큐. 회전 3커밋 완료(320px 수리 실측 127→202px·폭 회귀 숫자 가드·G-H1/G-M1 red proof) → design-review 재확인 PASS(B0/H0)·재-freeze C0/H0/M0/L0 → **track/engine@b9cfbf69 랜딩**(PR #1462·이슈 close). | `accepted` | 랜딩 완료(2026-08-17). 후속 티켓 후보: ①세션 검증 read-model(목록 행 칩+장스레드 최신 리포트 — D2+grok H2) ②goal_claim.sh 트랙 base ③종료 상태 칩 muted 전환 여부(성재/코어 표) ④경과 명칭 3종(「동안 작업」·「실행 시간」·「작업 시간」) 정렬+「1초 미만」 카피. |

| 2026-08-17 | 위생 파도 2 4건 / PR #1471·#1473·#1475·#1477 | **#1467**(PR #1471): 이탈 2 — pairing_challenge_hash 동계열 파생 확장·fmt 드리프트 6파일 관측. **#1464**(PR #1473): 이탈 5 — 라벨 소스 실동 불가(레포에 track:* 0개)로 선반영만·TRACKS.md 파싱은 힌트로 강등(웹/폰 goal이 engine에 실리는 실측과 충돌)·제목 태그/Base:줄/track 체크아웃 3소스 신설·local_gate 배선+TRACKS.md §1.1 신설·track/uxui diverged 관측. **#1465**(PR #1475): 이탈 4 — 이슈 증상 실측 정정(부재=0px·공백=46px)·폰 문구 미이식·**코어 잠복 크래시 발견-에스컬레이션**(artifacts.ts null body TypeError→타임라인 백지화)·캡처 증설. **#1443**(PR #1477): 이탈 6 — 코어 주석만 접촉·계측 표면 증설·@ 낱말 절단 사진 기록·세로 접기 대안 기각 기록·(회전)크롬 0.23 분리. | 전건 수용. 폐곡선 적발: #1475=3렌즈 전원 그린(Fable C0/H0/M0/L0·design-review PASS B0/H0 — 46px 죽은 띠 제거·부재 케이스 바이트 동일 검증·grok C0/H0/M0/L0), 잔여 Low=폰 공백 패리티→#1478. #1477=design-review PASS+M2(→#1479 묶음)·grok M1 실결함(창 몫이 크롬 미공제 — 측정 기기에서 불변식 거짓·@시트 결합 시 타임라인 0) → 회전(크롬 명명 상수 0.23·불변식 산수 복원·AX 상한 271→170.4=목록 등분·캡처 레인 크롬 상시 재실측·주석 과잉 주장 철회). #1442 계약 해제 1건(clippy 1줄→6건 실측 확장→보안 술어 4곳 성재 큐 이관)은 별도 항목 참조. | `accepted` | 4건 전부 랜딩(`9ba981e2`·`13eb8b52`·`0d6cc97f`·#1477). 후속 발급: #1472(fmt 드리프트+게이트 단계)·#1476(코어 null crash)·#1478(폰 공백 패리티)·#1479(@ 결합+절 예산)·#1480(멘션 시트 상한). 정렬: uxui sync #1474(`a8ab97ec`). |

| 2026-08-17 | 위생 파도 3 4건 / PR #1484·#1485·#1486·#1487 | **#1466**(PR #1484): 이탈 3 — 운영자 문서 2줄 범위 제약(momo-main 집행)·PG/게이트 부하 유보(momo-main 실행 91스텝+PG 12/12)·파서 유지 판단. **#1478**(PR #1485): 이탈 1 — 폰 1→3자리 통합(본문·안내·복사, 반증 확인). **#1403**(PR #1486): 이탈 5+회전 — grok C0/H3/M1 재판정에서 **H3 반증**(ConfirmButton은 aria-disabled 렌더 — 오탐), H1 확정(진행/잠금 문법 겹침 — 자기 주석이 인용한 actionBusy 규율과 모순) → 회전 97c2dc73(locked=disabled&&!saving 5사이트·CLEANUP_BUSY_NOTE 사유·카운트→구조 단정). **#1468**(PR #1487): 이탈 5 — 패킷 좌표 오류 교정(카드 라벨은 AgentCard 하드코딩)·폰 리터럴 이월·라벨 갈림 코어 kind 이관·캡처 확장. | 전건 수용. 3렌즈 수렴: #1485·#1487=발견 0 완전 그린, #1486=오탐 1 반증+실결함 3 회전 폐쇄(재확인 PASS B0/H0), #1484=C0/H0 청정. 렌즈 불일치(#1486 design-review PASS vs grok H3)는 코드 실측이 판정 — grok 오탐 원인=컨텍스트 사본에 SettingsFields 미포함(스테이징 교훈: 인용 선례 파일도 동봉). | `accepted` | 4건 랜딩(`f09f9d00`·`433ab9cb`·`1be4b087`·`b46da0ee`). 후속 발급: #1488(죽은 카피)·#1489(폰 라벨 결속)·#1490(ConfirmButton busyLabel)·#1491(done 칩 muted — 위임 결정 확정). |

| 2026-08-17 | 위생 파도 4 5건 / PR #1495~#1497·#1499·#1500 | **#1489**: 이탈 3(좌표 행 드리프트 관측·주석 정직 수정·락파일 드리프트 관측). **#1488**: 이탈 3 — 결정=도달 불가 갈래는 살아나면 거짓을 말했을 문장(제거·렌더 글자 단위 무변경)·코어 상수 신설·tooLong 낱말 기록만. **#1476**: 이탈 5 — 지목 1구멍→실제 3구멍 완결 수리(red proof 7-failed)·동형 잠복 2 발견-보고(quote.ts→#1498·web-legacy 죽은 코드)·>200KB 공백 diff 비대칭 정직 선언. **#1490**: 이탈 5 — 능력만 랜딩(배선→#1501)·낱말꼴 전수 조사(명사+중 규칙+위반 3 — 정본화 채택=위임 결정)·같은 클래스 5곳 발견(→#1502). **#1491**: 이탈 6 — 폰 로컬 초록 제거·running/unavailable 선재 어긋남 보고(→#1503)·verify_merge_tree 8레인 대체. | 전건 수용. 렌즈: #1495·#1496·#1489계=Fable 직접(시각 델타 0 비례성)·#1497=grok C0/H0/M0/L1(L=기선언 비대칭 — 수용)·#1500=design-review PASS(B0/H0 — 실패 세션 danger 유일 고채도 실검)·#1499=Fable 직접(Playwright 실측 동반). | `accepted` | 5건 랜딩(최종 engine=`cac7f16a`). 후속 발급: #1498·#1501·#1502·#1503. |

| 2026-08-17~18 | 위생 파도 5 5건 / PR #1508·#1513·#1514·#1509·#1512 | **#1498**: 이탈 3 — hasRenderableBody를 판정이 아닌 단정으로(''·공백=빈 ready 유지, 실측 반증 기반 4태 분리)·웹 가드 신설·web-legacy=살아있는 코드 정정. **#1503**: 이탈 7 — WorkStatusBadge 2줄 접촉(이슈 명시 범위 — 패킷 비접촉 지시가 과광의)·행 모양 변경(AX 붕괴 해소>밀도 −27pt, 위임 판정)·자체 fresh design-review 폐곡선(B0). **#1463**: 이탈 10+회전 — 원천을 채널 히스토리 DESC 스캔으로(설계 전환·스키마 무변경)·design-review 3판(B1+H2 폐쇄)·grok C0/H1/M1(오류 무구분 폴링 정지→분류+data 폐기, 이중 원천→캐시 구독 수렴 — red proof 3)·잔여 High 2=선재 remit 밖. **#1501**: 이탈 8 — 조사 부정확 2건 데이터 교정(「확인하는 중」은 화면에 없음·잔여 위반 8자리 발견)·같은 표면 3건 동반 정렬. **#1472**: 이탈 6 — 6→9파일(fmt 없는 손패치가 새 드리프트)·desktop 동반·#1377 뿌리(--all 부재=virtual workspace 무검사) 실측 해소·게이트 fmt 단계=record-not-pin. | 전건 수용. 특기: momo-main 검수가 층간 이음새 1건 추가 발견(#1498 — body 없는 카드 인용=거짓 묘비, 클라 단독 수리 불가)→#1510 계약 확장 티켓. **랜딩 지연 사건**: #1512 정책 발행이 GitHub 장애(major)로 3회 503 — 평가는 전 회차 PASS, 절차 결함 1건 병발(head 전진 시 마커만 갱신·라벨 재부착 누락 — 게이트의 「코멘트 이후 전이」 요건 위반). 교훈 성문화: **head 전진 시 마커 갱신과 라벨 재부착은 짝**. | `accepted` | 5건 랜딩(최종 engine=`aaaf2f34`). 후속 발급: #1510(인용 계약)·#1511(낱말 잔여 8+게이트)·#1515(칩 그릇 토큰)·#1516(soft 팔레트). |

| 2026-08-18 | 파도 6 5건 / PR #1544·#1537·#1532·#1533·#1540 | **#1524 LIVE-5a**(이탈 9+회전): 패킷이 claim 시점 레포 부재(발사 순서 결함 — 플러시 랜딩 전 발사)·base=main 사고(#1464가 engine에만)·SSH 차단(릴레이=#1545 성재 손)·validate 필드명 400 실경계 발견-수리·producer 자격 수명을 재검증 재발급으로(만료 절벽 논거)·grok 보안 freeze C0/H0/M0/L2→회전(TTL 24h clamp·remint 정직화+5c AC 편입)·web-legacy 타입 재생성(momo-main 기계 수리). **#1525**(이탈 5): 위반 17 정리(W-S1 잔여 14)·D2 환경상속 결함 동반 수리·게이트 밖 42건 보고. **#1526**(이탈 5): 실측 헤드라인=스택 80초·첫 에이전트 응답 도달 불가(F1)→T1~T8, #1534~#1536 발급. **#1527**(이탈 6): 규율 정본 신설·ADR-0113 D6 정정. **#1502**(이탈 8): 6사이트+노드 교체 초점 결함 수리. | 전건 수용. 파도 구조 교훈 2건: ①발사 전 패킷 랜딩 확인을 발사 체크리스트에(이번엔 복구 재랜딩 #1529로 해소) ②**main의 goal_claim에 #1464 부재 — 뿌리는 engine→main 코드 승격 81+커밋 미실행**(트랙 파이프라인의 main 동기화가 docs만 흘러온 상태, 위임 범위 내 승격 검토 필요). 렌즈: #1544=grok 보안(경계 전부 닫힘 확인)·#1540=design-review PASS+독립 실측·#1537=정책 마커·#1532/#1533=Fable 직접. | `accepted` | 5건 랜딩(최종 engine=`c9a390d9`). 후속 발급: #1534~#1536(온보딩 T)·#1541(in-flight 잔여)·#1542·#1543(사유·착지)·#1545(릴레이=성재 손). 다음: **LIVE-5b 편성**. |

| 2026-08-18 | 파도 7 4건 / PR #1556·#1553·#1557·#1560 | **#1534 T1**(이탈 5): 키가 둘 발견(PLATFORM_ADMIN_EMAILS만으론 403→503 — PROVIDER_LINK_MASTER_KEY 동반 배선이 수용기준 성립 필수)·기존 env 복구 경로·M5 최초 측정(3:58=재시도 소진 포함·NOTICE — ANSWERED는 T2 결정 후)·ADR 불요 판정(전부 선택형·기본 닫힘). **#1510**(이탈 0+회전): propsKind 최소 신호 — grok M1(서버 투영이 클라 술어 불일치)→회전(SQL 동형화·PG 4태·interaction.rs 복제 아님 실확인+낡은 주석 정정). **#1541묶음**(이탈 8): 6곳 분리+사유+착지 — 범위 밖 발견 2(SaveButton 정본 자체 결함→#1558·잔여 반→#1559). **#1549 LIVE-5b**(이탈 5+회전 2): 카드 라벨 유지 판정(owner 부재 — 라벨 변경=팀원 클릭 거짓)·inputChannel 선언 DECLARED ONLY·fresh reconnect 선택 — grok(grant/unmount 경쟁: AbortController 기각 논거로 (ii) 채택·pointerFrame action별 분리)+design-review FAIL(B1 Esc 도달 불가·H2)→회전(Esc=해제+포커스 인계·break-keep 8·focus=상태)→재확인 PASS(B0/H0). 게이트가 추가 실결함 2(ontrack 조기 스트림 유실·컨트롤이 포인터 밑 이동)+시간압축 결함 1 자가 수리. **목업/출하 위계차**(N3): 번들 밖 픽스처는 제품 타이포/간격 스케일을 못 실음 — 오버레이 승인 시 실물 구현으로 해소(워커 이탈 기록의 원장 반영이 본 행). | 전건 수용. 파도 7 특기: 성재 CI 소음 발제→실측(15실패 중 코드 1)→인터뷰 3확정(A-1 플러시 track 랜딩·C-1 Xcode 트리거·D-2 프리플라이트)→§3.1.1 성문화(#1562 — **새 규칙 첫 적용 자체가 main 비접촉**). | `accepted` | 4건 랜딩(최종 engine=`bc819ecc`+`9c2e16eb`). 후속: #1558·#1559·#1563(stuck key)·#1511 편입 2건. **LIVE-5 전 축 랜딩 — 잔여=5c(실기동 E2E)+릴레이 #1545(성재 손)**. |

| 2026-08-18 | 파도 8 4건 / PR #1567·#1569·#1568·#1570 | **#1535**(PR #1567, 이탈 3): T3 후반부(에이전트 절벽 절) 미작성 — #1534가 랜딩시킨 §5가 이미 그 절(중복 회피)·같은 문단 실측 정정 2건 편입(포트 하드코딩·`#general`→`general`)·bench_onboarding.sh:557 주석이 삭제된 문장 인용 잔존. **#1563**(PR #1569, 이탈 3): 4번째 판정 `ignore` 신설(press 없는 keyup)·메모리를 held-key 맵이 아닌 Escape 전용 1슬롯으로(프라이버시 불변식 논거를 코드에)·범위 밖 게이트 가드(포트 점유 시 남의 빌드를 조용히 측정하던 함정 — 실측 40분 무효가 강제). **#1536**(PR #1568, 이탈 4): 빈 상태 액션 둘(§4 「액션 하나」 이탈 — 초대의 유일 진입점 보존, 동급을 지우고 위계를 게이트가 잼)·DM 버튼 윤곽→채움(같은 act=같은 옷)·파일군 momo-core 두 걸음(카피 단일 소스 — 패킷 요구)·폰 버튼 미부여(EmptyState 아톰에 액션 자리 없음). **#1570**(LIVE-5c, 이탈 5+회전 1): microVM 내부 왕복 미측(권한 경계 — reverse tunnel/서버 기동 양쪽 거부, `unverified.inputDeliveryInMicroVM` 정직 라벨)·웹 test 2파일 접촉(라벨 크로스리드)·producer 실결함 3건 수리(goal 자체 산출물)·remint를 로컬 coturn 실측(정적 경로는 만료 부재로 측정 불가)·정렬 FAIL은 main 체크아웃의 낡은 로컬 track/engine. | 전건 수용. 렌즈: #1567=Fable 직접(docs·실기동 재실측 근거)·#1569=Fable+grok freeze **PASS**(C0/H0 — M1 검출 창은 #1571 편입, L2 빈 슬롯 repeat 승격은 down/up 짝 성립으로 noted)·#1568=design-review **PASS**(B0/H0 — M1 §4 문면은 본 플러시가 정본 반영, M2 라벨 이중 의미→#1573)·#1570=grok freeze **FAIL→회전 1**(C1 확정: non-text/비JSON continue가 재검증 deadline을 굶겨 반환된 controller의 XTEST 주입이 산 채로 남음 — 루프 선두 이동+ping pong 소비+`--jam --watch-revoke` red proof 75s 굶주림 재현→1주기 회수. H1=e2e가 반환 축을 실측하도록 강화·M1=ready 선송신 강등→prepare/begin 분리·M2=modifiersAreAdvisory 계약 명시) 후 랜딩. 오케스트레이터 기계 수리 1: momo-core per-package lockfile(워커 npm install 부산물, workspaces 루트가 정본) 제거 커밋. remint 택일 **(b) 채택**(천장 반증 실측 — 전권 위임 집행, 반영=#1574). | `accepted` | 4건 랜딩(최종 engine=`d987ff58`) — **★LIVE-5 전 축 종결**. #1561 close(alignment fail 0 실측). 후속 발급: #1571(게이트 포트-스쿼트 가드 일반화+검출 창)·#1572(base 위생 마이크로 2 — openapi YAML 1.1·bench 주석)·#1573(「멤버 초대하기」 이중 의미)·#1574(remint 반영). 성재 손 신규: microVM 내부 왕복용 momo-server 배치. |

| 2026-08-19 | 파도 9 4건 / PR #1580·#1581·#1582·#1583 | **#1572**(PR #1580, 이탈 0): openapi enum 인용·bench 주석 현행화 — red/green proof+산출 JSON byte-identical. 발견 1(display_input_e2e.py 실행 비트 — #1570 선재). **#1573**(PR #1581, 이탈 1): 택일 상신=채널측 「멤버 추가하기」 개명(문↔방 동사 일치·「초대」=워크스페이스 예약·Slack 관례)·capture-screens 셀렉터 1줄 파일군 밖. **#1574**(PR #1582, 이탈 2): chmod +x 선해소(#1572 발견분과 합류)·파일군 밖 3건 주석 접촉(잔존 0 목적). **#1571**(PR #1583, 이탈 5): 검출 신호 선택(stdio 파싱+ready-line 소유 증명 — lsof PID 대신, 플랫폼 의존 회피)·이중 GATE FAIL 접두 수리 2줄·boot-budget stdio inherit→piped·계약 테스트 신설·고아 wait 헬퍼 2 제거. | 전건 수용. 렌즈: #1580·#1582=Fable 직접(마이크로·서술 정정) · #1581=design-review **PASS**(B0/H0 — 택일 방향 지지 「반대가 나았을 근거: 없음」, Medium 1+Low 1+Nitpick→#1584) · #1583=grok freeze **PASS**(C0/H0/M0 — 소유 증명을 Vite CLI 실동작 대조로 검증, L2건=실패 갈래 stop() 비대칭·exit 스냅샷 진단은 fail-closed 유지라 noted). #1573 택일 (채널측 개명) 수용 — 전권 위임 집행. | `accepted` | 4건 랜딩(최종 engine=`fe3f2960`). 후속 발급: #1584(「초대」 어휘 정렬 마이크로 3). 같은 날 선행: **engine→main 승격 #1576(107커밋, main=e322ccf3)**+sync #1577/#1578 — topology 복원·PR-only 실측. |

| 2026-08-19 | 집행 창(성재 SSH 위임) / PR #1586·#1588 | **#1586**(momo-main 직접 커밋, 이탈 1): compose TURN 패스스루 5줄+예시 — 기계 수리 클래스 판정(배포 집행 선결·실시간 위임 창·워커 왕복 비용>이득). `${VAR:-}` 채택 논거(반쪽=미설정=정적 경로 유지 — 런북 롤백 성질)를 파일 주석에 성문화. **#1588**(#1587 microVM 실측, 이탈 2+회전 1): 검증자 측 momo-viewer 이미지(gstreamer1.0-libav — openh264dec mid-stream 합류 정지)·nginx /v1/ hairpin — 전부 teardown에서 복원. producer READY 경쟁 결함 발견-수리(set_state 비동기 vs create-data-channel — microVM에서 매번 재현·#1570은 운으로 통과, spec v4→v5). | 전건 수용. 렌즈: #1586=Fable 직접+렌더 검증(unset/set 케이스) · #1588=grok freeze **PASS**(C0/H0 — READY 규율·검증 순환 없음·라벨 XOR 게이트·C1 보존 확인)+회전 1(spec 주석 PLI 위치 정합·e2e 헤더 자기모순 해소·--remote-proof에 --ice-servers-json 필수 가드·--key-out fchmod). **비-repo 집행 기록(원장=#1545 코멘트)**: §6-2~6-4 전 걸음(병행 불성립 실측·정지창 은퇴·restart 필요 교훈)+프로덕션 배포(68fc52ff→8d0f7d9a·마이그 077·검증 5종)+로컬 죽은 자격 정리. C-1은 성재가 한도 도달로 워크플로 비활성화(더 강한 조치 — 수용, 9/7 재활성화 시 경로 제한 선적용 체크리스트化). | `accepted` | 랜딩(engine=`ef134609`) — **LIVE-5 정직 라벨 전부 해소**(`unverified.inputDeliveryInMicroVM`→runtimeVerified)·프로덕션=LIVE-5 전 축. 성재 손 잔여: #1361(대기 합의)·Xcode 9/7 체크리스트·배포 후 관찰. |

| 2026-08-19 | 파도 10 8건 / PR #1591·#1593·#1594·#1595·#1596·#1597·#1598·#1599 | **A조** #1511(#1593, 이탈 2+회전 1): 말줄임 정책을 일반 규칙으로 선언했으나 AST 실측이 반대(맨몸 155 vs 말줄임 16) — 주장을 PrimaryButton 지역 관례로 축소·**폰 캡처표의 mobile-login 행이 웹 ConnectPage를 390pt로 찍은 레인**(「web」 배지 확인)이라 삭제+폰 관찰 단정 신설·폰 스캐너 신설(규칙 객체를 자식 프로세스로 — jest .mjs import 불가 실측)·정본 §5.1/§5.2 승인·§5.3 사정거리 구절. #1584(#1597, 이탈 2): 잔량 가드 신설(줄번호 아닌 ≤)·폰 캡처 대신 전수 스윕. **C조** #1558(#1591, 이탈 1): 소비처 실측 3자리(제목 「5+」는 과대)·수정 0건의 무결을 기계 스캔으로 고정. #1559(#1595, 이탈 2+회전 1): design-review High 4 — 별칭(submitBlocked) 뒤에 숨은 접힘이 **PR이 가장 많이 고쳐 쓴 파일에** 생존·빈 상태 무사유 회색 CTA·금지된 prop으로 포커스 `<body>` 이탈·**지킬 수 없는 약속을 aria-describedby로 승격**. **B조** #1515(#1596, 이탈 2+회전 2): 회전 1이 허용목록을 다른 허용목록(CHIP_CLASS import)으로 바꿔 H-1 재개 → 회전 2가 **탐색을 기하 시그니처로 키잉**(전제를 참으로 만드는 대신 불필요하게)+HAND_ROLLED 천장 7. #1516(#1598, 이탈 1+회전 1): D1(라이트 danger-soft 1.046 유지)=「불가능」이 아니라 「거부한 거래」로 재서술. **D조** #1479(#1594, 이탈 0): U+2060 소스 리터럴(게이트가 원문 정규식으로 읽어 이스케이프는 자와 화면을 가름). #1480(#1599, 이탈 2+회전 2): 「넷」의 근거가 인접 파일과 모순(matchMembers는 정렬 안 함)·핸들이 AX 배수에서 소실(flex:1=basis 0이라 압력 전부터 폭 0). | 전건 수용. **폐곡선이 실결함 11건 적발**(design-review 4판 + 재확인 3판): 회전을 부른 High가 대부분 **코드가 아니라 주장**이었다 — 말줄임 정책·「넷」의 근거·「모든 칩을 훑었다」·「폰엔 이 칩이 없다」·「그대로 보내집니다」. 리뷰어가 그물을 **직접 재구현해 변종을 먹인** 것이 별칭 스캔 3구멍·CHIP_CLASS 사각지대를 냈다. 특기 3: ①#1596 회전 2의 새 가드가 base 머지 직후 **빨갛게 도착**(잔량 27·미분류 3) — 허용목록이었다면 조용히 초록 ②워커가 리뷰어 지시를 **근거 있게 거부**(#1599 표지 flexShrink) 후 회전 2에서 **자기 근거의 재현 불가를 스스로 발견**해 그 줄을 판정이 아닌 못으로 재서술 ③#1593 워커의 파이프 종료코드 은폐 자기 신고가 다음 워커의 실제 실패(EXIT=1인데 마지막 줄 PASS)를 잡음 → TRACKS §3.1.1 성문화. | `accepted` | 8건 전부 랜딩(최종 engine=`28bf1a54`). 후속 발급: #1600(폰 톤 표면 발산)·#1601(멘션 순위 부재 — 정확 일치가 상한 6에서 잘림, 코어 티켓)·#1602(그물 3종 사각지대)·#1603(재고 묶음)·#1604(폰 진행/잠금 방언). |
| 2026-09-04 | UX-R1a #1996 / PR #2043 | 브리프 계약 항목 「`select.tsx`에 `POPOVER_MOTION` 적용」이 **작성된 대로는 불가능**: 네이티브 `<select>` 팝업은 OS가 그리므로 CSS를 받지 않는다(검수 R1 독립 재현 — 프로브 `tagName=SELECT`, `[role=listbox]` 0, 박스의 computed animation duration 0). 박스에 모션을 걸면 팝업이 아니라 **컨트롤**이 움직여 없느니만 못하다. 워커는 제외 + 부정 단정(주석 벗긴 소스에 `<select` 필수·`POPOVER_MOTION`/`data-[state=`/`@radix-ui/react-select` 금지)을 남겼다. | 계약 미충족이 아니라 계약 오기(브리프가 프리미티브 구현을 확인하지 않고 열거). 부정 단정은 Radix Select로 몰래 바꿀 때 붉어지는 실제 가드다. 모션 사다리(ADR-0179 D4)는 그대로. | `noted` | `select.tsx`·`STATUS.md`에 기록됨. Radix Select 도입 시 재측정 후 D4 적용 — 별도 티켓 없음(도입 계획 없음). |
| 2026-09-04 | UX-R1c #1998 / PR #2045 (R5) | R5 미션의 높이 사다리 상한 「프레임당 ≤12px」을 12채널 늘어남(+224px)에는 지킬 수 없어 워커가 그 케이스만 cap 64로 완화(줄어듦·+14는 12 유지). | 검수 R5 판정: 정직한 이탈 — 224에서 가드는 공허, 12에서 실제 사다리가 실패, 64에서 무애니 점프는 여전히 실패. 다만 64는 120Hz 측정값(48Hz 68.9px)이라 상한의 **모양**이 틀렸다: 이징 함수에 대고 단정하면 per-case 숫자가 필요 없다. 페이드 창을 Δh로 늘리는 대안은 기각(§2.6 사다리 고정). | `noted` | #2057에서 이징 기반 단일 규칙으로 교체. |
| 2026-09-05 | UX-R1b #1997 / PR #2072 | 브리프·ADR-0179 D1 문면은 「드로어 = `--motion-standard` 240」이나 워커는 390 오버레이 드로어·스크림을 `--motion-fast` 180 대칭으로 두고 데스크톱 사이드바 접기만 standard로 남겼다. ⌘K는 cmdk `Command.Dialog` 대신 하우스 `Dialog` + `AnimatePresence`로 exit을 붙들었고, `gate:shell` 390 Escape 단정을 스크림 detach까지 기다리도록 조였다. | 검수 R1 판정 수용: D1이 쓰일 때 「드로어」는 한 물체였고 지금은 둘 — 데스크톱 접기는 셸 상태 변화(그리드 열 보간, 233ms 실측)라 standard, 390 드로어는 `--elevation-float` 오버레이(shadow-lg + 블러 스크림)라 D4/D6 문법상 작은 떠 있는 표면 = fast; 대칭 180은 패널과 스크림을 한 사건으로 묶는다. ⌘K 교체는 Radix `open={false}`가 부모 언마운트와 겹치면 exit이 안 도는 실측. gate:shell 조임은 사보타주로 red 확인. | `accepted` | ADR-0179 D1 정오표(이 PR) + README §2.6·§5.2 행(PR #2072 내 반영). |
| 2026-09-05 | UX-R1e #2000 / PR #2071 | 브리프 「hover만 있는 상호작용 표면 전수에 `press`」를 본문 메시지·대기 행에 적용하면 `.press`의 `:active { scale(0.98) }`가 본문 드래그 선택을 무너뜨렸다(capture seq 1416, #1743 B-4). 워커는 그 자리를 채움만으로 두었고, 검수가 「와이드 행(≥480px 또는 열의 절반)은 채움만, 변형 없음」으로 일반화했다. `--surface-pressed` 토큰 신설(§2.2 자로 2회 재산정 → `light-dark(#efe2c8, #262335)`). | 검수 R1~R7 판정: D5는 「Button 전 variant는 `.press`, 행·칩 상속은 DS-1」이라 행의 scale을 요구하지 않는다 — 드래그 선택 보호는 정당한 D5 읽기. 대체 채움이 hover와 같은 토큰이던 것(#1515)은 `--surface-pressed`로 교정, 값은 §2.2 자(그릇 8쌍·accent-soft dE≥0.02·AA, 1670만 점 스캔으로 여유 한계 구조적) 확정. 폭 규칙 정의 통일은 #2076. | `accepted` | README §2.6 D5·§2.2 토큰(PR #2071 내 반영). #2076·#2074·#2048·DS-1(전폭 scale 소관). |

## 소급 항목 (2026-07-09 감사에서 발견된 역사적 이탈)

아래는 이 로그가 없던 시기의 이탈로, ADR 큐에 이미 배정됨 — 참고용.

| 출처 | 이탈 | 배정 |
|---|---|---|
| MOMO-256 | 김인턴 → Hermes 정체성 교체 (근거 무기록) | ADR-0106 |
| MOMO-325~333 | gateway 경로의 사실상 기본화 (계약 문서와 모순) | ADR-0102 |
| LOCAL_SOLO 레인 | M2 멀티팀 → 로컬 솔로 피벗 | ADR-0103 |
| 2026-06-26 | Actions 중단 → 로컬 게이트 머지 권위 관례화 | ADR-0107 |
