# 다음 세션 진입 브리프 (2026-08-09 작성 · Fable 인계용)

> 이 문서는 **새 Fable 세션이 가장 먼저 읽는 자리**다. `CURRENT_STATE.md`(스냅샷)는 누적 이력, 이 문서는 **지금 무엇을 하면 되는가**만 담는다. 오래되면 지우고 다시 쓴다.

## 0. 한 줄

메신저 본체·에이전트 층·인프라(CubeSandbox T3)는 서 있고, **지금 열린 전선은 ①디자인 시스템 「오르트 구름」 구축 ②Swift(macOS 클라) 삭제 ③프로덕션 보안 헤더 부재(#1213)** 셋이다. 성재 검수(데스크톱 Tauri + 모바일 RN)가 이 셋과 얽혀 있다.

## 1. 지금 상태 (2026-08-09 기준)

- **라이브**: 웹 `index-D4M7P01H` · 서버 `momo-rust:b727ea4f`(롤백 08e0c9d9) · NCP 101.79.11.189. **미배포 랜딩분 있음**(#1212 이후 — 다음 배포 묶음에 포함).
- **검수용 빌드**: 데스크톱 `scratchpad/oort-0809.app`(3축 이식 **이전** 버전 — 새로 구워야 함) · 폰 `scratchpad/MomoMobile-0809.xcarchive`(기기 unavailable로 설치 대기).
- **트랙**: `track/engine`이 개발선, `main`은 8/9 새벽 동기화 완료(a749d765). uxui 트랙은 정지 상태.

## 2. 열린 전선 3

### ① 디자인 시스템 「오르트 구름」 (ADR-0159 Accepted)
- 이름·정본화·전 축 가드 확장·macOS 동반 재조준까지 성재 결정 완료.
- 진행 중이던 것: **#1210**(결함 3 수리 — button secondary 3:1 미달·폰 파괴 채움 토큰 부재·포커스 링 페이드 25곳·웹 ESLint 게이트 미배선) · **#1211**(정본 문서 신설+토큰 대조 가드를 색→간격·타이포·반경·그림자로 확장).
- 정본: `docs/adr/0159-oort-cloud-design-system.md` · 감사 `research/2026-08-09-design-system-audit.md`(679줄 — **이게 명세다**).

### ② Swift 삭제
- **선행조건 충족됨**: Tauri 이식 3축(#1202 — 웹훅·이벤트구독·첨부) 랜딩 완료.
- **삭제 가능**: 클라 407파일(macOS 327·iOS 50·Core 30). **삭제 불가**(살아있는 제품): `relay/PushRelay`(유일 APNs)·`workers/WorkHostDaemon`(T3가 띄우는 바이너리)·`server/Migrations`(배포 DDL)·`services/MomoMetrics`.
- 파리티 갭: Swift-only 라우트 83개(openapi 성문 65) — 서버 삭제의 선행 과제.
- 정본: `research/2026-08-09-swift-removal-audit.md`(6단계 삭제 순서·12 티켓 후보·W-S 워커 지시 초안 포함) · ADR-0145 증보 1(삭제 조건)·증보 2(빌드 퇴역).
- 동반 처리: **#1201**(check_spm_licenses가 base부터 red — 전 프로파일 게이트 차단. 성재 결정: 삭제와 함께 은퇴).

### ③ 프로덕션 보안 헤더 부재 (#1213 · 신규·미착수)
- **app.oor7.com이 CSP·HSTS·X-Frame-Options·X-Content-Type-Options·Referrer-Policy를 하나도 안 보낸다**(실측).
- 원인: Caddy 설정 2벌 분기 — #1207이 고친 `infra/prod/Caddyfile`은 셀프호스트용이고, **라이브가 쓰는 `infra/rust/Caddyfile`엔 CSP 블록 자체가 없다**.
- 주의: HSTS는 켜면 브라우저가 기억하고, CSP는 잘못 좁히면 기능이 죽는다 — 배포 전 검증 필수(#1212가 만든 `gate:csp-deploy`를 라이브 파일도 재게 확장).

## 3. 성재 대기·결정 항목

- **검수**: 데스크톱(Tauri)·모바일(RN) — 3축+디자인 수리 반영 빌드를 새로 구워야 함. 폰은 기기 연결 필요.
- **#1164 ②**: accent 의미 재배선(「내 것」 vs 「여기를 보라」 소프트 단이 가까움) — 실기기 검수 후 판단.
- 미결 소형: #1168(스트리밍 preamble이 도구 카드 위) · #1208(웹훅 비밀 울타리에 회전 레인 부재) · #1173 후속 · ASC 콘솔 Disable · 셀프호스티드 러너 첫 등록 · codex CLI 롤백 여부.

## 4. 다음 세션에서 할 일 (권고 순서)

1. **#1210·#1211 결과 확인**(이 문서 작성 시점에 실행 중이었다면 PR 상태부터) → 검수·design-review·머지.
2. **#1213 보안 헤더** — 라이브가 무방비이므로 우선순위 높음. 헤더 세트 추가 + 두 Caddy 설정 관계 정리 + 게이트 확장 + **배포 후 라이브 헤더 재실측**.
3. **배포 묶음** — #1212 이후 랜딩분 전체(웹+서버) + 검수용 빌드 2종(Tauri `.app`·폰 아카이브) 재생성.
4. **Swift 삭제 1단계**(클라 407파일) — 감사 문서의 6단계 순서 그대로, 참조 0 만들기부터.
5. 그 뒤: 메모리 플레인(ADR-0129 MOMO-526~529 — 아직 미착수 대형 트랙) 또는 성재 검수 피드백.

## 5. 운영 규율 (이 세션에서 성문화된 것)

- 워커=**무명 단발 Opus**(이름 붙이면 mailbox 팀메이트가 되어 최종 보고가 유휴 신호로 대체됨). 패킷 없이 발주 금지. PR 만들고 STOP.
- **발주 전 랜딩분 대조**(`git log -S`/PR 검색) — #1139·#1146/#1149 헛발주 전례.
- track base PR은 **머지 시점에 이슈 수동 종결**(GitHub이 자동으로 안 닫는다).
- UI 변경은 design-review 필수. 리뷰 FAIL 시 원 워커 재개(세션 만료면 리뷰 전문+워크트리를 새 워커에).
- core 접촉 PR은 `scripts/verify_merge_tree.sh` 7레인. 병합 트리 flake 1회는 동일 트리 재실행으로 판정.
- NCP: 비번은 `~/.ncp/credentials.env`+pem으로 **API 재복호화가 정본**(파일은 tmp 청소로 사라진다). 전원 조작·이미지 전송은 세션 분류기에 막힐 수 있어 성재 `!` 대행 필요.
- 대기 중에도 큐를 병렬로 돌린다(성재 2026-08-09 지적).
