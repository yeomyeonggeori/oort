# 파이널 체크 목록 — 2026-08-04 사이클 (성재 실기 확인 항목만)

> 원칙: 자동으로 검증 가능한 것은 전부 게이트·MAESTRO 레인이 덮었다. 여기 있는 것은 **사람 눈·손·실기기만이 답할 수 있는 항목**이다.
> 전제 상태: 서버 `8a8df012` 라이브 · 웹(작업 패널 포함) 라이브 · 폰 Release 아카이브 `MomoMobile-8a8df012` (설치는 기기 연결 시 대행).

## A. 폰 (Release 빌드 재설치 후)

| # | 확인 | 기대 | 근거 |
|---|---|---|---|
| A1 | **컴포저 성장 수리 실기 유효성** (이번 사이클 1순위) — 입력이 두 줄로 늘 때·`@` 멘션 목록이 열릴 때·활동 줄이 설 때, 마지막 메시지가 컴포저에 가려지지 않는가 | 항상 보임 | #1007의 보정이 "한 이벤트 수명"이라 실기 iOS 이벤트 순서 의존(리뷰 M3 — 코드로 판정 불가) |
| A2 | 버벅임 체감 — 화면 전환·스크롤이 이전 대비 나아졌는가 | 개선 체감 | #1003은 렌더 횟수만 잠갔다. 프레임 타임은 실기만 안다 |
| A3 | `@luna` 멘션 → **타임라인 맨 아래 「루나 작업 중…」 자리표시** → 답 도착 시 그 자리가 답으로 바뀜 | 짧은 턴에도 인지 가능 | #1009 + 서버 여는 프레임(#1013) — 이제 라이브에서 실동 |
| A4 | 활동 줄의 **중단 버튼** 탭 → 확정 패널이 좁은 폭에서 두 줄로 접히며 버튼 둘 다 화면 안 → 중단 영수증+채널 취소 라인 | 잘림 없음 | #996 H1 수리의 실기 확인(리뷰 잔여) |
| A5 | 에이전트 탭 — 루나·오르트 상태 즉시 표시(스피너·"볼 수 없음" 없음), 긴 이름+배지 2개 행의 잘림 여부 | 상태 표시·잘림 허용 범위 | roster paused(#995)+소비(#996) · 행 폭은 미실측(리뷰 M5) |
| A6 | **잠금화면 승인**: ①아래 «승인 시나리오» 실행 → 푸시 도착 ②잠금화면에서 승인/거부 → Face ID 요구 ③앱 인박스에서 같은 건이 "이미 결정됨" | Face ID 강제·이중 결정 안전 | 푸시·Face ID는 시뮬 불가 — 실기 전용 |
| A7 | 백그라운드 정직성: 루나 작업 중 홈으로 → 20~30초 뒤 복귀 → 끝난 턴이 "작업 중"으로 안 남음 | 거짓 배지 없음 | #994 2R H1 수리 실기 확인 |

**승인 시나리오 (A6 선행)**: 검수 직전에 제가 안내하는 `!` 한 줄로 루나 툴(`work.session.end`) 활성화(이제 소비자가 라이브라 실효) → `@luna 지금 열려 있는 작업 세션 있으면 끝내줘` → 툴콜 → 승인 대기 생성.

## B. 웹 (app.oor7.com — 지금 바로 가능)

| # | 확인 | 기대 |
|---|---|---|
| B1 | `@luna` 멘션 → 활동 줄 턴 클릭 → **작업 패널**: phase 전이(생각 중→답을 쓰는 중→완료)+글자 흐름+비용 | 사고 과정이 시간순으로 쌓임 — "이 지점부터 관전" 고지 포함 |
| B2 | 에이전트 허브: 루나 모델 피커에 **luna 계열**·추론 강도 **매우 높음/최대** → 루나=luna 지정 저장 | 저장 성공(오르트=sol은 기본값이라 불요) |
| B3 | 작업 패널+스레드 동시 개방(900px 부근 창) — 채팅이 눌리지 않고 패널 닫으면 스레드 복귀 | 26px→276px 수리 실감 |
| B4 | 패널 도구 단계에서 인자 **값**이 안 보이는 것 — 이 상태가 취향에 맞는지(성재 결정 대기 1건: taste §9 완화 여부) | 현 상태=정본 준수. 값 노출 원하시면 §9 개정 결정 |

## C. 자동 검수가 이미 덮은 것 (참고 — 다시 볼 필요 없음)

렌더 횟수 회귀·어휘 경계(작업 중≠승인 대기)·인자 은닉·aria-live 범위·replay 종료 프레임·컴포저-키보드 간격 0px·거부/superseded 문구 — 게이트·red proof 총 30여 종이 잠갔고, MAESTRO 레인(랜딩 시)이 시뮬에서 5플로우를 반복 검증한다.
