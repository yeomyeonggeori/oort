# Design Review — ADE 2단계(#1136 @d4783104) — Verdict: FAIL(B1·H2·M3·N2)

## Blocker
- **B1 세션 카드=죽은 컨트롤**: `/channels/{id}?work=` 로 navigate하나 라우트 표는 `c/:channelId`뿐·와일드카드는 `/` 리다이렉트 — 런타임 증명(클릭→location=/·패널 0·쿼리 소실). D1이 지키려는 동선(orphaned 세션 이어받기)이 첫 클릭에서 죽음 — 카드 두 종류 중 세션 절반 전멸. 게이트 구조 구멍(확대 검사가 run 카드만 클릭) — 수리 시 세션 카드 확대 단언 동반.

## High
- H1 형제 표면(AgentWorkPanel) 미계산: ①Esc 한 번에 두 층 동시 닫힘(같은 타깃·같은 단계 리스너는 stopPropagation으로 서로 못 막음 — 양쪽 자기 주석 위반) ②600~899px에서 서랍이 패널을 완전히 덮는데 inert는 라우트에만 — 가려진 패널이 탭 순서에 잔존.
- H2 안 덮인 라우트 오른쪽="살아 보이는 시체": inert인데 시각 표지·스크림·바깥 클릭 닫기 전무(런타임 증명 — 허들 버튼 클릭 무반응·서랍 유지). 같은 셸 사이드바 서랍은 스크림을 버튼으로 세운 선례+근거 명문.

## Medium
- M1 resetAdeDrawer 미호출(로그아웃 생존 — 재로그인 직후 inert+덮임 시작) · M2 서랍이 라우트 이동 생존(사이드바 서랍은 정반대 판정 명문 — 한 셸 두 규칙) · M3 880~1000px 조각 띠(899px 실측: 19px 띠에 전송 버튼 반원 절단).

## Nit
- N1 마지막 작업 종료 시 opener 언마운트→닫기 포커스 body 추락(폴백 목적지 부재) · N2 접근 이름 zero-width space 자리표.

통과 기록: 불밀림 픽셀 증명·요약 줄 무게 적정·390px 무절단·대비 닫힌 행렬·생존성 3문구 정직·의도적 결정 5종 재론 없음. emdash 12=기존 부채(이 PR 유입 0).
