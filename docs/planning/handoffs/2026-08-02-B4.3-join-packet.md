# goal B4.3 — /v1/join 구현: 초대 링크 가입 (도그푸딩 온보딩)

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(B4.2 랜딩 `b42f1194` 이후). 워크트리 `~/projects/momo-tracks/momo-worktrees/B43-join`(브랜치 `feat/B43-join`, 생성됨).
근거: Swift 실측 `server/Sources/MomoServer/Routes/JoinRoutes.swift`(915 — 초대 코드→워크스페이스 해석은 `momo_join_private.invite_workspace_id` 잠금 함수, 009·bootstrap_roles 실측) + diff 매트릭스 C-1(`/v1/join`).

## 0. 규율 (기존 동일)
`.env` 금지 · **PR 후 STOP**(amend/force-push 금지) · docker 검증 금지(`#[ignore]`만) · Swift 계약=정답 · UI 수정 금지 · route raw SQL 0(단 `momo_join_private.invite_workspace_id` 호출은 잠금 함수라 소유 crate에서 허용 — 위치 판단·명시) · 새 마이그레이션 금지 · 신규 crate 시 Dockerfile 목록 갱신.

## 1. 할 일
- `/v1/join` 전체 흐름 Swift 실측 파리티: 초대 코드 검증(잠금 함수 경유 — momo_app만 EXECUTE 가능, bootstrap_roles 실측)→계정 생성/기존 로그인→멤버십 부여→토큰 발급. B4.2가 연 초대 발급과 접합(half-open door 마감).
- 레이트리밋·중복 가입·만료 코드 등 Swift의 거절 케이스 전수.
- **conformance `#[ignore]`**: 발급→가입→로그인→roster 반영 왕복 red(코드 재사용·만료·비잠금 롤 EXECUTE 거부 포함) + RLS.

## 2. 하지 말 것
에이전트 허브·routing·초대 UI·이메일 발송(있으면 후속 기록)·새 마이그레이션.

## 3. 검증·PR
cargo check/test/fmt/clippy·구조 grep·docker build는 오케스트레이터. PR `feat/B43-join` → `track/engine`. 본문: 흐름 파리티 표·거절 케이스 목록·red 절차·이탈. **PR 후 STOP.**
