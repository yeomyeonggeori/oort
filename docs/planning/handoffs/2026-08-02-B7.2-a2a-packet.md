# goal B7.2 — A2A 개방: 에이전트 간 멘션·위임 (깊이 캡·루프 게이트·과금 상한)

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(B6 랜딩 `e6e437bb` 이후). 워크트리 `~/projects/momo-tracks/momo-worktrees/B72-a2a`(브랜치 `feat/B72-a2a`, 생성됨).
배경: B5.2가 A2A를 fail-closed로 차단(소스 run 없이 depth 캡 강제 불가 → `a2a_source_run_unavailable` 감사만). 이제 안전장치와 함께 연다.
근거 실측: Swift `MessageRoutes.routeAgentMentions`의 A2A 분기 + 루프 안전 게이트(G1/G2/G3 — Swift AgentWorker/게이트웨이에서 실측: 무엇이 G1/G2/G3인지 코드가 정본) + `agent_run`의 depth/부모 결속 스키마(001:272-282 등 실측).

## 0. 규율 (기존 동일)
`.env` 금지 · **PR 후 STOP**(amend/force-push 금지) · docker 검증 금지(`#[ignore]`만) · Swift·스키마 실측=정답(스키마가 depth 컬럼을 이미 갖는지 실측 — **새 마이그레이션 금지**, 부족하면 STOP 보고) · route raw SQL 0 · 실 provider 호출 금지(mock).

## 1. 할 일
- **worker 응답 경로에 멘션 라우팅 합성**: agent-worker가 응답을 쓸 때(B5.1 send_message_in_tx 호출부) 본문 멘션 파싱+mention 라우팅을 같은 tx에 — 사람 HTTP send와 동일한 의미(B1.2/B5.2의 route 합성과 대칭, momo-agent API 재사용). 이때 **소스 run이 있으므로 depth = 부모+1**.
- **깊이 캡**: 기본 `A2A_MAX_DEPTH=3`(env). 초과 멘션 = run 생성 안 함 + 채널에 시스템 라인("위임 깊이 한도") + 감사. Swift 게이트 실측 의미 우선.
- **루프 게이트**: Swift G1/G2/G3 실측 이식(예상: 동일 체인 내 재방문/왕복 반복/시간창 상한 — 실측이 정본). 위반 = 정직한 시스템 라인+run 중단.
- **체인 과금 상한**: 한 루트 트리거에서 파생된 전체 체인의 토큰/비용 합계 상한(env, ledger 합산) — 초과 시 체인 정지+가시 라인.
- **conformance `#[ignore]`**: ①A2A 대화 e2e — 에이전트A 멘션→A 응답이 @B 멘션→B run(depth=1)→B 응답 채널 도착 ②상호 멘션 루프 → 캡에서 정지(red: 캡 제거 시 무한) ③체인 과금 상한 red ④depth·부모 결속이 run 행에 기록(provenance/감사 추적 가능).

## 2. 하지 말 것
Claude provider(B7.1)·tool call/Context Packet(B7.3)·workd(B7.4)·새 마이그레이션·UI(후속).

## 3. 검증·PR
cargo check/test/fmt/clippy·구조 grep. PR `feat/B72-a2a` → `track/engine`. 본문: Swift 게이트 실측표(G1/G2/G3 실체)·depth 스키마 근거·red 절차·이탈. **PR 후 STOP.**
