# Shared orchestration and context — implementation brief

Status: ready
Planning ID: PLN-20260912-SHARED-PIPELINE
Owner/reviewer: Astra (Codex)
Track/base: engine, origin/track/engine @ ed26b020
Implementer: Grok 4.6
Authorization: 2026-09-12 user explicitly requested simplifying the full pipeline/Markdown structure for interchangeable Astra+Grok4.6 or Fable+Grok4.6 in Codex/Claude Code. This replaces conflicting model-specific stop rules and unnecessary reading/checklist mandates, NOT product/API/security/release decisions.
Issue binding: added after issue creation. Acceptance authority: this packet.

## Outcome
Both harnesses recover the same current work, ownership, evidence and next step from this checkout plus a shared local coordination directory. Root instructions are short and task-specific references are loaded only when relevant. Existing beta work (#2498/#2499 and Fable PRs) remains paused; this issue changes operation/docs only.

## Design (Astra)
1. AGENTS.md is the shared concise contract for BOTH planner and worker, not just worker. CLAUDE.md is a thin native @AGENTS.md import + harness-specific note. Codex consumes AGENTS normally. Do not create a parallel CLAUDE state, second roadmap, or duplicate rulebook.
2. Common path for tracked knowledge stays docs/planning/CURRENT_STATE.md (ONE current snapshot), JOURNAL (history), README (workflow choices), PIPELINE (only model/lane/cap values). Move old CURRENT_STATE snapshot block intact to archive, turn stale NEXT_SESSION_BRIEF into redirect. Archived text is historical evidence, not live instructions.
3. Cross-worktree live state belongs to a tiny host-local directory under resolved git --git-common-dir, e.g. oort-coordination/. Both harnesses and all worktrees of this repo see it; separate clones don't (document this limit). Keep scoped ownership and short checkpoint notes there; canonical decisions/evidence still tracked. No secret values, production data, or private logs in tracked docs.
4. A small stdlib helper scripts/planning_session.py may expose path/status/claim/checkpoint/release. Use atomic claim, refuse a second owner for the same scope; separate planner scopes allowed; integration scope is exclusive. No time-based auto-steal or implicit worker termination. Notes support source worktree/HEAD, owner/model/harness, issue/PR, run directory/PID/exit artifact, last verified result, next action, actual blocker. Avoid elaborate scheduler/config, daemons, dependencies or copied shell launchers. Safely publish checkpoint writes, preserve last notes on release. Input names must not escape the shared directory. Keep CLI small and explain --help.
5. scripts/planning_context.sh default becomes a small offline restore: checkout/HEAD/dirty, CURRENT_STATE, shared coordination status and next step. Move global Proposed ADR/deviation scans and journal detail behind --details; keep --github opt-in only. Must work from nested directories and arbitrary hook cwd by resolving its own repo path; a missing live board means no registrations, not proof no external workers. No launch/fetch/network on default read.
6. Add tracked .claude/settings.json SessionStart hook (startup/resume/clear/compact) to the same offline restore script, preserving existing settings.local.json untouched. Codex AGENTS instructs restore only on planning/session handoff, not before every typo. No global user config edits.
7. Planning skill becomes short capability/router with precise description, one real source under .agents/skills/momo-planning and compatibility path .claude/skills/momo-planning (relative symlink or thin router, never duplicate workflow prose). Simplify design-review entry description and unnecessary universal reading only if needed; preserve established UX rubric and B0/H0, current Opus-design option in PIPELINE. No broad redesign of design skill.
8. PIPELINE supports planner Astra/Codex OR Fable/Claude Code as equals, Grok4.6 implementation. Keep combined heavy worker/reviewer cap2 and sequential heavy gates. Document available native vs existing local Grok CLI adapters only on demand; DO NOT prescribe bypass permissions as default. Tool availability/caps -> smaller task-specific tool set; platform content denial -> record and stop that action, never route around it. FABLE_DOWNGRADE_ROUTINE becomes compatibility pointer to model availability/ownership handoff; switching between approved Astra/Fable is not downgrade requiring stop.
9. README: self-contained Issue acceptance for routine bounded changes; optional handoff packet when cross-domain/ADR/complex work needs it. Do NOT mandate ADR→BUILD_TICKETS→packet→Issue for a typo. Existing packet contracts remain authoritative. Product/API/security/schema/stack direction changes retain Accepted ADR gate. User authorization persists; do not ask again for reversible authorized local edits/tests. Continue implement→verify→review→fix until issue DoD; worker stopping after handoff does not stop orchestrator.
10. Remove obsolete unconditional all-doc reading, Slack comparison for every ADR, blanket mutation/red-proof for every assertion, mandatory full gate reruns with unchanged tested head, unconditional merge origin/main before work, automatic host-global Docker pruning, native-thread assumptions and Swift-era 5-worker/CLI paths. Regression proof required when it demonstrates a real behavioral bug or oracle risk, not every Markdown change.
11. Preserve actual contracts: worktree isolation, single issue/PR, immutable schema_v0.sql, tenant RLS, single transactional write spine, permissive dependencies, secret hygiene, M7/store/release and owner approval, exact-base policy verifier and current PR CI, no force pushing canonical branches, single integrator, preservation of others' dirty work.
12. Relocate build/validation specifics to existing docs/LOCAL_PR_GATE.md or a gated docs/runbooks/development-validation.md, linking them from AGENTS. Do not silently remove required Rust fmt/clippy/workspace tests, relevant TS merge-tree, runtime verification, docs gate. Verify removed anchors/callers; fix live callers without rewriting history.
13. Live docs still contradict track flush: docs/TRACKS §3.1.1 is correct (planning lands track/engine). Repair PIPELINE/MULTI_SESSION direct-main advice and stale disabled Actions/Swift UI routes. Keep standing promotion authorization exactly scoped; no broader release grants.
14. scripts/goal_status.sh currently labels almost everything swift and suggests removing canonical track/engine and track/uxui worktrees as done-candidates. Inspect and fix just these pipeline reporting defects, using actual current gate profiles and never emitting cleanup for canonical branches. Do not delete any worktrees.
15. User progress format: short overall phase table plus this batch's done/current/next with evidence; distinguish implemented/reviewed/merged/released/runtime-verified. No fabricated percent/date. At meaningful checkpoints save shared note, at integration/scope completion update canonical pointers + 5-line journal. Do not require writing 5 docs for every action.

## Files in scope
AGENTS.md, CLAUDE.md; docs/planning/{README,PIPELINE,CURRENT_STATE,NEXT_SESSION_BRIEF,FABLE_DOWNGRADE_ROUTINE,HANDOFF_TEMPLATE,JOURNAL}.md and appropriate archive/current checklist; docs/TRACKS.md and docs/MULTI_SESSION_OPS.md; .claude/settings.json; planning skill only + narrow design-review entry update; docs/LOCAL_PR_GATE.md or docs/runbooks/development-validation.md; scripts/planning_context.sh, new planning_session.py, scripts/goal_status.sh, directly relevant focused tests; STATUS top 1–3 lines. Correct live links in docs/INDEX.md/.github templates if necessary.
scripts changes explicitly authorized here for this task and require Astra diff audit. No policy-integrity evaluator/workflow, local_gate gate weakening, product code, .env, schema, deps, release changes. If check_docs_commands coverage must include a relocated command doc, keep checks at least as strong and report exact change.

## Current project checkpoint to migrate accurately
- User paused beta implementations. #2499 PR2500 HEAD f98c826571dfa8428b6ee992d42646c6e6c45f45 clean/pushed; Railway pins fixed, focused tests PASS, Astra docs77 PASS at that head on 2026-09-12 14:23:01KST; not merged/deployed.
- #2498 HEAD16fcf007bac0bf0fe8e7a12c138233c260bd3940 clean/unpushed (2 commits ahead), production code saved; fmt + auth88 + routes9 PASS from worker log. No independent review/HTTP-PG concurrency/full clippy/tests/PR yet.
- Plan base main4cf30e4f, engine ed26b020, UXUI0de7a796. Read actual Git/PR before claiming newer status.
- Fable-owned PRs #2485/#2490/#2497 not taken over. Latest prior evidence #2490 R2 pending, #2485 design pending, #2497 read-only review completed. Preserve ownership uncertainty.
- Railway volume/UID default causes drive dir permission boot failure; notifier + backup/restore + same-image A/B still remaining.
- #2066 existing rate limiter already exists; only key-preserving separation/bounded audit proposed, not approved.
- No fixed launch date confirmed. G1′ A/B -> ITO -> external3 + iOS v0/G2.
- Prior Astra beta turn failed at 14:23:21KST due platform automatic classification with unknown precise trigger; both background workers later exit0. No auto retry of that action in this pipeline task.
- Durable earlier audit files at /Users/kwakseongjae/.codex/visualizations/2026/09/12/01a093d5-70d3-7e70-ac40-940b34f86ecd/momo-audit/ (read only if needed; do not commit private absolute artifact paths).

## Validation and handoff
Test observable behavior: two detached temporary worktrees share same board; second same-scope claim denied; different scopes allowed; checkpoint visible from both; release retains history; traversal rejected; default context offline from subdir and hook cwd; malformed/missing board handled honestly. Fake git/gh fixture or existing goal_status harness for canonical cleanup suppression and rust/web/profile mapping. No external processes or existing resources mutated.
Measure before/after entry/context bytes (diagnostic evidence, not fragile wording test); check symlinks/local links and Claude JSON. Cold-start scenario from both entrypoints should recover same next task/owner/blocked state.
Run focused tests/static validation first and commit. Do NOT run full docs gate in this round: known inherited Railway pin failure on engine is repaired only by PR2500, and Astra manages heavy gates sequentially. Report this honestly; never incorporate #2499 code silently. No PR/push/merge/close in this worker round; Astra will review and complete integration workflow.
Return DONE / COMMITS / TESTS / REMAINING / DEVIATIONS plus file list, concise. Existing root AGENTS clauses conflicting with this packet's user-authorized simplification are being revised as task scope, not permission blockers.

