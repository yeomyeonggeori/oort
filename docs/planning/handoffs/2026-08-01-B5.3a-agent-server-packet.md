# goal B5.3a — 에이전트 운영 서버 표면: 채널 초대·프로필 편집·pause·routing

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(B5.2 랜딩 `0df2b1b9` 이후). 워크트리 `~/projects/momo-tracks/momo-worktrees/B53a-agent-server`(브랜치 `feat/B53a-agent-server`, 생성됨).
근거: Swift 실측 `ChannelRoutes.swift`(멤버 초대/제거)·`AgentProfileRoutes.swift`(편집·pause·allowed-models)·`RunRouting.swift`+`WorkTierPolicyRoutes`(routing 계약 — B4 diff의 `routing{}` 프로브가 잠근 그 표면) · momo-agent/momo-messaging 기존 API.

## 0. 규율 (기존 동일)
`.env` 금지 · **PR 후 STOP**(amend/force-push 금지) · docker 검증 금지(`#[ignore]`만) · Swift 실측=정답 · route raw SQL 0 · 새 마이그레이션 금지 · **병렬 배치 B5.3b가 clients/web 작업 중 — 무접촉**.

## 1. 할 일
- **채널 멤버십 REST**(B5.2 갭): 채널에 멤버(사람·에이전트) 추가/제거 — Swift ChannelRoutes 실측 파리티. 에이전트를 채널에 넣으면 멘션 가능해지는 것이 티키타카 온보딩.
- **프로필 편집·pause**: PUT 프로필·pause/resume 토글·allowed-models(실측 범위) — B5.2가 "읽고 존중"만 하던 paused의 쓰기 표면.
- **routing 실구현**: send의 `routing{}` 요청 수용(B4.1이 400 거절하던 것) — Swift 계약 실측(tier/model 선택→run에 반영). B4.1의 `thread_root_then_routing` 순서 함수 규약대로 그 함수 갱신(프로브 거짓말 금지 — 이제 지원하니 ready가 진실).
- **conformance `#[ignore]`**: ①에이전트 채널 추가→멘션→응답(초대→티키타카 접합) ②pause→멘션해도 run 0+paused 라인 ③routing 지정 send→run에 반영 ④비인가 케이스.

## 2. 하지 말 것
웹 UI(B5.3b)·스트리밍·A2A·새 마이그레이션.

## 3. 검증·PR
cargo check/test/fmt/clippy·구조 grep. PR `feat/B53a-agent-server` → `track/engine`. 본문: 파리티 표·red 절차·오케스트레이터 목록·이탈. **PR 후 STOP.**
