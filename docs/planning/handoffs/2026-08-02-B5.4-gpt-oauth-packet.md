# goal B5.4 — GPT OAuth provider (ADR-0147 이행)

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(B5.3b 랜딩 `a9e26204` 이후). 워크트리 `~/projects/momo-tracks/momo-worktrees/B54-oauth`(브랜치 `feat/B54-gpt-oauth`, 생성됨).
근거 정본: **ADR-0147**(Accepted — 결정 4·제약 전문) · ADR-0004 증보 1(봉인 계약) · B5.1 agent-worker provider 추상 · B4.2 provider link REST/암호화 · Codex CLI auth.json 형태 실측(`~/.codex/auth.json` 구조 — **값 열람 금지, 키 이름·구조만**. jq 'keys' 수준).

## 0. 규율
`.env`·auth.json **값** 열람 금지(구조만) · **PR 후 STOP**(amend/force-push 금지) · docker 검증 금지(`#[ignore]`만) · momo-server HTTP 0 유지(OAuth 호출·갱신=agent-worker만) · 새 마이그레이션 금지(provider_link 스키마가 kind 확장을 수용하는지 실측 — 안 되면 STOP하고 보고) · Dockerfile 목록(신규 crate 시).

## 1. 할 일
- **provider_link kind 확장**: `oauth-openai` — 봉인 페이로드에 refresh_token(+access_token·만료·계정 라벨). B4.2 REST가 그대로 받도록(필드 실측). 링크 메타에 "개인 계정 귀속·내부용" 라벨 필드.
- **agent-worker OpenAI provider**: Bearer 호출(chat completions — Codex/ChatGPT OAuth 토큰이 쓰는 엔드포인트 실측) + 401/만료 시 refresh 갱신(OpenAI token endpoint) + **갱신 토큰 금고 재봉인**(worker→DB, 봉인 계약 준수) + 갱신 실패=run 실패+사용자 가시 "다시 로그인" 오류.
- **conformance `#[ignore]`**: mock OAuth 서버(테스트 내 HTTP)로 ①정상 티키타카(Bearer 호출) ②만료→refresh→재봉인→재시도 성공(red: 재봉인 빼면 다음 turn 실패) ③갱신 실패→run failed+오류 메시지 ④금고 평문 비유출(로그·DB 평문 0).

## 2. 하지 말 것
momo-server의 OAuth 중계·브라우저 플로우 · 코딩 에이전트 경로(ADR-0144 불변) · 실 OpenAI 호출(전부 mock — 실토큰은 오케스트레이터/성재 smoke 몫) · 새 마이그레이션.

## 3. 검증·PR
cargo check/test/fmt/clippy·구조 grep(momo-server reqwest 0 유지). PR `feat/B54-gpt-oauth` → `track/engine`. 본문: auth.json 구조 실측(키 이름만)·엔드포인트 근거·red 절차·이탈. **PR 후 STOP.**
