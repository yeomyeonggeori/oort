# 핸드오프 패킷 — Tauri 이식 3기능 (#1202, 3워커 병렬)

- status: **ready** · 기준: `origin/track/engine` 최신 · 워커=단발 Opus 무명 · 성재 지시 2026-08-09(Swift 삭제 선행조건·**한 배치 3기능**)
- 정본: #1202 · 감사 `research/2026-08-09-swift-removal-audit.md`(macOS 표면 목록) · **이식 원본=macOS SwiftUI 구현**(clients/macOS — 화면 구조·문구·상태 처리의 참조 정본. 베끼는 게 아니라 **웹 관용구로 다시 쓴다**) · 서버 계약=`docs/api/openapi.yaml`(셋 다 기존재 — 와이어 신설 0이 목표)
- 경합 지도: W-T=첨부(컴포저·타임라인) / W-U=웹훅 설정(설정 표면) / W-V=이벤트구독 설정(설정 표면) — **U·V가 같은 설정 셸을 만진다**: V는 U의 라우팅/셸 구조를 따르되 파일 분리, 머지는 U→V 순차.
- 공통 규율: **UI 변경 — design-review는 오케스트레이터 발주**(캡처 다크/라이트 필수). 웹 스택 taste 스킬(`momo-design-taste-web`)+ux-bible 준수. 와이어 신설 시 이탈 절에 근거. 동결층·시크릿 불변.

## 워커 T — 첨부 UI (첫 배치의 무게중심)
- 서버는 ADR-0151/#1119로 완료(Drive 3경로+바인딩·openapi 21). 웹에는 **의도적 부재 주석**만 있다(`ThreadComposer.tsx:21` "no attachments. Those belong to the surface where…") — 그 주석이 가리키는 "다른 표면"이 무엇인지 실측 후, 컴포저(메인+스레드)에 업로드·진행·실패·재시도 4상태와 타임라인 렌더(이미지 미리보기·파일 카드·용량/타입 표기)를 웹 관용구로.
- macOS 참조: 첨부 관련 16파일 — 특히 상태 전이·실패 문구·권한 경계(누가 볼 수 있나)를 그대로 계승.
- 함정: 업로드 중 전송·취소·재시도의 낙관적 UI와 `clientMsgId` 정합 · 대용량/느린 회선에서 진행 표시가 거짓말하지 않게 · 첨부 없는 메시지 경로 무회귀.
- 검증: 웹 스위트+lint 총계·게이트(관련)·캡처 4상태×2스킴·red proof ≥2·병합 트리 7레인. PR "#1202 첨부 축"·이탈 절·STOP.

## 워커 U — 웹훅 설정 표면
- macOS 11파일 참조. 설정에 웹훅 목록/생성/수정/삭제·시크릿 표시 규율(한 번만 보여주기 등 macOS 규율 계승)·전송 실패 가시성. openapi 25건이 계약 정본 — 와이어 신설 0 목표.
- 함정: 시크릿을 화면·로그에 남기지 않기(ADR-0004 정신) · 삭제는 되돌릴 수 없으므로 확인 단계 · 권한(누가 만들 수 있나 — 서버 판정 신뢰, 클라 임의 게이트 금지).
- 검증: 위와 동일 + 시크릿 비노출 단정. PR "#1202 웹훅 축"·STOP.

## 워커 V — 이벤트구독 설정 표면
- macOS 3~7파일 참조. 구독 목록/생성/삭제·이벤트 종류 선택·대상(채널/워크스페이스) 경계. **W-U의 설정 셸 구조를 따른다**(중복 셸 금지 — U 브랜치를 로컬 병합해 개발, PR 본문에 의존 명시).
- 검증: 위와 동일. PR "#1202 이벤트구독 축"·STOP.

## 마감 조건
3축 전부 랜딩+design-review PASS → **#1202 종결 → macOS 삭제 워커 발사 가능**(그 시점에 #1201 라이선스 검사도 함께 은퇴 — 성재 결정 2026-08-09).
