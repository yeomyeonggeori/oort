# goal #851 (2R) — 칩 축소 우선순위 · 세 관점 일관성 · 상세 진입. 그 외 금지.

기존 브랜치 `feat/851-momo-644-my-sessions`(HEAD `ac92b074`)에 **커밋·푸시만**. PR 생성·merge/close 금지.
모델: gpt-5.6-sol medium.

1R 판정: **FAIL (Blocker 1 · High 3)**. 오케스트레이터가 코드에서 재확인했다.
**리뷰가 인정한 것(회귀 금지)**: mine 표면의 삼분법(원장 불변·표시만 "호스트 응답 없음"·스레드 열림) ·
host 진실 대기 스켈레톤(mine) · 소유자 필터 · 빈 상태 3종 · 중립 미지 폴백 · preflight 10/10.
**핵심 원칙이 이미 네 코드에 있다** — "원장이 정본, 표시는 사실만, host 진실을 기다린다."
1R 지적 4건은 전부 **그 원칙을 네가 안 지킨 나머지 분기**다.

## 0. 착수 전
1. `git status` clean. 2. 자격증명 금지. 3. typecheck·test·build·preflight 필수, Playwright는 오케스트레이터. 4. grep으로 실재 확인.

## 1. [Blocker] 기본 1280 창에서 스코프 칩 "전체"·"내 세션"이 "전...", "내 ..."로 잘린다

`ScopeButton` 셋이 전부 `min-w-0 truncate`(`WorkPanel.tsx:87`)를 공유해, **채널 칩이 길면 고정 라벨 둘을 굶긴다.**
다인 DM(참가자 이름 연결)이면 평범하게 재현되고, **잘리는 라벨이 이 PR이 추가한 진입점 자신이다.**
→ **고정 짧은 라벨 둘("전체"·"내 세션")은 절대 줄지 않고, 채널 칩만 truncate.** flex 축소 우선순위로 풀어라.

## 2. [High] 채널/전체 관점은 여전히 host 진실 전에 "실행 중"을 그린다

패널은 **채널 관점으로 기본 오픈**되므로, 열 때마다 첫 화면이 "거짓 안심 → 뒤집힘"이다.
mine은 스켈레톤으로 기다리는데(`02` 실측) 채널/전체의 `SessionRow`는 hosts-미도착(null)을 online과 동일 취급한다.
→ **세 관점 모두 같은 규칙**: host 진실 도착 전에는 "실행 중" 단정을 그리지 않는다. mine의 대기 방식을 공유해라.

## 3. [High] 오프라인이면 mine 행의 유일한 상세 진입이 잠긴다 — 재개 결정에 상세가 필요한 바로 그 순간에

상세는 이 상태를 정직하게 다룬다(중립 배너·fail-closed 카피·세션 종료/발췌 공유 액션 보유).
그런데 mine 행에서 상세로 가는 유일한 버튼이 "터미널 관전"이고 오프라인이면 disabled다 —
**"터미널이 스트림 못 함"과 "상세가 닫힘"을 섞었다.**
→ **터미널 거부는 유지하되 상세 도달은 유지해라.** (진입이 터미널 이름으로 상세 역할을 하고 있던 것이 원인 — 이름과 역할을 갈라라.)

## 4. [High] 호스트 등록 0건이면 헤더는 "세션 1개"를 세면서 본문은 세션을 숨긴다

`WorkPanel.tsx:855`가 `hosts.length > 0`을 렌더 조건으로 걸었는데, **행 단위 답이 이미 있다**
("알 수 없는 호스트 · 상태 확인 필요" — `10` 실측). "내 세션"이라는 이름의 표면이 원장이 인정하는 세션을 부정한다.
→ **hosts-empty 안내는 보이는 세션도 0일 때만.** 세션이 있으면 행을 그리고 호스트 칸은 미지 폴백으로.

## 5. [Medium — 같이, 싸게]
- **M-1 메타 줄 truncation이 사실 중요도를 뒤집는다**: 긴 호스트 이름이 host 상태·tool·시작 시각을 밀어낸다.
  **고정 길이 사실은 무한 필드의 희생자가 되지 않게** — 무한 필드(호스트/채널 이름)만 truncate.
- **M-2 `openWorkSessionThread` 무한 페이지 걸음**(`ChatShell.tsx:201`): 페이지 상한 + 기존 not-found 카피로 묶어라.
- **M-3 상세 오프라인 배너가 "세션 스레드는 계속 확인할 수 있습니다"라며 스레드 액션이 없다**: 문장이 위치를 말하거나, 상세가 그 액션을 갖거나. 택1, 근거를 커밋에.
- **N-4 상세 닫기 후 포커스가 mine 행 버튼으로 안 돌아온다**(rowRefs가 SessionRow만 추적) — #838/#839에서 반복된 클래스다. 잡아라.

## 6. 하지 말 것
- M-4 "터미널 관 / 전" 줄바꿈은 **선존재**(ObserverTerminal 무변경) — 후속 티켓, 손대지 마라.
- N-1(스켈레톤 개수)·N-2(이중 "상태 확인 필요")·N-3(행 버튼 문법)은 후속. 서버 변경 금지. idle 선구현 금지.

## 7. 검증
- typecheck · test(897+, 무회귀) · build · preflight 10/10.
- **게이트에 잠글 것**: ①긴 채널 이름 픽스처에서 "전체"·"내 세션" 라벨 전문 표시 ②채널 관점 host-gap 중 "실행 중" 부재 ③hosts-empty+세션 존재 시 행 렌더. 기존 단정·red proof 2종 유지. **각각 되돌리면 빨개지는 절차 명시.**
- 커밋에: flex 우선순위 방식, 세 관점 공유 방식, 상세 진입 재설계, 손대지 않은 것.
