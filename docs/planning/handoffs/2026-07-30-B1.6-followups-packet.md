# goal B1.6 — 소품 3종: logout/refresh route · 러너 멱등 추적 · OutboxKind 완비

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(B1.5 랜딩 `c98b6474` 이후). 워크트리: `~/projects/momo-tracks/momo-worktrees/B16-followups`(브랜치 `feat/B16-followups`, 생성됨).

## 0. 착수 전 필수
1. `git status` clean. 2. `.env`류·자격증명 열람 금지. 3. **PR 후 STOP**, amend/force-push 금지. 4. docker 검증은 오케스트레이터 몫(`#[ignore]` 작성만). 5. Swift 실측 후 이식. 6. **B2.1이 병렬 진행 중**(신설 `momo-t3`·momo-provider mock) — 그 영역 무접촉. 네 표면: `bins/momo-server`(route)·`crates/momo-auth`(token_store revoke API)·`crates/momo-db`(migrate.rs)·`crates/momo-outbox`(emit.rs enum)만.

## 1. 소품 A — logout/refresh route (revocation 트리거 표면)
- Swift `AuthRoutes.swift`에서 logout·refresh 엔드포인트 실측(경로·요청/응답·revoke 범위 — access만? 세션 전체?) → momo-server에 동일 이식.
- token_store에 `revoke` 계열 공개 API 추가(Swift `TokenStore.revoke`/`revokePrivilegedSessionTokens` 실측 의미).
- refresh: refresh 토큰 검증(typ)→새 access 발급→(Swift가 구 토큰 revoke하면 동일하게).
- **smoke 확장**(`http_smoke_pg.rs`에 케이스 추가): logout→구 access로 요청→401 red / refresh→새 access 정상·(Swift 의미대로면) 구 refresh 재사용→401.

## 2. 소품 B — 마이그레이션 러너 schema_migrations 멱등 추적
- `scripts/migrate.sh`의 skip 판정 실측(schema_migrations 테이블 — 어느 마이그레이션이 만드나, 없으면 러너가 만드나 실측) → `momo-db/migrate.rs` `run_migrations`에 동일 의미 이식: 적용 전 조회→적용분 SKIP→신규만 APPLY→기록. verify 패스(2-run 등가) 선택.
- **red**: 같은 DB에 러너 2회 실행 → 두 번째 전 파일 SKIP(신규 적용 0). conformance에 추가(`#[ignore]`). 이로써 "테스트 바이너리마다 fresh DB" 제약 해소 — 각 테스트 doc 주석 갱신.
- migrate.sh·마이그레이션 파일 무수정.

## 3. 소품 C — OutboxKind `push_candidate` 추가
- `momo-outbox` enum에 `PushCandidate`("push_candidate", 011) 추가 + 라벨 테스트. relay claim이 broadcast만 집는 것 불변(라벨 필터 확인). emit 경로에서 push_candidate를 앱이 만들 일은 없음(DB 트리거 몫) — doc 주석에 명시.

## 4. 검증·PR
- cargo check/test/fmt/clippy 무회귀. smoke 확장 red 케이스 명시.
- **오케스트레이터 목록**: 러너 2-run 멱등 red + logout/refresh smoke(fresh pgvector/pg18).
- PR `feat/B16-followups` → `track/engine`. 본문: 소품별 실측 근거·red 절차·이탈. **PR 후 STOP.**
