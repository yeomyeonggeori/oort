# 워커 브리프 — LS-0 게이트 재배선: Swift·web-legacy·infra/prod·NCP 레인 제거 + web 프로파일 clients/web 재조준 + 은퇴 verifier 삭제 (engine · #2142 · ADR-0183 Accepted)

> 워커: grok 4.6 · base=origin/track/engine · 워크트리 `momo-worktrees/wls0`(`feat/ls0-gate-rewire`) · 시작 절차: `git merge origin/main --no-edit`
> 정지 조건: 머지·이슈 close 금지. **보호 경로 허용 목록(이 PR은 정책 무결성 감사 대상 — planner가 승격 시 감사)**: `scripts/local_gate.sh` · `scripts/tests/**` · `scripts/check_docs_commands.py` · `scripts/check_compose_env_templates.sh` · `scripts/gate_oort_user_facing.sh` · 아래 §1 삭제·개명 대상 · `.github/workflows/pr-ci.yml` · `.github/dependabot.yml` · `Makefile` · `server-rust/Dockerfile`·`Dockerfile.dockerignore`·`bins/momo-migrate/**` · `infra/rust/sql/**`(신설) · `infra/rust/README.md` · `docs/LOCAL_PR_GATE.md`. **그 밖의 `scripts/**`·`.github/**` 무수정.** `AGENTS.md`·`docs/TRACKS.md`·`CODEX.md` 무접촉(LS-3). `server/Migrations`·`schema_v0.sql` 무접촉. 시크릿 커밋 금지. Docker는 로컬만.
> **초점(성재 2026-09-07 — 셀프호스팅·그록봇 연동) 무접촉 목록**: `scripts/verify_agent_port*.sh` · `verify_hosted_*` · `verify_display_attach.sh` · `verify_agent_credentials_rust.sh` · `verify_openapi_contract_rust.sh`(+`openapi_sampled_on_rust.txt`·`openapi_shape_check.py`·`openapi_spec_to_json.sh`) · `scripts/oort` · `scripts/lib/**` · `scripts/self_host_*` · `verify_policy_integrity*` · `check_track_alignment.sh` · `verify_merge_tree.sh` · `github_*`·`goal_*` · `check_release_manifest.sh` · `verify_pgbackrest_*` · `run_pitr_gated_migrate.sh`.
> 근거: ADR-0183 D3·D4·D5·「영향·게이트」 · 인벤토리 §4(스크립트 배선)·§7(게이트가 붙드는 것)·§8(라이브 의존) · 후보 §1 #5·#9·#10·#18·#20 · #2124(SH 시험 3본 편입).

## 구현 계약
1. **삭제(`git rm`)** — 인벤토리 §4의 「Swift 전용 60본」 전부 + 다음: `reclaim_worktrees.sh` · `worktree_janitor.sh` · `agent_host_local.sh` · `scripts/spikes/**`(10) · `scripts/transcription/**`(8) · `tests/test_bench_onboarding_aggregate.sh` · `verify_onboarding_greeting.sh` · `verify_workd_reconcile.sh` · `verify_workspace_create.sh` · `verify_workspace_rest_create.sh` · `verify_provider_cascade.sh` · `verify_quota_snapshot.sh` · `verify_t3_interval_precision.sh` · `verify_usage_summary.sh` · `verify_momo_channel_adapter.sh` · `verify_run_routing.sh` · `verify_cors_allowlist.sh` · `openapi_server_routes.py` · `verify_openapi_contract.sh`(Swift 패스 — `_rust` 유지) · `make_deploy_bundle.sh` + `tests/test_make_deploy_bundle.sh` · `aws_internal_alpha_preflight.sh` · `prod_env_preflight.sh` · `mock_codex_app_server.py` · `mock_push_relay.py` · `push_relay_keygen.sh` · `fixtures/ios-push-sample.apns` · `tests/test_push_relay_vocabulary_contract.py`. 삭제 뒤 **어떤 실행 배선(scripts·.github·Makefile·infra/rust·server-rust·adapters·web gates·package.json)도 참조하지 않게 된 mock·fixture**(`mock_hermes.py`·`mock_acp_agent.py`·`mock_provider_hop.py`·`fixtures/**` 등)는 함께 삭제하되, `infra/rust`·`server-rust`·Agent Port 검증기가 참조하면 보존. `verify_webhook_rust.sh`·`verify_push_kit_inheritance.sh`·`tests/test_release_manifest.sh`는 **보존**(Rust/폰 킷/살아 있는 게이트의 미배선 시험) — `test_release_manifest.sh`는 docs 프로파일에 편입.
2. **`local_gate.sh` 프로파일 규칙**: `add_swift_commands` 함수·호출 전부 삭제(`grep -c add_swift_commands` = 0). `swift` 프로파일 삭제. 나머지 프로파일(`diagnostics|staging-smoke|host-runtime|backup|local-alpha|internal-alpha|runtime-db|runtime-relay|runtime-live|runtime-agent|external-agent-provider|m3-dbc|web-serving|license|secrets`)은 **각 명령이 삭제 후에도 존재하고 `infra/rust`·`server-rust`·정적 검사만 참조하면 유지, 아니면 명령을 지우고 명령이 0이 된 프로파일은 삭제**. `--help` 목록·case 라벨·`--auto` 지도(`auto_select_profile`의 경로→프로파일 매핑, 「web 프로파일이 web-legacy를 겨눈다」 주석 포함)·`docs/LOCAL_PR_GATE.md` 프로파일 표 네 곳을 같은 커밋에서 갱신. 정적 블록: 삭제 스크립트의 `bash -n`·`py_compile` 항목 제거, `infra/prod/centrifugo.prod.json`·`infra/prod/docker/internal-smoke-migrate.sh`·AWS internal alpha preflight 항목 제거, `verify_eve_profile` 항목 제거(examples/eve는 LS-3 삭제 — 게이트 참조를 먼저 끊는다).
3. **`web` 프로파일 재조준**: `clients/web`에서 `npm ci --no-audit --no-fund` → `npm run lint` → `npm run typecheck` → `npm run test` → `npm run build`. `verify_web_generated_types`·`verify_web_serving`·`verify_web_login_smoke`·`web-serving` 프로파일 삭제. 라이선스 게이트는 `check_npm_licenses.mjs` 기본 루트(현행 트리)만.
4. **CI**: `pr-ci.yml`의 「OpenAPI ↔ web-legacy generated contract」 job과 그 경로 필터(`clients/web-legacy/`·`verify_web_generated_types.sh`) 삭제, 「PR CI gate」의 필수 레인 목록 정합. `dependabot.yml`에서 `clients/web-legacy`·`examples/*` 항목 삭제. `scripts/tests/test_pr_ci_guardrails.sh`를 새 계약으로 갱신(레인 부재를 단정 — 레인이 되살아나면 붉어져야 한다).
5. **부트스트랩 SQL 이전**: `git mv infra/prod/{bootstrap_runtime_roles,set_initial_owner,bootstrap_owner_if_absent,bootstrap_owner_claim_if_absent}.sql infra/rust/sql/` → `server-rust/Dockerfile` COPY 4행 · `bins/momo-migrate/src/main.rs` 경로 문자열 전부(12곳) · `Dockerfile.dockerignore` · `scripts/tests/test_publish_images_contract.py` · `infra/rust/README.md`·`docker-compose.push.yml` 주석 갱신. `cargo test -p momo-migrate` 초록 + `docker build -f server-rust/Dockerfile --target <런타임 스테이지> .`가 COPY 단계를 통과(전체 빌드가 무거우면 `--target`으로 SQL COPY 스테이지까지만; 로그 첨부).
6. **개명**: `verify_ncp_centrifugo_contract.sh`·`verify_ncp_centrifugo_boundary.sh`·`tests/test_ncp_centrifugo_boundary.sh` → `verify_public_edge_centrifugo_contract.sh`·`verify_public_edge_centrifugo_boundary.sh`·`tests/test_public_edge_centrifugo_boundary.sh`(`git mv`, 본문의 자기 이름·NCP 문면만 치환, 검사 논리·픽스처 불변). `check_compose_env_templates.sh` 목록에서 `infra/prod/*` 3행과 `ncp-rust-deploy` 행 삭제(infra/rust 행 유지).
7. **문서 게이트**: `check_docs_commands.py` GATED_DOCS에서 `CODEX.md`·`docs/RUN.md` 제거 + `tests/test_docs_commands_gate.sh` 픽스처 정합. `gate_oort_user_facing.sh` 스캔 루트에서 Swift 5트리·`clients/web-legacy/index.html` 검사 제거(현행 트리 검사는 불변). SH 시험 3본(`tests/test_oort_day2.sh`·`test_oort_doctor.sh`·`test_public_edge.sh`)과 `tests/test_release_manifest.sh`를 docs 프로파일 `add_cmd_once`로 편입(#2124).
8. **Makefile**: `swift-build`·`swift-test` 삭제. `up`/`down`은 `docs/SELF_HOST.md`의 로컬 빌드 모드 명령(`scripts/oort`/`self_host_env.sh` 경로)을 그대로 감싼다 — 새 compose 경로 발명 금지, 못 감싸면 타깃을 지우고 `help`에 SELF_HOST 절을 가리킨다. `COMPOSE_FILE` 기본값이 `infra/docker-compose.yml`을 가리키면 그 참조도 제거.

## red proof (선행 커밋 + 계수)
- 삭제 전/후 계수 표를 PR 본문에: `git ls-files scripts | wc -l`(265 → 실측) · `grep -c add_swift_commands scripts/local_gate.sh`(→0) · `git grep -lE 'web-legacy|server/Sources|infra/prod|swift:6' -- scripts .github Makefile server-rust`(→ 빈 목록; `infra/rust/sql` 새 경로는 제외).
- `scripts/tests/*.sh` 전부 + `python3 scripts/tests/*.py` 전부 초록(원문 첨부). `test_pr_ci_guardrails.sh`는 web-legacy job을 되살린 스크래치 사본에서 **붉어지는지** 증명(사보타주 1회).
- `scripts/local_gate.sh --profile docs` PASS · `--profile web` PASS(clients/web 5단계 원문) · `--help` 출력 = 실재 프로파일.
- `cargo test -p momo-migrate` 초록 · Docker COPY 스테이지 통과 로그.

## 완료 절차
커밋 순서: ①SQL 이전+Rust 경로(시험 초록) ②CI 레인·guardrails ③local_gate 프로파일·정적 블록·web 재조준 ④삭제·개명 ⑤문서 게이트·Makefile·LOCAL_PR_GATE. → `git push -u origin feat/ls0-gate-rewire` → PR(base `track/engine`) 본문에 계수 표·게이트 원문·삭제 목록·프로파일 전/후 표·「보호 경로 변경 파일 목록」(감사용). 마지막 출력: `DONE / COMMITS / GATES / PR / NOTES(계획 이탈·보존 판단한 mock 목록)`.

## 규율
숫자로 잰다(계수·grep 0·시험 원문). 삭제는 `git rm`(파일을 비우거나 주석 처리 금지). 프로파일 하나를 지우면 `--help`·case·`--auto`·LOCAL_PR_GATE 네 곳을 같은 커밋에서. 게이트 논리를 「초록으로 만들려고」 완화하지 않는다 — 검사 대상이 사라진 항목만 제거. 막히면 우회 말고 NOTES에 보고 후 정지. 무접촉 목록의 파일이 diff에 나타나면 그 자체가 결함.
