# goal B2.6 — AgentGateway: LLM-run 표면 + usage_ledger 과금 (마지막 큰 T3 표면)

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(B2.5 랜딩 `6767c0fb` 이후). 워크트리 `~/projects/momo-tracks/momo-worktrees/B26-gateway`(브랜치 `feat/B26-gateway`, 생성됨).
근거: Swift 실측 `server/Sources/MomoServer/Routes/AgentGatewayRoutes.swift`(2405 — **계약 실측이 이 배치의 절반**)·`AgentRunRoutes.swift`·`usage_ledger` 스키마(001·041)·기존 crate 공개 API.

## 0. 규율 (기존 동일)
1. `.env` 열람 금지. 2. **PR 후 STOP**, amend/force-push 금지. 3. docker 검증 금지(`#[ignore]` 작성만). 4. Swift·스키마 실측이 정답. 5. route raw SQL 0·outbox는 emit_outbox·정산 t3_terminate만·Centrifugo 호출 0. 6. 새 마이그레이션 금지. 7. 허용 표면: momo-server route·momo-t3(또는 신규 모듈) 공개 API 추가(명시).

## 1. 목표 (스코프 = 과금 척추, 전체 2.4k 이식 아님)
agent run의 **생성→이벤트 수신→usage_ledger 계상→완료** 척추 + gateway 인증. 목적: LLM-run 과금(usage_ledger — B2.2에서 "usage/summary가 이걸 집계"라 확인된 그 원장)이 Rust에서 성립.

## 2. 할 일
- **gateway 인증**: Swift 실측(AGENT_GATEWAY_SECRET 계열·agent bearer — AuthMiddleware의 agent 경로) 파리티. legacy secret 게이트는 Swift와 동일 env 의미.
- **agent run 수명주기**: run 생성(trigger message 결속·멱등 — 001:272-282 실측)·상태 전이·완료. `agent_job` outbox 연동은 emit_outbox 경유(kind=AgentJob).
- **usage_ledger 계상**: 이벤트(모델·토큰) 수신→불변 행 append(041 effort 제약 실측). **usage/summary GET**(B2.2에서 이관된 그 라우트 — 이제 writer가 생기므로) 포함.
- **conformance `#[ignore]`**: ①run 생성→이벤트→ledger 행·summary 집계 일치 ②멱등(같은 trigger 재호출 시 live run 1) ③비인가 gateway 요청 거부 ④무회귀 전제(공유 DB).

## 3. 하지 말 것
스트리밍/실시간 중계·ACP 어댑터(workd/B5)·mention 라우팅 의미(메신저 breadth)·provenance 확장·새 마이그레이션.

## 4. 검증·PR
cargo check/test/fmt/clippy·구조 grep. PR `feat/B26-gateway` → `track/engine`. 본문: 계약 파리티 표·ledger 계상 규칙 실측 근거·red 절차·오케스트레이터 목록·이탈. **PR 후 STOP.**
