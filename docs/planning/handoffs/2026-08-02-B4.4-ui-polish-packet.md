# goal B4.4 — 웹 화면 폴리시 + oort 브랜딩 (도그푸딩 첫인상)

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서.
**base = `track/engine`**. 워크트리 `~/projects/momo-tracks/momo-worktrees/B44-ui`(브랜치 `feat/B44-ui-polish`, 생성됨).
**트랙 예외**: 웹 UI는 UXUI 소유이나 이 배치는 **성재 직접 지시**(2026-08-02 "화면도 좀 잘 만들어봐")로 진행 — ENGINE_HANDOFF에 통지 기록. 배포 전 design-review(Blocker 0)는 오케스트레이터가 실행.

## 0. 규율
`.env` 금지 · **PR 후 STOP**(amend/force-push 금지) · **서버 코드(`server-rust/`·`server/`) 무접촉** · 대상은 `clients/web/**`만 · 새 의존 최소(있으면 근거) · `~/.claude/skills/momo-design-taste/SKILL.md` 정독 후 그 원칙(위계·여백·대비·상태 표현) 준수 · ux-bible P1~P15 참조.

## 1. 브랜딩
- 제품명 표기: **oort** (momo→oort 리브랜딩, 도메인 app.oor7.com). 코드 식별자·API는 무변경 — **사용자 가시 문자열·타이틀·로고만**.
- 로고: `scratchpad 경로의 oort-logo-test.png` 컨셉(O+궤도+말풍선 꼬리) 참고. **네가 codex로 변형 생성 가능**:
  `cd <에셋 디렉터리> && codex exec --skip-git-repo-check --sandbox workspace-write "<프롬프트>" </dev/null`
  — 필요 변형: 앱 로고(투명배경)·favicon(단순화)·로그인 히어로. 생성물은 `clients/web/public/`에 최적화(크기 절제 — 각 200KB 이하 권장, favicon은 SVG나 32px PNG).
- 다크 기조(로고 컨셉과 정합) 권장 — 단 taste 스킬 원칙이 우선.

## 2. 화면 폴리시 (도그푸딩 첫인상 순)
1. **로그인 화면** — 현재 상태 실측 후 브랜딩·위계·오류 표현 개선.
2. **앱 셸** — 사이드바(채널·DM 구획, unread 뱃지 가독), 헤더, 채널 뷰 여백·타이포.
3. **빈 상태들** — 채널 없음·메시지 없음·검색 결과 없음 (일러스트는 codex 생성 가능하되 절제).
4. 로딩·오류 상태 일관화.
- **기능 변경 금지** — 시각·카피·레이아웃만. 라우팅·상태관리 로직 무접촉(스타일 파일·컴포넌트 마크업·에셋만).

## 3. 검증·PR
- `npm run build` green + `npx tsc -b` + (있으면) lint. 스크린샷: `npm run dev`를 띄워 주요 화면 4장 캡처해 PR에 첨부(플레이라이트 등 설치가 부담이면 생략하고 오케스트레이터 캡처에 위임 — 명시).
- PR `feat/B44-ui-polish` → `track/engine`. 본문: 변경 화면 목록·에셋 출처(codex 생성 프롬프트)·taste 원칙 적용 표·이탈. **PR 후 STOP.**
