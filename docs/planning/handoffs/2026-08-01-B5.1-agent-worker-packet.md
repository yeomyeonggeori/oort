# goal B5.1 — momo-agent-worker: agent_job 소비 → LLM 호출 → 응답 작성 (티키타카의 심장)

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(join·UI 랜딩 이후). 워크트리 `~/projects/momo-tracks/momo-worktrees/B51-agent-worker`(브랜치 `feat/B51-agent-worker`, 생성됨).
근거: Swift 실측 `workers/AgentWorker/**`(AgentJobPayload·provider 호출·응답 작성 경로 — **의미 파리티의 정본**) · momo-agent(B2.6 — job을 만드는 쪽) · momo-outbox(claim 패턴) · B4.2 provider link(복호화 키 계약).

## 0. 규율 (기존 동일 + 특례 1)
`.env` 금지 · **PR 후 STOP**(amend/force-push 금지) · docker 검증 금지(`#[ignore]`만) · Swift 실측=정답 · 새 마이그레이션 금지 · Dockerfile 매니페스트 목록 갱신(신규 bin) · **특례: 이 워커 바이너리는 LLM provider로의 outbound HTTP가 본질**(Swift AgentWorker 동일) — 단 momo-server에는 여전히 HTTP 0, Centrifugo publish는 relay만(응답은 message 쓰기경로 → outbox → relay가 나름).

## 1. 할 일 — `server-rust/bins/momo-agent-worker`
- **job 소비**: outbox `kind='agent_job'` claim(momo-outbox에 공개 API 추가 — broadcast claim과 대칭, per-agent 직렬화=partition_key. relay가 안 집는 것 재확인) → `momo_worker` 롤(BYPASSRLS, bootstrap_roles 실측).
- **provider 호출**: job payload의 agent·provider link 해석 → `PROVIDER_LINK_MASTER_KEY` 복호화(Swift ProviderLinkCrypto 실측 — momo-wire/settings 어디 소유인지 판단·명시) → LLM API 호출(Swift가 지원하는 provider 계약 실측 — 최소 1 provider(anthropic 계열) + **mock provider**(테스트용, 결정적 응답)).
- **응답 작성**: 결과를 **momo-messaging 쓰기경로로**(send_message — 에이전트=member로서, 척추 그대로) + run 상태 전이(momo-agent API) + usage_ledger 계상(momo-agent API — B2.6 계약).
- **실패 처리**: provider 오류→run failed+사용자 가시 오류 메시지(Swift 파리티), 백오프·재시도 한도.
- **conformance `#[ignore]`**: ①mock provider로 e2e — job 생성→worker 1 iteration→**에이전트 응답 message가 채널에 seq 달고 존재**→run 완료→ledger 행 ②실패 경로(mock 500→failed+오류 메시지) ③per-agent 직렬화(같은 agent 2 job 동시→순차) ④시크릿 비유출(로그에 복호화된 링크 값 0).

## 2. 하지 말 것
멘션→run 라우팅(B5.2)·스트리밍·에이전트 허브 UI·ACP/코딩 에이전트(workd 몫)·새 마이그레이션.

## 3. 검증·PR
cargo check/test/fmt/clippy·구조 grep(momo-server HTTP 여전히 0·outbox SQL momo-outbox만). PR `feat/B51-agent-worker` → `track/engine`. 본문: Swift 파리티 표·provider 계약 실측 근거·red 절차·오케스트레이터 목록·이탈. **PR 후 STOP.**
