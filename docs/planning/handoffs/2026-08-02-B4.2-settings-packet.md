# goal B4.2 — 설정 표면 나머지 완결 (D-3, diff 매트릭스 18쌍)

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(B4.1 랜딩 `2817574f` + Dockerfile 수정 이후). 워크트리 `~/projects/momo-tracks/momo-worktrees/B42-settings`(브랜치 `feat/B42-settings`, 생성됨).
근거 정본: `docs/planning/2026-08-01-b4-contract-diff.md` §9(재분류판 — **설정 관련 미구현 쌍 전수가 이 배치 스코프**) + Swift route 실측.

## 0. 규율 (기존 동일)
`.env` 금지 · **PR 후 STOP**(amend/force-push 금지) · docker 검증 금지(`#[ignore]`만) · Swift 계약=정답 · UI 파일 수정 금지(ENGINE_HANDOFF 기록만) · route raw SQL 0·emit_outbox·Centrifugo 0 · 새 마이그레이션 금지 · **Dockerfile 매니페스트 목록: 신규 crate 만들면 반드시 추가**(2026-08-01 배포 실측 결함).

## 1. 할 일
- diff 매트릭스에서 설정/프로필 계열 미구현 쌍 전수 실측(워크스페이스 설정·멤버 프로필·알림 설정 잔여·초대 등 — 매트릭스가 정본) → 전부 Swift 파리티로 마감.
- 매트릭스 재분류 갱신 + 남은 미구현의 성격 명시.
- **conformance `#[ignore]`**: 설정 읽기/쓰기 왕복 + RLS 0행 + 클라 시퀀스 smoke에 설정 단계 통합.

## 2. 하지 말 것
에이전트 허브(D-4/D-5)·routing 실구현·huddle/attachment·새 마이그레이션.

## 3. 검증·PR
cargo check/test/fmt/clippy·구조 grep + **`docker build` 성공 확인은 오케스트레이터 몫으로 명시**. PR `feat/B42-settings` → `track/engine`. 본문: 마감 목록·재분류 수·red 절차·오케스트레이터 목록·이탈. **PR 후 STOP.**
