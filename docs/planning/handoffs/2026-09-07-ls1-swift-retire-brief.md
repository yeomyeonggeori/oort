# 워커 브리프 — LS-1 Swift 은퇴: Swift 4트리·infra/prod·Swift e2e 컴포즈·PushRelay·workd·LinkShort·examples 삭제 + 참조 0 (engine · #2165 · ADR-0183 D4·D5 Accepted, 정책 감사 대상)

> 워커: grok 4.6 · base=origin/track/engine · 워크트리 `momo-worktrees/wls1`(`feat/ls1-swift-retire`) · 시작 절차: `git merge origin/main --no-edit`
> 정지 조건: 머지·이슈 close 금지. **보호 경로 허용 목록(정책 감사 대상)**: `scripts/local_gate.sh` · `scripts/check_compose_env_templates.sh` · `scripts/gate_oort_user_facing.sh` · `scripts/migrate.sh` · `scripts/ensure_runtime_env.sh` · `scripts/collect_diagnostics.sh` · `scripts/internal_alpha_stack.sh` · `scripts/local_alpha_runner.sh` · `scripts/local_soak_monitor.sh` · `scripts/tests/**` · §1-B의 삭제 대상 · §1-C의 주석 정정 대상 · `.github/workflows/pr-ci.yml`(경로 필터·주석만) · `AGENTS.md` · `docs/TRACKS.md`. 그 밖의 `scripts/**`·`.github/**` 무수정. `server/Migrations/**`·`server/Fixtures/**`(웹 시험이 소비)·`schema_v0.sql` 무접촉. Docker 로컬만. 시크릿 금지.
> **초점 무접촉 목록**(셀프호스팅·그록봇 연동): `scripts/verify_agent_port*.sh` · `verify_hosted_*` · `verify_display_attach.sh` · `verify_agent_credentials_rust.sh` · `verify_openapi_contract_rust.sh`(주석 정정만) · `scripts/oort`·`scripts/lib`·`scripts/self_host_*` · `infra/rust/docker-compose.rust.yml`·`local.override.yml`·`caddy.override.yml`·`Caddyfile*`(SQL 경로 갱신 외 무수정) · `docs/SELF_HOST*.md`·`SELF_HOST_AGENT*.md`(workd·PushRelay 문장 정정 외 무수정) · `infra/cubesandbox/**` · `server-rust/crates/momo-t3/**` · 웹 work 표면(LS-2).
> LS-0 결과(2026-09-07): scripts 265→167, `web` 프로파일=clients/web, GATED_DOCS에서 CODEX/RUN 제외, `run_cmd`가 호출자 PATH 재적용, `worktree_janitor.sh` 복원(런북 구동), 정책 파일 변경은 track PR·승격 PR·sync PR 셋 다 감사.
> 근거: ADR-0183 D4(결정 ①②) · D5 · 부록 A · 인벤토리 §8(라이브 의존) · 후보 §1 #1·#2·#3·#9·#10·#15·#17·#19 · 2026-08-09 Swift 삭제 감사 §2.

## 1. 구현 계약
**A. 삭제(`git rm`, 비보호)**: `server/Sources` · `server/Tests` · `server/Package.swift` · `server/README.md` · `server/.gitkeep` · `workers/**` · `relay/**` · `services/**` · `.swift-version` · `infra/prod/**`(SQL 4본은 LS-0이 `infra/rust/sql/`로 이전했음을 확인) · `infra/docker-compose.yml` · `infra/docker-compose.e2e.yml` · `infra/e2e/**`(단 `bootstrap_roles.sql`은 `infra/rust/sql/`로 `git mv` — `server-rust/Dockerfile:220`·`momo-migrate`·`infra/rust/docker-compose.rust.yml`·`docker-compose.push.yml`·`infra/rust/README.md` 경로 갱신) · `infra/eve/**` · `infra/workd/**` · `adapters/codex-workbench/**` · `examples/**` · `infra/rust/docker-compose.push.yml`·`docker-compose.push.build.yml`·`push-relay.env.example`(결정 ① — Rust `momo-notifier`의 relay 클라이언트 코드는 유지, env 미설정 = 릴레이 없음).
**B. 삭제(보호 — 감사)**: `scripts/verify_linkshort.sh` · `verify_push_relay.sh` · `push_relay_keygen.sh` · `verify_workd_attach.sh` · `verify_workd_rust.sh` · `verify_acp_host.sh` · `verify_eve_profile.sh` · `verify_internal_host_runtime.sh`·`verify_internal_hosting_smoke.sh`·`verify_momo_ops.sh`·`verify_momo_ops_runtime.sh`·`verify_staging_smoke.sh`·`verify_prod_rls_posture.sh`·`verify_owner_bootstrap.sh`·`verify_runtime_role_bootstrap.sh`·`verify_prod_seed_password.sh`(infra/prod 스택 전제 — `infra/rust`로 재조준 가능하면 재조준, 아니면 삭제; 판정표에) · `verify_agent_context.sh`·`verify_agent_live_channel.sh`·`verify_agent_worker.sh`·`verify_external_agent_provider.sh`·`verify_realtime_live.sh`·`verify_huddle_lifecycle.sh`는 **실행 스택을 판정**: `infra/rust` 스택으로 도는 것은 보존(주석 정정), Swift e2e 컴포즈·삭제 트리 없이는 돌 수 없는 것은 삭제. 판정표(스크립트 · 스택 · 보존/삭제 · 근거)를 PR 본문에.
**C. 주석·경로 정정(보호)**: 남는 스크립트·`local_gate.sh`·`pr-ci.yml`·`Makefile`·`server-rust/Dockerfile`·`Dockerfile.dockerignore`에서 `server/Sources|workers/|relay/|services/|infra/prod|docker-compose.e2e|swift:6` 언급을 「`f399e417:<path>`」 고정 참조 또는 삭제로 바꾼다. `local_gate.sh`에서 삭제 스크립트의 `bash -n`·프로파일 항목 제거, `check_compose_env_templates.sh`의 push 행 2개 삭제, `gate_oort_user_facing.sh` 잔여. `pr-ci.yml` 경로 필터의 Swift 트리 주석·`server/Fixtures/` 항목은 **유지**(Fixtures 보존).
**D. 문서(비보호)**: `docs/runbooks/ncp-rust-deploy.md`는 **이 PR에서 삭제하지 않는다**(`verify_public_edge_centrifugo_contract.sh`가 그 런북의 회전·SHA-256·롤백 절을 단정 — 계약의 RUNBOOK을 `docs/SELF_HOST.md` 회전 절로 옮기는 것은 LS-3+SH 문서 작업). 삭제 — `docs/PUSH_RELAY_RUNBOOK.md` · `docs/cicd/12-push-relay-deploy-runbook.md` · `docs/WORK_HOST_QUICKSTART.md` · `docs/AGENT_HOSTING_QUICKSTART.md` · `docs/BYOC_CLOUD_HOST.md` · `docs/runbooks/workd-rust-session.md`·`workd-terminal-attach.md`. 정정 — `docs/INDEX.md` §0·§6(코드 디렉터리 표를 D1 정본 목록으로) · `docs/architecture/overview.md`·`server-rust.md`(workd·PushRelay·codex-workbench·LinkShort 문장 → 「은퇴, git 히스토리」 1줄) · `infra/rust/README.md` · `docs/cicd/10-ios-signing-identity-runbook.md`(PushRelay 절) · `docs/SELF_HOST_FIRST_DAY(.ko).md`·`SELF_HOST_AGENT(.ko).md`의 workd·PushRelay 문장 1줄 정정 · `docs/TRACKS.md` §1 표에서 「은퇴 중」 열 삭제 · `AGENTS.md`의 docker-compose.e2e·LinkShort·workd·PushRelay 문장.
**E. LinkShort**: `git grep -il linkshort -- server-rust clients packages infra/rust` 결과를 PR에. 서버·클라가 링크를 생성하지 않으면 대체 없이 삭제(ADR-0121 S-4 v0 초대 단축은 셀프호스트 범위 밖). 생성 코드가 있으면 그 경로를 보고 후 **정지**(planner 판정).
**F. 부록 A 실측**: 삭제 직전 `server/Sources/MomoServer/Routes`에서 `/v1` 라우트 전수(메서드+경로) 추출 → Rust 라우트(`server-rust/bins/momo-server/src/routes`)와 위치 기준 대조 → Swift-only 패밀리 표(패밀리 · 경로 수 · Rust 존재 여부)를 PR 본문과 `docs/adr/0183-codebase-docs-lightening.md` 부록 A(이 표만 편집 허용)에 기록.

## 2. red proof (선행 커밋 + 계수)
- 계수 표: 추적 파일 수·`.swift` 파일 수(224→0)·`scripts/` 수·`infra/` 수 전/후.
- `git grep -lE 'server/Sources|workers/|relay/|services/|infra/prod|docker-compose\.e2e|swift:6|\.swift\b' -- . ':!docs/adr' ':!docs/planning/JOURNAL.md' ':!docs/planning/archive' ':!STATUS.md' ':!docs/planning/research' ':!docs/planning/handoffs'` → **빈 목록**(`f399e417:` 고정 참조 제외).
- `make build`·`make test`(Rust+TS) 초록 · `docker build -f server-rust/Dockerfile .` 완주(SQL COPY 경로 포함) · `scripts/local_gate.sh --profile docs`·`--profile web` PASS · `scripts/tests/*` 전부 초록 · 셀프호스트 로컬 빌드 모드 `scripts/oort doctor --json` PASS 1회(릴레이 없음 상태에서 outbox 오라클 비실패 확인 — SH-3b 규칙).
- 사보타주: `check_compose_env_templates.sh`에서 삭제한 push 행을 스크래치에서 되살려 게이트가 「파일 없음」으로 붉어지는지 1회.

## 3. 완료 절차
커밋 순서: ①SQL 이전(`infra/e2e/bootstrap_roles.sql`)+Rust 경로 ②보호 스크립트 판정·삭제·주석 ③트리 삭제(A) ④문서(D)·TRACKS·AGENTS ⑤부록 A. → push `feat/ls1-swift-retire` → PR(base `track/engine`): 계수 표 · grep 0 · 판정표(B) · LinkShort 조사(E) · 부록 A 표 · **보호 경로 변경 파일 목록**(감사용). 마지막 출력 `DONE / COMMITS / GATES / PR / NOTES`.

## 4. 규율
숫자로 잰다. 삭제는 `git rm`. 게이트 논리 완화 금지. 무접촉 목록의 파일이 diff에 나타나면 결함. 막히면 우회 말고 보고 후 정지(E의 정지 조건 포함).
