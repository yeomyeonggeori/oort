# goal B5.2 — 멘션→run 라우팅 + 에이전트 생성/초대 표면 (티키타카 배선)

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(B5.1 랜딩 `31ccf18e` 이후). 워크트리 `~/projects/momo-tracks/momo-worktrees/B52-mention`(브랜치 `feat/B52-mention`, 생성됨).
근거: Swift 실측 `MessageRoutes.swift`의 `routeAgentMentions`(B1.2가 이연한 그 함수 — 호출부 3곳: send·AgentGatewayRoutes:924·OnboardingGreeting:154) · `AgentRoutes.swift`·`AgentProfileRoutes.swift`(에이전트 생성/초대) · momo-agent(run 생성 API — B2.6)·momo-agent-worker(B5.1 소비자).

## 0. 규율 (기존 동일)
`.env` 금지 · **PR 후 STOP**(amend/force-push 금지) · docker 검증 금지(`#[ignore]`만) · Swift 실측=정답 · route raw SQL 0·정산/전이표 규율 유지 · 새 마이그레이션 금지 · Dockerfile 목록(신규 crate 시).

## 1. 할 일
- **멘션→run 라우팅**: send 경로에서 `mention_member_ids` 중 agent member를 골라 **run 생성+agent_job emit**(momo-agent API — B2.6의 생성 경로 재사용, 트리거 메시지 결속·멱등). Swift 의미 파리티(동일 메시지 재전송 시 이중 run 0 — 001:282 멱등 근거). B1.2의 mention 합성 지점(insert와 finish 사이)과 정합하게.
- **에이전트 생성/초대 표면**: 워크스페이스에 에이전트 만들기(AgentRoutes 실측 — kind='agent' member 생성·프로필·표시명). 도그푸딩에서 "에이전트 초대"가 이 표면이다. 프로필 조회 GET(허브 UI가 소비할 최소).
- **conformance `#[ignore]`**: ①@agent 멘션 send→run+job 생성→(B5.1 워커 1 iteration)→**응답 티키타카 e2e** ②재전송 멱등(run 1) ③비-agent 멘션은 run 0 ④에이전트 생성→roster에 kind=agent로 등장→멘션 가능.

## 2. 하지 말 것
허브 UI(B5.3)·routing 선택기 실구현(B5.3)·스트리밍·OnboardingGreeting(후속)·새 마이그레이션.

## 3. 검증·PR
cargo check/test/fmt/clippy·구조 grep. PR `feat/B52-mention` → `track/engine`. 본문: 파리티 표·멱등 근거·red 절차·오케스트레이터 목록·이탈. **PR 후 STOP.**
