# goal B5.4c — Responses SSE 스트리밍 소비 (실서버 실측: "Stream must be set to true")

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(진단 패치 포함 HEAD). 워크트리 `~/projects/momo-tracks/momo-worktrees/B54c-sse`(브랜치 `feat/B54c-sse`, 생성됨).
실측 배경: NCP 실 smoke에서 ChatGPT 백엔드가 ①모델명(gpt-5.6-sol OK) ②인증(우리 요청 수용) 확인됐고, 남은 거절은 **"Stream must be set to true"** 하나 — Responses SSE가 유일 와이어다.

## 0. 규율 (기존 동일)
자격증명 값 열람 금지 · **PR 후 STOP** · docker 검증 금지(`#[ignore]`만) · 실 OpenAI 호출 금지(mock SSE) · momo-server HTTP 0 · 새 마이그레이션 금지.

## 1. 할 일
- **Responses 어댑터 SSE 모드**: `stream:true` 전송, SSE 이벤트 소비(`response.output_text.delta` 누적 → `response.completed`에서 usage 추출 — 이벤트명은 `@openai/codex` 0.144.1·openai-python 실측). 최종 텍스트·usage를 기존 반환 계약(TurnOutcome)에 그대로 — **다운스트림(척추 쓰기·ledger) 무변경**(스트리밍은 provider 왕복 내부 구현).
- kind=oauth-openai일 때 SSE 기본. legacy bearer(chat/completions) 무변경.
- **겸사 정리**: HEAD의 진단 패치(HttpStatus(u16,String))가 남긴 clippy 경고 7건 해소(fmt 포함).
- **conformance `#[ignore]`**: mock SSE 서버로 ①delta 누적=최종 본문 일치 ②completed usage→ledger ③스트림 중단(연결 끊김)→재시도/실패 처리 ④비스트림 400("Stream must be set to true" mock)→SSE 재시도 아닌 처음부터 SSE.

## 2. 하지 말 것
클라이언트로의 실시간 중계(스트리밍 UI — 후속·outbox 계약 그대로 최종본 1회)·tool call·momo-server 변경.

## 3. 검증·PR
cargo check/test/fmt/clippy(-D warnings — 기존 7건 포함 0으로). PR `feat/B54c-sse` → `track/engine`. 본문: SSE 이벤트명 실측 근거·red 절차·이탈. **PR 후 STOP.**
