# Handoff — PLN-20260714-02 Superapp Engine

> From: planning lane `PLN-20260714-02`
> To: `momo-main`
> Branch: `docs/pln-20260714-02-superapp-engine`
> Base: `348c79a1e2db`
> Status: momo-main architecture/security review reflected; owner decisions required before implementation

## 1. 읽을 문서

1. Gap audit: `research/14-superapp-engine/00-pln-20260714-02-gap-audit.md`
2. Roadmap + issue drafts: `docs/planning/proposals/2026-07-14-superapp-engine-roadmap.md`

## 2. 핵심 판단

- Work 카드와 server run/approval ledger는 구현되어 있다.
- codex-workbench adapter는 repo-local mock 8개가 green이지만 실제 Codex/momo end-to-end는 미검증이다.
- 현재 workspace-write 승인은 Codex native approval relay가 아니라 “read-only plan → momo coarse approval → workspace-write resume” 합성 경로다.
- 실제 interactive Work에는 user-owned host의 local `codex app-server` stdio bridge를 ADR 후보로 권고한다. `codex exec`는 read-only batch/fallback로 유지할 수 있다.
- Context Broker는 partial, Memory Plane/Capability Cache는 spec-only, inbound MCP는 runtime stub, plugin은 spec-only, webhook은 placeholder, GWS는 research spec-only다.
- superapp data critical path는 ADR-0113/0116 → Capability/Memory foundation → Context Broker → Plugin registry → GWS Drive read다.
- Codex Work real-runtime path는 별도 ADR-0114 → app-server bridge → `E-WORK-1`이며 GWS/webhook에 종속되지 않는다.
- GWS write는 첫 slice에서 제외하고 common approval-bound executor가 증명된 뒤 연다.

## 3. 절대 보안 경계

- momo가 upstream Codex/OpenAI OAuth token, refresh token, API key를 보관·proxy·log하는 설계 금지.
- Codex auth는 user-owned execution host에만 둔다.
- `MOMO_AGENT_TOKEN`은 momo control-plane bearer다.
- Hermes-facing bearer는 별도 runtime secret이며 packet/manifest/message/audit에 넣지 않는다.
- Google Workspace token custody는 Accepted ADR 전 구현 금지.

## 4. ADR 후보

1. ADR-0113 Connector Credential, Capability Grant, and External Action Boundary
2. ADR-0114 Codex Work Host Runtime and Native Approval Bridge (ADR-0102/0111 amendment)
3. ADR-0115 Signed Webhook Ingress and Rotation
4. ADR-0116 Context/Memory Persistence and Retention

상세 결정 항목과 권고안은 proposal §3에 있다.

## 5. Issue 후보 — 번호 배정 전

| Draft | 제목 | 주요 deps |
|---|---|---|
| SE-02A | Capability registry/cache minimum runtime | ADR-0113 |
| SE-02B | Typed Memory/SourceRef minimum runtime | ADR-0116 |
| SE-02C | Governed external action envelope/executor | ADR-0113, SE-02A |
| SE-01 | Context Broker + immutable Context Packet runtime | ADR-0113/0116, SE-02A/02B |
| SE-03A | Inbound MCP auth/session issuance | ADR-0113, SE-01/02A |
| SE-03B | Inbound MCP read tools | SE-03A, SE-01/02B |
| SE-03C | Inbound MCP post/action proposal tools | SE-03A, SE-02C |
| SE-04A/04B | Plugin registry, then signed webhook reference | ADR-0113/0115, SE-02A |
| SE-05A/05B | Codex app-server bridge, then E-WORK-1 real gate | ADR-0114 |
| SE-06A/06B/06C | Drive read runtime, real GWS gate, then citation UX | ADR-0113/0116, SE-02A/02B/04A |

각 이슈의 Goal/Context/Acceptance/Out of scope와 security/performance/runtime gate는 proposal §4~6에 있다.

기존 candidate 처분은 proposal §2.2 표를 따른다. `MOMO-307`은 강화해 유지하고 `MOMO-308`은 non-claimable umbrella로 전환한다. SE-03A/B/C는 새 ID를 받는다. `MOMO-310`은 advanced RAG로 보류하고, `MOMO-320`은 완료된 env drift guard 전용이며 Drive 의미를 제거한다. `MOMO-321/322`는 후속 archive/wiki 후보로 동결한다.

## 6. momo-main 요청 사항

1. 이 lane completion을 `docs/planning/CURRENT_STATE.md`와 planning journal에 canonical 반영한다.
2. engine planner가 ADR-0113~0116 draft를 작성하고, `momo-main`은 canonical 통합과 상태 전환을 맡는다. Accepted 전에는 security-boundary implementation issue를 `ready`로 승격하지 않는다.
3. 기존 backlog candidate와 분할된 SE draft를 proposal §2.2대로 reconcile한다.
4. engine PR은 기본적으로 `clients/macOS`를 수정하지 않는다. UX-owned 잠금 파일과 예외 merge owner는 proposal §7을 따른다.
5. `E-WORK-1` host는 성재의 user-owned macOS, credential 동작은 성재, 실행은 engine runtime worker, evidence 검수/merge는 `momo-main`으로 고정한다.
6. 절대 latency를 hard gate로 쓰기 전 PERF-HARNESS issue를 별도로 발급한다.

## 7. 미확정 질문

- catalog source: 권고는 core DB first, signed external index는 후속
- webhook signature: 권고는 per-install HMAC-SHA256 + secret reference/rotation, asymmetric는 후속
- Context Packet/Memory retention and legal hold
- Memory는 권한/retention 증명을 위한 keyword/metadata first 권고, pgvector/RRF는 MOMO-310
- app-server version pin/fallback window and unpriced Codex run policy
- GWS first path: per-user `drive.file` vs enterprise shared-drive service account
- inbound webhook만 v0로 할지 outbound도 같은 trust ADR에 포함할지

## 8. momo-main review disposition

- security review High 7/Medium 2와 architecture review High 6/Medium 3을 반영했다.
- source body의 Capability Cache 저장 오류를 금지로 정정했고 BYPASSRLS allowlist 확대를 금지했다.
- common action executor, typed Codex RPC allowlist/approval identity, webhook atomic receipt, GWS cursor/checkpoint 계약을 추가했다.
- 너무 큰 SE-02/04/05/06을 one-issue/one-PR 단위로 분할하고 Work real gate를 GWS critical path에서 분리했다.
- current local Codex CLI 0.144.1의 experimental `app-server --stdio`를 실측했으며 실제 authenticated runtime은 여전히 `runtime-unverified`다.

## 9. 검증과 변경 범위

- 실행: codex-workbench repo-local tests 8개 PASS.
- 실행: planner 원본 docs gate PASS 후 momo-main 최종 review diff에서도 dirty docs gate PASS(`run_id=20260714T145941Z-pid33813-ns1784041181992158000-wtc32931bd803d-r05b2e1251fbd`). final clean commit gate와 main post-merge gate는 PR evidence에 기록한다.
- 실제 Codex/GWS/provider runtime: 실행하지 않았으며 `runtime-unverified`다.
- planner 원본은 audit/proposal/handoff 3개뿐이었다. momo-main 통합은 ROADMAP/BACKLOG/STATUS/redesign tracker/CURRENT_STATE/JOURNAL과 GitHub Issue #383을 추가로 갱신했다.
- source code와 schema는 변경하지 않았다. 구현 issue/ADR Accepted 전환은 하지 않았다.
