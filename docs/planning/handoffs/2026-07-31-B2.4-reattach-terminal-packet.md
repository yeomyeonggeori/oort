# goal B2.4 — 재부착/replay(ADR-0139) + terminal attach + audit_log 완결

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(B2.3 랜딩 `6935b2dc` 이후). 워크트리: `~/projects/momo-tracks/momo-worktrees/B24-reattach`(브랜치 `feat/B24-reattach`, 생성됨).
근거: **ADR-0139**(재부착/replay — 심장) · Swift 실측 `server/Sources/MomoServer/Routes/TerminalAttachRoutes.swift`(480)·`WorkSessionRoutes.swift`의 재부착 경로·`workers/WorkHostDaemon/.../TerminalAttachServer.swift`(계약만) · `momo-db/src/audit.rs`(B0 스텁).

## 0. 필수 규율 (기존 배치와 동일)
1. `.env`류 열람 금지. 2. **PR 후 STOP**, amend/force-push 금지. 3. docker 검증 금지(`#[ignore]` 작성만 — 오케스트레이터 실행). 4. 허용 표면: momo-server route 추가·momo-t3/momo-db 공개 API 추가(명시)·신규 파일. 기존 파일 본문 수정 금지(단 audit.rs 스텁 구현은 **이번 배치의 명시 표면**). 새 마이그레이션 금지. 5. Swift·마이그레이션 실측이 정답. 6. route raw SQL 0·정산 t3_terminate만·Centrifugo 호출 0 유지.

## 1. 할 일
### 1-1. audit_log 완결 (B0 스텁 해소 — 밀린 감사 기록의 근원)
- `momo_db::audit::write_audit` 실구현(Swift audit 기록 실측: 테이블·컬럼·호출 관례). 그동안 미뤄진 호출부(logout·sweep 후속 등)는 **이번 배치 표면에 닿는 곳만** 배선, 나머지는 목록화.
### 1-2. 재부착/replay (ADR-0139 — momo-t3 확장)
- 세션 재부착 REST(Swift 실측 경로): 살아있는 세션에 재접속 → replay 커서(seq 기반) → 이어보기. ADR-0139의 검증된 실왕복(배치 2에서 Swift로 증명) 의미 파리티.
- momo-t3에 재부착 도메인 API 추가(세션 상태 스냅샷+이벤트 replay 조회 — 실측 기반).
### 1-3. terminal attach 표면 (계약만)
- `TerminalAttachRoutes` 실측 파리티로 REST 표면(attach 토큰 발급 등). **PTY/WSS 실물 중계는 workd 몫(B5)** — 서버측 계약(토큰·권한·세션 결속)까지만.
### 1-4. conformance (`#[ignore]`)
- 재부착: 세션 생성→이벤트 N→재부착→replay가 seq 연속 반환(gap 0). red: 커서를 wall-clock으로 바꾸면 실패.
- attach 토큰: 발급→검증 왕복 + 비멤버 403/404(구분 정확).
- audit: 배선한 호출부가 audit_log 행을 남김.

## 2. 하지 말 것
AgentGateway·usage_ledger 과금(B2.5) · workd 실물 PTY(B5) · 관리형 어댑터 이식 · 새 마이그레이션.

## 3. 검증·PR
cargo check/test/fmt/clippy(-D warnings). 구조 grep 3종. PR `feat/B24-reattach` → `track/engine`. 본문: route 파리티 표·momo-t3/db 추가 API·red 절차·미배선 audit 호출부 목록·오케스트레이터 목록·이탈. **PR 후 STOP.**
