# 핸드오프 패킷 — run 터미널 동봉(#1166) + 가드·기계검사 묶음(#1170·#1141) (2워커 단발)

- status: **ready** · 기준: `origin/track/engine` 최신(#1169 머지 후) · 워커=단발 Opus 무명 · 발주 전 랜딩분 대조 완료(2026-08-08 — #1166·#1170·#1141 모두 미착수 확인) · 중간 보고 없음
- 경합 지도: W-G=server routes(messages 페이지 읽기)+openapi+클라 endedRuns 씨딩 / W-H=클라 가드·preflight 스크립트·코어 문자열 — **서로 파일 무교차**(endedRuns.ts는 W-G 전속).

## 워커 G — #1166 페이지 읽기에 run 터미널 상태 동봉
- **핵심**: #1165의 방어 렌더링(닫는 PATCH 실패 시 final:false+run 종결→같은 꼬리)이 세션-로컬(endedRuns=터미널 프레임을 본 run만) — "닫는 PATCH 실패+리로드" 이중 실패에서 반쪽 답이 완결 행세. 메시지 페이지 읽기(REST backfill) 응답에 **그 페이지의 스트리밍 메시지가 가리키는 run들의 터미널 여부**를 동봉해 리로드 후에도 판정이 성립하게.
- 설계 지침: 코어 판정 함수(streamStop.ts)는 재사용 — 와이어만 추가. 동봉 형태는 실측으로 고르되(메시지별 인라인 vs 응답 사이드카) **이미 있는 문법 우선**(발명 최소) — 페이지 응답에 사이드카 전례가 있으면 따르고 근거 기록. `momo.stream` props에 서버가 터미널을 "써넣는" 안은 기각 사유 명기 후 배제(메시지는 writer의 것 — #1152 규율).
- 경계: momo-server 라우트+openapi(선택 필드 — 하위호환 단정)+코어 소비 판정+두 클라 endedRuns 씨딩. ephemeral 경로 무접촉.
- 검증: 실DB conformance(열린 스트림+종결 run을 픽스처로 리로드 시나리오 폐곡선)·red proof ≥2(①동봉 제거 시 리로드 후 꼬리 소실 재현 ②종결 아닌 run을 동봉이 종결로 표기 불가)·코어/웹/폰 스위트+병합 트리 3종. UI 픽셀 변화 0이면 design-review 생략 가능(오케스트레이터 판단) — 꼬리 문구/스타일 불변 단정 포함.
- PR "Closes #1166"·이탈 절·STOP.

## 워커 H — #1170 이름 가드 잔여 + #1141 코어 기계 검사
- **#1170(소형 4)**: ①웹 이름-분열 tripwire를 vitest 게이트로(수동 capture:honesty 의존 탈피 — 폰과 대칭) ②폰 리터럴 가드 백틱·JSX 맨글자 커버 ③ConversationScreen.tsx ≈809·1014행 낡은 「메시지 찾기」 산문 ④SearchScreen backLabel 낭독 자투리. 화면 픽셀 변화 0(④는 낭독 라벨만).
- **#1141(설계 포함)**: preflight가 clients/web/src만 훑어 코어 사용자 가시 문구가 무검사(#1138 B2의 원인). **분리 규칙 먼저 설계** — 렌더 문자열 vs 주석/테스트/독스트링을 기계로 가르는 규칙(코어는 순수 TS라 AST 또는 명명 규약 — 실측으로 고르고 근거 기록) → preflight에 코어 스캔 편입. 기존 부채(emdash 73·raw_color 47 실측치)는 **일괄 치환 금지** — 신규 유입만 막는 기준선(base 대비) 또는 허용목록으로 단계 편입, 부채 일소는 별도 티켓 제안으로.
- 검증: preflight selftest(기존 11케이스 무회귀+코어 케이스 추가)·red proof ≥2(①코어에 고의 emdash 렌더 문자열 주입→빨강 ②주석의 같은 문자는 통과) ·웹·폰·코어 스위트+병합 트리 3종. PR "Closes #1170"(#1141은 부채 일소 잔여 시 부분 해소 명시)·이탈 절·STOP.

## 공통
무명 단발 Opus·`origin/track/engine` 새 워크트리·동결층 불변·시크릿/프로덕션 금지·Docker down -v·워크트리 보고 후 대기. 스크래치 접두 `term1166-*`/`guard1170-*`.
