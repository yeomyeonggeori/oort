# goal B4.1 — 도그푸딩 차단분 마감: roster·채널 생성·스레드·설정

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(B4 랜딩 `67f272a2` 이후). 워크트리 `~/projects/momo-tracks/momo-worktrees/B41-dogfood`(브랜치 `feat/B41-dogfood`, 생성됨).
근거 정본: **`docs/planning/2026-08-01-b4-contract-diff.md`**(68쌍 매트릭스 — 이 배치의 스코프 소스) + Swift route 실측.

## 0. 규율 (기존 동일)
`.env` 금지 · **PR 후 STOP**(amend/force-push 금지) · docker 검증 금지(`#[ignore]`만) · Swift 계약=정답·갭은 서버가 닫는다 · UI 파일 수정 금지(ENGINE_HANDOFF 기록만) · route raw SQL 0·emit_outbox·Centrifugo 0 · 새 마이그레이션 금지.

## 1. 할 일 (diff 매트릭스의 도그푸딩 시퀀스 차단분 — 우선순위 순)
1. **roster** — 멤버 목록/이름(사이드바·멘션 표시의 이름 복원. Swift RosterRoutes 실측).
2. **채널 생성** — `POST …/channels`(B4 이탈 D-7 — 판정 기준 이미 충족 확인됨).
3. **스레드** — root_id 기반 스레드 조회/전송 계약(B1 척추의 root_id를 클라 계약대로 노출. MessageRoutes thread rollup 실측 — 척추 의미 무변경).
4. **설정** — 클라 부팅 시퀀스가 요구하는 최소(notification_pref 등 diff 매트릭스 실측분만).
5. diff 매트릭스 문서 갱신(마감분 재분류) + **도그푸딩 게이트 판정**(남은 차단분 0인지 명시).
- **conformance `#[ignore]`**: 클라 시퀀스 smoke 확장(로그인→roster→채널 생성→스레드 왕복→설정 읽기) + RLS 0행.

## 2. 하지 말 것
huddle·attachment·미구현 51 중 시퀀스 비차단분·T3·새 마이그레이션.

## 3. 검증·PR
cargo check/test/fmt/clippy·구조 grep. PR `feat/B41-dogfood` → `track/engine`. 본문: 마감 목록·매트릭스 재분류 수·**도그푸딩 게이트 판정**·red 절차·오케스트레이터 목록·이탈. **PR 후 STOP.**
