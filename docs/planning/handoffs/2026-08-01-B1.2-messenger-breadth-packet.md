# goal B1.2 — 메신저 breadth: DM·read-state·mention·search (도그푸딩 1차 전제)

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(B2.6 랜딩 `214ab47e` 이후). 워크트리 `~/projects/momo-tracks/momo-worktrees/B12-breadth`(브랜치 `feat/B12-breadth`, 생성됨).
근거: Swift 실측 `server/Sources/MomoServer/Routes/{DMRoutes,ReadStateRoutes,ReadStateMentions,SearchRoutes}.swift` + `MessageRoutes`의 mention 라우팅(props·mention_member_ids) · momo-messaging crate(B1).

## 0. 규율 (기존 동일)
1. `.env` 열람 금지. 2. **PR 후 STOP**, amend/force-push 금지. 3. docker 검증 금지(`#[ignore]` 작성만). 4. Swift·스키마 실측이 정답(경로·DTO·상태코드). 5. route raw SQL 0·outbox는 emit_outbox·Centrifugo 호출 0. 6. 새 마이그레이션 금지. 7. 허용 표면: momo-messaging 모듈·momo-server route 추가(명시).

## 1. 할 일 (momo-messaging 확장 + route)
- **DM**: DM 채널 개설·조회(Swift DMRoutes 실측 — 멤버쌍 멱등 등 의미 파리티). 메시지는 기존 척추 재사용.
- **read-state**: 읽음 커서(채널×멤버, seq 기반)·unread 계산·mention 카운트(ReadStateRoutes+ReadStateMentions 실측).
- **mention 라우팅**: send 경로의 mention 파싱→props/알림 연동 의미(Swift MessageRoutes 실측 — 어디까지가 서버 몫인지 실측 후 그만큼만; push_candidate 트리거와의 관계 주의 — B1 게이트 교훈).
- **search**: 메시지 검색(pg_trgm — SearchRoutes 실측 파리티. pgvector memory plane은 제외).
- **conformance `#[ignore]`**: ①DM 개설 멱등 ②read 커서 전진·unread 정확(seq 기준 red — 커서를 시계로 바꾸면 실패) ③mention이 대상 멤버 카운트에 잡힘 ④search가 본문 히트 반환 ⑤RLS cross-tenant 0행(전 표면).

## 2. 하지 말 것
huddle·attachment(별도 배치)·memory/pgvector·클라 재배선(B4)·provenance 확장·새 마이그레이션.

## 3. 검증·PR
cargo check/test/fmt/clippy·구조 grep. PR `feat/B12-breadth` → `track/engine`. 본문: route 파리티 표·red 절차·오케스트레이터 목록·이탈. **PR 후 STOP.**
