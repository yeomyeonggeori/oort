# 파도 10 핸드오프 패킷 — 자율 큐 소탕(카피·토큰·버튼·폰 AX) + #1361 준비

> 2026-08-19 Fable 발급 · Status: `ready` · 워커: 단발 무명 Opus 5 ×4 + 준비 워커 1.
> planning ID: **PLN-20260815-01** · integrator: momo-main. 전건 이슈 본문 1차 정본.
> 기준: origin/track/engine 최신(`e3262eb8`~). fetch 후 claim `--base track/engine` 명시.
> 병렬 파일군: A(#1584+#1511)=웹 카피 문자열+코어 주석+폰 SidebarScreen 1줄+카피 게이트 · B(#1515+#1516)=디자인 토큰 정본(docs/design-system·웹 tokens/칩 표면) · C(#1558+#1559)=SaveButton 정본+busy 소비처(설정/훅 표면) · D(#1479+#1480)=폰(hangul-word·멘션 시트) · E(#1361 준비)=로컬 스택·Agent Port(레포 비변경 원칙).
> A와 C는 둘 다 clients/web을 만지나 표면이 다름(A=문자열·라벨, C=버튼 컴포넌트/설정 폼). 같은 파일 충돌 시 후발이 정지+이탈 보고.

## 공통 계약
표준 절차·merge/close 금지·ADR-0132·red proof·UI goal 프리플라이트(design_preflight_web.sh — 폰은 jest 스위트)·캡처(light/dark)·docker/시뮬레이터 회수·push 전 정렬 프리플라이트(TRACKS §3.1.1). design-review는 오케스트레이터 몫(캡처만 남겨라). 한 워커가 2 goal을 지면 **PR은 goal별로 분리**(폐곡선 단위 유지) — 같은 워크트리 순차 작업 허용.

## A — #1584(「초대」 어휘 정렬 3건) + #1511(「명사+중」 잔여 8자리+기계 게이트)
이슈 본문 정본. #1584: 채널 범위 「초대」 잔존 3곳(CreateChannelDialog:60·폰 SidebarScreen:338·AppShell:217 주석+코어 인용) — #1581의 not.toContain("초대") 크로스리드와 정합 유지. #1511: 잔여 8자리 정렬+낱말 게이트 도입(코멘트 편입 2건 포함 — 이슈 코멘트도 정본). 순서 권장: #1511 게이트를 먼저 세우면 #1584 정정이 게이트 아래서 증명됨.

## B — #1515(수명주기 칩 그릇 토큰) + #1516(soft 채움 토큰)
이슈 본문 정본. 디자인 시스템 정본 문서+웹 토큰 구현 동시(문서만 바꾸고 구현 안 바꾸는 반쪽 금지). 대비 쌍은 tokens.contrast.test.ts에 등록. 두 이슈는 같은 토큰 표면이라 한 설계로 풀되 PR 분리.

## C — #1558(SaveButton blocked=!canSave||busy 정본 결함) + #1559(in-flight 접힘 잔여 반)
이슈 본문 정본. #1558은 정본 컴포넌트 수리(소비처 5+ 전수 확인·actionBusy 규율 인용), #1559는 event-subscription-create·InviteSection·WebhookSec 등 잔여 소탕(#1541 파도의 남은 반). #1558 랜딩 후 #1559가 그 정본을 소비하는 순서.

## D — #1479(hangul-word @ 절단) + #1480(멘션 시트 maxHeight 180)
이슈 본문 정본. 폰 전용. #1479=AX 광고 절 끊김(「보내기, @」/「로 부르기」) — hangul-word가 @를 낱말로 보는 문제, packages/momo-core hangul 계열이면 코어 접촉 허용(웹 회귀 가드 필수). #1480=동적 타입 비례+대화 열 잠식 — #1477의 크롬 상수·AX 상한 선례 참조. 시뮬레이터 캡처 필수.

## E — #1361 준비(레포 비변경) — 성재 클릭 직전까지
목표: 성재가 Grok 앱만 열면 되는 상태. ①이 맥에서 로컬 oort 스택 기동(self_host_env.sh 계열 — **세션 후 회수하지 말 것**, 성재 E2E가 이어짐. 포트·기동 명령 기록) ②Agent Port MCP endpoint 활성 확인 — #1363 dual-era 계약대로 initialize 왕복을 curl로 실측(modern 2026-07-28·legacy 2025-11-25 각각), closed capability/tool list 기록 ③dedicated member·pairing 시작 절차를 UX1(#1360) 화면 경로로 확인(스크린샷) ④산출물: `docs/planning/research/2026-08-19-grok-e2e-prep.md` 초안(성재 단계별 안내문+붙여넣을 설정값 자리표시자+검증 커맨드) — **raw secret은 절대 파일에 넣지 않는다**(자리표시자+발급 경로만). 이슈 #1361 자체는 close하지 않는다(수동 E2E 본편은 성재+오케스트레이터 세션).

## 컨텍스트 델타
- 프로덕션=8d0f7d9a(LIVE-5 전 축)·TURN 정적 은퇴 완료·LIVE-5 라벨 0 잔여(#1588).
- E의 스택은 프로덕션 비접촉·로컬 전용. Grok 앱은 같은 맥의 localhost endpoint로 접속(선례 #1344).
