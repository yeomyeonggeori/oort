# goal B5.4b — OpenAI Responses 어댑터 (ADR-0147 이행 완결)

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서. 계약 `AGENTS.md`.
**base = `track/engine`**(B5.4 랜딩 이후). 워크트리 `~/projects/momo-tracks/momo-worktrees/B54b-responses`(브랜치 `feat/B54b-responses`, 생성됨).
근거: ADR-0147(+이행 각주) · B5.4의 `OpenAiCompatProvider`/`HttpTokenRefresher`(재사용) · **Responses API 와이어 실측**(`@openai/codex` 0.144.1 — 요청/응답 형태를 바이너리·공개 문서에서 실측. B5.4 PR #948 본문 §3의 엔드포인트 근거 참조).

## 0. 규율
B5.4와 동일: 자격증명 값 열람 금지 · **PR 후 STOP** · docker 검증 금지(`#[ignore]`만) · momo-server HTTP 0 · 실 OpenAI 호출 금지(mock) · 새 마이그레이션 금지.

## 1. 할 일
- **Responses 어댑터**(agent-worker 두 번째 와이어): oauth-openai envelope일 때 chat/completions 대신 Responses API 형태로 요청 조립·응답 파싱(토큰 사용량 추출 포함 — ledger 계상 유지). 자격증명 기계장치(B5.4 refresh·재봉인)는 그대로 재사용.
- provider 선택 로직: envelope kind → 어댑터 매핑(legacy bearer=chat/completions 유지, oauth-openai=Responses).
- **conformance `#[ignore]`**: mock Responses 서버로 ①티키타카 e2e(멘션→run→Responses 호출→응답 메시지) ②사용량 추출→ledger ③만료→갱신→재봉인 red(B5.4 mock을 Responses 형태로) ④와이어 형태 단정(요청 필수 필드).

## 2. 하지 말 것
스트리밍·tool call·momo-server 변경·새 마이그레이션.

## 3. 검증·PR
cargo check/test/fmt/clippy·구조 grep. PR `feat/B54b-responses` → `track/engine`. 본문: 와이어 실측 근거·red 절차·이탈. **PR 후 STOP.**
