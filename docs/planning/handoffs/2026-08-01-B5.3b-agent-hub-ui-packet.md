# goal B5.3b — 웹 에이전트 허브 UI (D-4/D-5)

너는 momo 레포의 구현 worker다(Claude Opus 5). 이 문서가 유일한 지시서.
**base = `track/engine`**(B5.2 랜딩 이후). 워크트리 `~/projects/momo-tracks/momo-worktrees/B53b-agent-ui`(브랜치 `feat/B53b-agent-ui`, 생성됨).
**트랙 예외**: 성재 직접 지시 흐름(도그푸딩 준비)의 연장 — ENGINE_HANDOFF 통지 기록. 배포 전 design-review(Blocker 0)는 오케스트레이터.
근거: `docs/planning/2026-08-01-b4-contract-diff.md`의 D-4/D-5 쌍 + B5.2 신설 REST(`POST …/agents`·`GET …/agents/{agent}/profile`) + B5.3a가 병렬로 여는 표면(채널 초대·pause·프로필 편집 — **계약은 Swift 실측 기준으로 먼저 구현, B5.3a 랜딩 전이면 해당 버튼은 만들되 오케스트레이터가 접합 확인**).

## 0. 규율
`.env` 금지 · **PR 후 STOP** · 대상 `clients/web/**`만(서버 무접촉) · `~/.claude/skills/momo-design-taste/SKILL.md` 원칙 준수 · 기존 컴포넌트 재사용(EmptyInvite·공유 4상태) · **병렬 B5.3a와 무접촉**.

## 1. 할 일 (도그푸딩 시나리오 순)
- **에이전트 허브 화면**: 워크스페이스의 에이전트 목록(kind=agent roster)·만들기 폼(B5.2 REST)·프로필 카드(모델/티어/상태)·pause 토글·채널 배치 UI.
- **routing 선택기 활성화**: `MentionRoutingBar` 등 capability 판정이 실서버 지원과 일치하게(B4.1 §9.2 원칙 — 거짓 ready 금지, B5.3a 랜딩 후 진짜 ready).
- **승인 화면(D-5)**: 실행 승인 요청 카드의 승인/거절(기존 REST 실측 — B2.2 ApprovalDecision 계열이 이미 있으면 그 계약).
- 캡처: `npm run capture:design`에 신규 화면 추가.

## 2. 검증·PR
`npm run build`+`tsc`+`npm test`+lint+`design_preflight_web.sh`. PR `feat/B53b-agent-ui` → `track/engine`. 본문: 화면 목록·소비 REST 표·프리플라이트 결과·이탈. **PR 후 STOP.**
