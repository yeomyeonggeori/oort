# oort — 경량 PR CI (#1241). PR마다 자동으로 도는 유일한 워크플로다.
#
# 왜 신설했나: 감사 D(research/2026-08-10-buzz-audit-D.md §6)의 실측 —
#   · 워크플로 5개가 전부 `workflow_dispatch`(2026-06-26 과금 중단 조치)
#   · 마지막 실제 빌드 2026-06-25, 그보다 뒤에 들어온 `server-rust`는
#     **CI에서 한 번도 컴파일된 적이 없다**(오늘 프로덕션에서 도는 코드다)
#   · `pull_request` 트리거를 가진 워크플로 0개 → 외부 기여 PR을 기계적으로
#     판정할 수단이 0
# 감사 A(§A5)는 같은 구멍의 라이선스 쪽을 잰다: `deny.toml`·
# `scripts/check_npm_licenses.mjs` 게이트가 `scripts/local_gate.sh`에서만 돌고
# CI에는 없어서, 외부 PR은 라이선스 검사를 한 번도 받지 않는다.
#
# 설계 제약(private + GitHub Free = 월 2,000분, ubuntu 1배·macOS 10배 과금):
#   · **ubuntu-latest 전용.** macOS 러너는 이 워크플로에 들어오지 못한다.
#   · 세 잡뿐이고, 문서 전용 PR은 `changes` 한 잡(≈1분)만 태운다.
#   · `cancel-in-progress`로 연속 푸시의 앞선 런을 즉시 회수한다.
#   · 잡마다 `timeout-minutes`로 소모 상한을 못박는다(런당 최악 32분).
#
# 스코프: 이 워크플로는 **전체 스위트가 아니다.** 컴파일·타입·라이선스까지만
# 본다. 런타임 검증기·Docker 스택·e2e·Swift 트리는 `docs/LOCAL_PR_GATE.md`의
# 로컬 게이트 몫으로 남는다(공개 전환으로 Actions가 무료가 되면 확장 후보).
# 기존 워크플로 5개(ci-build·publish-images·release-*)는 무접촉이다.
name: pr-ci

on:
  pull_request:
    branches:
      # 트랙 파이프라인(docs/TRACKS.md): 워커 PR은 track/engine으로, 통합만
      # main으로 간다. 둘 다 걸어야 PR CI가 실제 머지 경로를 덮는다.
      - main
      - track/engine
  # PR 없이 레인을 한 번 돌려보고 싶을 때(과금 판단·러너 실측용). 이때는
  # 변경 파일 목록이 없으므로 두 레인을 모두 돈다.
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: pr-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── ① 어떤 레인을 돌릴지 판정 ────────────────────────────────────────────
  # checkout 없이 GitHub API로 변경 파일만 읽는다(체크아웃 100MB·수십 초를
  # 문서 전용 PR에 물리지 않기 위해서다). third-party path-filter 액션을 쓰지
  # 않는 이유도 같다 — 이 판정에 공급망을 하나 더 늘릴 이유가 없다.
  changes:
    name: 변경 경로로 레인 선택
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      rust: ${{ steps.filter.outputs.rust }}
      node: ${{ steps.filter.outputs.node }}
    steps:
      - id: filter
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          set -uo pipefail

          run_all() {
            echo "→ rust=true node=true ($1)"
            echo "rust=true" >> "$GITHUB_OUTPUT"
            echo "node=true" >> "$GITHUB_OUTPUT"
            exit 0
          }

          # workflow_dispatch: PR 번호가 없다.
          [ -n "$PR" ] || run_all "no pull request context"

          # 조회 실패는 "변경 없음"이 아니라 "모르겠음"이다. 모르면 전부 돈다
          # (fail-open의 방향이 CI를 *거르는* 쪽이 되면 게이트가 무의미해진다).
          if ! files="$(gh api --paginate "repos/$REPO/pulls/$PR/files" --jq '.[].filename')"; then
            run_all "could not list changed files"
          fi
          [ -n "$files" ] || run_all "empty file list"

          echo "changed files:"
          printf '%s\n' "$files"

          # 레인별 관심 경로. 목록에 없는 경로(docs/·STATUS.md·infra/·scripts/·
          # 은퇴한 Swift 트리 server/·relay/·workers/·clients/macOS/)만 바뀐 PR은
          # 두 레인 모두 skip된다. skip된 잡은 GitHub에서 성공으로 집계되므로,
          # 공개 전환 후 required check으로 승격해도 문서 PR이 막히지 않는다.
          rust=false
          node=false
          printf '%s\n' "$files" | grep -Eq \
            '^(server-rust/|clients/desktop/src-tauri/|deny\.toml$|scripts/check_cargo_licenses\.sh$|\.github/workflows/pr-ci\.yml$)' \
            && rust=true
          printf '%s\n' "$files" | grep -Eq \
            '^(clients/web/|clients/mobile/|packages/|package\.json$|package-lock\.json$|scripts/check_npm_licenses\.mjs$|\.github/workflows/pr-ci\.yml$)' \
            && node=true

          echo "→ rust=$rust node=$node"
          echo "rust=$rust" >> "$GITHUB_OUTPUT"
          echo "node=$node" >> "$GITHUB_OUTPUT"

  # ── ② Rust 레인 ─────────────────────────────────────────────────────────
  # `cargo check`이지 `cargo build`도 `cargo test`도 아니다. 목표는 "라이브
  # 서버가 컴파일은 되는가"라는, 지금 CI에 통째로 비어 있는 사실 하나다.
  # 테스트 스위트는 DB가 필요한 것이 섞여 있어 로컬 게이트에 남긴다.
  rust:
    name: cargo check + 카고 라이선스 게이트
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: 툴체인 확인(러너 사전설치 stable)
        # rustup 툴체인 액션을 쓰지 않는다. 레포에 rust-toolchain.toml이 없어
        # 고정할 버전이 없고, ubuntu-latest 이미지에 stable이 이미 들어 있다.
        # MSRV는 server-rust/Cargo.toml의 rust-version = "1.80".
        run: |
          rustc --version
          cargo --version

      - name: cargo 캐시(레지스트리 + target)
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            server-rust
            clients/desktop/src-tauri
          # 캐시가 없거나 상해도 게이트 결과는 같다. 오직 시간(=분당 소모)만
          # 달라진다: cold ≈ 5-7분, warm ≈ 1-2분.

      - name: cargo check --workspace (server-rust)
        # --locked: Cargo.lock을 고쳐야 하는 매니페스트 변경이 락 커밋 없이
        # 들어오면 여기서 빨강. 락 갱신은 리뷰 대상이지 CI의 부작용이 아니다.
        run: cargo check --workspace --locked --manifest-path server-rust/Cargo.toml

      - name: cargo-deny 설치
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-deny

      - name: 카고 의존 라이선스 게이트(deny.toml, 두 워크스페이스)
        # #1225가 만든 게이트를 그대로 호출한다. 여기서 CI가 정책을 다시 쓰면
        # 로컬 게이트와 CI가 서로 다른 정책으로 갈라진다.
        # `check licenses`만 돈다(advisories/bans는 #1225 스코프 밖).
        run: scripts/check_cargo_licenses.sh

  # ── ③ Node 레인 ─────────────────────────────────────────────────────────
  # 타입체크 3트리 + npm 라이선스 게이트. lint·단위테스트·vite 빌드는 넣지
  # 않는다(로컬 `web` 프로파일 몫 — 이 레인의 목표는 "타입이 맞는가"다).
  node:
    name: TS 타입체크 + npm 라이선스 게이트
    needs: changes
    if: needs.changes.outputs.node == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          # clients/mobile/package.json engines = ">= 22.11.0"이 레포의 유일한
          # 선언 제약이고, 24는 개발 머신에서 실제로 npm ci가 도는 버전이다.
          node-version: '24'
          cache: npm
          cache-dependency-path: |
            package-lock.json
            clients/web/package-lock.json
            clients/mobile/package-lock.json

      - name: npm 의존 라이선스 게이트(워크스페이스 루트 + web + mobile)
        # 설치 전에 돈다 — 이 스크립트는 package-lock.json만 읽으므로 npm ci가
        # 필요 없다. 라이선스가 막을 PR이면 3트리 설치를 태우기 전에 막는다.
        run: node scripts/check_npm_licenses.mjs

      - name: npm ci (워크스페이스 루트 = packages/momo-core)
        run: npm ci

      - name: npm ci (clients/web)
        run: npm ci --prefix clients/web

      - name: npm ci (clients/mobile)
        run: npm ci --prefix clients/mobile

      - name: typecheck — @momo/core
        run: npm run typecheck --workspace @momo/core

      - name: typecheck — clients/web
        run: npm --prefix clients/web run typecheck

      - name: typecheck — clients/mobile
        run: npm --prefix clients/mobile run typecheck
